---
phase: quick-260521-26u
plan: 01
type: execute
wave: 1
depends_on: []
quick_id: 260521-26u
autonomous: true
requirements: [ACCEL-LINUXONE-01]
files_modified:
  - accelerators/confluent-on-linuxone/DESIGN.md
  - accelerators/confluent-on-linuxone/README.md
  - accelerators/confluent-on-linuxone/ATTRIBUTION.md
  - accelerators/confluent-on-linuxone/KNOWN-GAPS.md
  - accelerators/confluent-on-linuxone/MIGRATION.md
  - accelerators/confluent-on-linuxone/RUNBOOK.md
  - accelerators/confluent-on-linuxone/kustomization.yaml
  - accelerators/confluent-on-linuxone/.flox/env/manifest.toml
  - accelerators/confluent-on-linuxone/.flox/env/manifest.lock
  - accelerators/confluent-on-linuxone/base/
  - accelerators/confluent-on-linuxone/layers/01-rbac/
  - accelerators/confluent-on-linuxone/layers/02-tls/
  - accelerators/confluent-on-linuxone/layers/03-schema-governance/
  - accelerators/confluent-on-linuxone/layers/04-audit/
  - accelerators/confluent-on-linuxone/overlays/dev/kustomization.yaml
  - accelerators/confluent-on-linuxone/overlays/prod/kustomization.yaml
  - .github/workflows/accelerator-linuxone.yml

must_haves:
  truths:
    - "A developer can `flox activate` the accelerator env and get a build-ready toolchain (oc, helm, kubectl, kustomize, cfssl, yq, jq, confluent)"
    - "`base/fetch-upstream.sh` clones Mondics's repo at a pinned SHA into base/upstream/ which is gitignored and never committed"
    - "`kustomize build accelerators/confluent-on-linuxone/overlays/prod` produces one buildable manifest stream composed of base + 4 Components in dependency order"
    - "Each of the 4 FSI layers is its own directory with a README (FSI rationale), a kind:Component kustomization, CR patches, and a validator script"
    - "No secrets are committed — all credential material is <PLACEHOLDER> in secrets.template/ dirs"
    - "auditor-readonly is bound only to _confluent-audit-log-events + SR subjects, never to payments.* business topics"
    - "Top-level README distinguishes IBM/Mondics upstream from GoodLabs additions and attributes Mondics + the article"
    - "MIGRATION.md, KNOWN-GAPS.md, RUNBOOK.md, ATTRIBUTION.md, DESIGN.md are complete and committed"
  artifacts:
    - path: "accelerators/confluent-on-linuxone/DESIGN.md"
      provides: "Committed design record (copy of approved plan content)"
    - path: "accelerators/confluent-on-linuxone/base/fetch-upstream.sh"
      provides: "Fetch-by-SHA scaffold — clone --depth 1 + checkout pinned SHA into upstream/"
    - path: "accelerators/confluent-on-linuxone/base/.gitignore"
      provides: "Ensures upstream/ is never committed"
      contains: "upstream/"
    - path: "accelerators/confluent-on-linuxone/layers/01-rbac/kustomization.yaml"
      provides: "RBAC layer as kind:Component"
      contains: "kind: Component"
    - path: "accelerators/confluent-on-linuxone/layers/02-tls/kustomization.yaml"
      provides: "TLS layer as kind:Component"
      contains: "kind: Component"
    - path: "accelerators/confluent-on-linuxone/layers/03-schema-governance/kustomization.yaml"
      provides: "Schema governance layer as kind:Component"
      contains: "kind: Component"
    - path: "accelerators/confluent-on-linuxone/layers/04-audit/kustomization.yaml"
      provides: "Audit layer as kind:Component"
      contains: "kind: Component"
    - path: "accelerators/confluent-on-linuxone/overlays/prod/kustomization.yaml"
      provides: "Single buildable Kustomization for prod — base + all 4 components"
      contains: "components:"
    - path: "accelerators/confluent-on-linuxone/.flox/env/manifest.toml"
      provides: "Pinned dev toolchain"
  key_links:
    - from: "overlays/prod/kustomization.yaml"
      to: "base/ + layers/01-04"
      via: "resources: [../../base] + components: [01-rbac,02-tls,03-schema-governance,04-audit]"
      pattern: "components:"
    - from: "layers/04-audit kafka audit patch"
      to: "layers/01-rbac ConfluentServerAuthorizer"
      via: "component ordering (RBAC before audit)"
      pattern: "01-rbac.*04-audit"
    - from: ".flox/env/manifest.toml [hook]"
      to: "base/fetch-upstream.sh"
      via: "on-activate hook runs fetch script"
      pattern: "fetch-upstream"
---

<objective>
Build the `accelerators/confluent-on-linuxone/` directory — a new top-level tier of the
fsi-kafka-platform repo — by forking Matt Mondics's public Confluent-on-LinuxONE runbook
as a fetch-by-SHA base and layering four FSI hardening controls (RBAC, mTLS, Schema
Registry governance, audit logging) as Kustomize Components.

Purpose: An FSI team can `flox activate`, follow `RUNBOOK.md`, and deploy production-grade
hardened Confluent Platform on LinuxONE/OpenShift with each control independently validated —
seeing the basic IBM example and the enterprise-grade GoodLabs additions side by side.

Output: Complete `accelerators/confluent-on-linuxone/` tree (DESIGN, README, ATTRIBUTION,
KNOWN-GAPS, MIGRATION, RUNBOOK, Flox env, base/ scaffold, 4 layer Components, dev/prod
overlays) plus a CI build-and-lint job. Each of the 10 implementation-sequence steps is
its own atomic commit.

This plan implements EXACTLY the approved plan at
@/Users/jhogan/.claude/plans/look-into-planning-to-refactored-goose.md — do not redesign,
do not second-guess. Decompose and execute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhogan/.claude/plans/look-into-planning-to-refactored-goose.md
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md

# Reference assets to wire in — DO NOT MODIFY these:
@docs/adr/009-linuxone-deployment-guidance.md
@scenarios/cfk-openshift-linuxone/values/kafka.yaml

# Reuse-map assets (read for conventions, do not modify):
# - ansible/roles/cfk_operator/    (CFK operator install — RUNBOOK calls this)
# - ansible/roles/cp_rbac/         (RBAC drift reconciliation pass)
# - ansible/roles/cp_mtls/         (CA/leaf PEM minting)
# - LinuxOne/ansible-mtls/         (CA/cert PEMs feeding spec.tls.secretRef)
# - observability/splunk/          (SIEM dashboards — referenced by layer 04)
# - observability/dynatrace/       (SIEM dashboards — referenced by layer 04)
# CI scripts live in ci/scripts/ ; workflows in .github/workflows/
</context>

<constraints>
- The approved plan is the authoritative, locked spec. Implement it; do not redesign.
- Locked decisions (HONOR EXACTLY):
  - D-01: Upstream base = fetch-by-SHA. `base/upstream/` is gitignored and NEVER committed.
  - D-02: `auditor-readonly` = audit-topic-scoped — bound to `_confluent-audit-log-events`
    + SR subjects (metadata) + read cluster metadata ONLY; NOT bound to `payments.*` topics.
  - D-03: Reuse existing repo assets (cfk_operator, cp_rbac, cp_mtls, observability/,
    scenarios/cfk-openshift-linuxone/, docs/adr/009) — wire in, do not duplicate.
- Tech stack: CP 8.2.0, CFK 3.2.x, KRaft, `platform.confluent.io/v1beta1` CRs.
- s390x compatibility for every cluster image; gaps go in KNOWN-GAPS.md.
- CFK-operator-managed only — no parallel broker management path.
- Idempotent / declarative; no secrets committed (`<PLACEHOLDER>` only).
- No new platform deps beyond cert-manager (a standard OpenShift operator).
- Each layer kustomization is `kind: Component` (`kustomize.config.k8s.io/v1alpha1`);
  only `overlays/{dev,prod}/kustomization.yaml` are buildable `kind: Kustomization`.
- Components apply in order 01→04 (RBAC's authorizer before audit; TLS secrets before MDS).
- Prefer first-class CR fields over raw `configOverrides` to keep patches non-colliding.
- ATOMIC COMMITS: one commit per task, using the task's `commit` message.
- Cluster-dependent validators (`validate-*.sh`) are AUTHORED here but EXECUTED by the
  developer against a real OCP-on-LinuxONE cluster. CI/local verification is limited to
  `kustomize build` and `kubeconform`/CRD-lint.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Directory structure + DESIGN.md</name>
  <files>
    accelerators/confluent-on-linuxone/DESIGN.md
    (plus mkdir: base/, layers/{01-rbac,02-tls,03-schema-governance,04-audit}/, overlays/{dev,prod}/)
  </files>
  <action>
    Create the `accelerators/confluent-on-linuxone/` directory tree per the approved plan's
    "Directory structure" section (lines 41-64): create empty dirs `base/`,
    `layers/01-rbac/`, `layers/02-tls/`, `layers/03-schema-governance/`, `layers/04-audit/`,
    `overlays/dev/`, `overlays/prod/` (use `.gitkeep` only where a dir would otherwise be
    empty at commit time — later tasks fill them).
    Create `DESIGN.md` as a committed copy of the approved plan's design content: copy the
    entire content of @/Users/jhogan/.claude/plans/look-into-planning-to-refactored-goose.md
    verbatim into `accelerators/confluent-on-linuxone/DESIGN.md`, adding a one-line header
    note that this is the committed design record reviewed before implementation.
    This is implementation-sequence step 1 (per D — the design doc is committed first for
    the review record).
  </action>
  <verify>
    <automated>test -f accelerators/confluent-on-linuxone/DESIGN.md && grep -q "Fork & FSI-harden" accelerators/confluent-on-linuxone/DESIGN.md && test -d accelerators/confluent-on-linuxone/layers/01-rbac && test -d accelerators/confluent-on-linuxone/overlays/prod</automated>
  </verify>
  <done>DESIGN.md exists with the full approved-plan content; the directory skeleton (base, 4 layer dirs, 2 overlay dirs) exists.</done>
  <commit>feat(accelerator): scaffold confluent-on-linuxone directory + commit DESIGN.md</commit>
</task>

<task type="auto">
  <name>Task 2: Flox dev environment (manifest.toml + manifest.lock)</name>
  <files>
    accelerators/confluent-on-linuxone/.flox/env/manifest.toml
    accelerators/confluent-on-linuxone/.flox/env/manifest.lock
  </files>
  <action>
    Create the Flox v1-schema dev environment per approved plan "Flox dev environment"
    (lines 165-173). `.flox/env/manifest.toml`: `version = 1`; `[install]` pins `oc`
    (OCP 4.14+ client), `helm` (3.x), `kubectl`, `kustomize`, `cfssl`, `yq`, `jq`, and
    `confluent` CLI. Add a `[hook]` `on-activate` block that runs `base/fetch-upstream.sh`
    so a freshly-activated env is build-ready (guard so it no-ops if upstream/ already
    populated). Include `why` comments per package (project convention: configs explain
    why). Generate `manifest.lock` by running `flox lock-manifest` (or `flox activate`)
    if Flox is available on the workstation; if Flox is not installed, write a minimal
    valid lock stub and note in KNOWN-GAPS that the lock must be regenerated on a
    Flox-equipped machine. Flox runs on the developer's macOS workstation — s390x does
    not constrain the toolchain.
  </action>
  <verify>
    <automated>test -f accelerators/confluent-on-linuxone/.flox/env/manifest.toml && grep -q "version = 1" accelerators/confluent-on-linuxone/.flox/env/manifest.toml && grep -Eq 'oc|kustomize|confluent' accelerators/confluent-on-linuxone/.flox/env/manifest.toml && grep -q "fetch-upstream" accelerators/confluent-on-linuxone/.flox/env/manifest.toml</automated>
  </verify>
  <done>manifest.toml is a valid v1 Flox manifest pinning all 8 tools with an on-activate hook calling fetch-upstream.sh; manifest.lock exists.</done>
  <commit>feat(accelerator): pin Flox dev toolchain for confluent-on-linuxone</commit>
</task>

<task type="auto">
  <name>Task 3: base/ fetch-by-SHA scaffold + RUNBOOK.md + ATTRIBUTION.md</name>
  <files>
    accelerators/confluent-on-linuxone/base/fetch-upstream.sh
    accelerators/confluent-on-linuxone/base/UPSTREAM.md
    accelerators/confluent-on-linuxone/base/kustomization.yaml
    accelerators/confluent-on-linuxone/base/.gitignore
    accelerators/confluent-on-linuxone/RUNBOOK.md
    accelerators/confluent-on-linuxone/ATTRIBUTION.md
  </files>
  <action>
    Implement implementation-sequence step 3 and locked decision D-01.
    `base/fetch-upstream.sh`: `set -euo pipefail`; `git clone --depth 1` of
    `mmondics/Confluent-LinuxONE-Mirror`, `git checkout <PINNED_SHA>` into `base/upstream/`;
    idempotent (skip if upstream/ already at the pinned SHA); echo progress. Use a clearly
    marked `UPSTREAM_SHA` variable near the top.
    `base/UPSTREAM.md`: repo URL, the pinned commit SHA, and a "do not edit upstream/ —
    regenerate via fetch-upstream.sh" notice.
    `base/kustomization.yaml`: `kind: Kustomization`, `resources:` referencing the upstream
    manifests under `./upstream/` (confluent-platform / C3 / clusterlink / kafkarestclass /
    producer-app-data — per approved plan line 54).
    `base/.gitignore`: single line `upstream/` (MUST ensure fetched code is never committed).
    `RUNBOOK.md`: adapted wrapper (not a rewrite) per approved plan lines 177-179 — walks
    deploy end-to-end, cites Mondics's `cluster-linking-runbook.md` for base steps, inserts
    FSI layer-apply + validation steps, and CLEARLY MARKS upstream vs GoodLabs steps. RUNBOOK
    calls `ansible/roles/cfk_operator` for the operator install (Reuse Map, D-03), not raw
    `helm install`.
    `ATTRIBUTION.md`: credits Matt Mondics + the 2026-05-20 article, states the license-gap
    (upstream has no LICENSE) as the reason for fetch-by-SHA.
  </action>
  <verify>
    <automated>test -x accelerators/confluent-on-linuxone/base/fetch-upstream.sh -o -f accelerators/confluent-on-linuxone/base/fetch-upstream.sh && grep -q "^upstream/$" accelerators/confluent-on-linuxone/base/.gitignore && bash -n accelerators/confluent-on-linuxone/base/fetch-upstream.sh && grep -q "UPSTREAM_SHA" accelerators/confluent-on-linuxone/base/fetch-upstream.sh && test -f accelerators/confluent-on-linuxone/RUNBOOK.md && test -f accelerators/confluent-on-linuxone/ATTRIBUTION.md && grep -qi "Mondics" accelerators/confluent-on-linuxone/ATTRIBUTION.md</automated>
  </verify>
  <done>fetch-upstream.sh passes `bash -n` and uses a pinned SHA; base/.gitignore excludes upstream/; base/kustomization.yaml references upstream resources; RUNBOOK.md marks upstream vs GoodLabs and calls cfk_operator; ATTRIBUTION.md credits Mondics + cites the license gap.</done>
  <commit>feat(accelerator): add fetch-by-SHA base scaffold, RUNBOOK, ATTRIBUTION</commit>
</task>

<task type="auto">
  <name>Task 4: layers/01-rbac/ — MDS RBAC Component</name>
  <files>
    accelerators/confluent-on-linuxone/layers/01-rbac/README.md
    accelerators/confluent-on-linuxone/layers/01-rbac/kustomization.yaml
    accelerators/confluent-on-linuxone/layers/01-rbac/patches/ (Kafka MDS patch + SR/KsqlDB/KafkaRestProxy/ControlCenter dependencies.mds patches)
    accelerators/confluent-on-linuxone/layers/01-rbac/rolebindings/ (6 ConfluentRolebinding CRs)
    accelerators/confluent-on-linuxone/layers/01-rbac/secrets.template/ (mds-token-keypair, ldap-credentials)
    accelerators/confluent-on-linuxone/layers/01-rbac/validate-rbac.sh
  </files>
  <action>
    Implement approved plan "Layer detail 1" (lines 99-114). `kustomization.yaml`:
    `kind: Component` (`apiVersion: kustomize.config.k8s.io/v1alpha1`) carrying this layer's
    `patches:` + `resources:`.
    Kafka CR patch: `spec.authorization.type: rbac`; `spec.services.mds` with TLS,
    `tokenKeyPair.secretRef`, `provider.type: ldap` + LDAP config block as the IdP boundary.
    SR / KsqlDB / KafkaRestProxy / ControlCenter patches add `dependencies.mds`.
    `ConfluentRolebinding` CRs mapping the 6 FSI roles to Confluent predefined roles exactly
    per lines 103-108: platform-admin→SystemAdmin; topic-admin→ResourceOwner Topic PREFIXED;
    producer-only→DeveloperWrite; consumer-only→DeveloperRead (Topic+Group PREFIXED);
    schema-admin→ResourceOwner Subject PREFIXED (schemaRegistryClusterId scope);
    auditor-readonly→DeveloperRead on `_confluent-audit-log-events` + SR subjects ONLY
    (D-02 — explicitly NOT bound to payments.* business topics; add an inline comment
    citing D-02). Sample bindings for a `payments.` domain bind to LDAP `type: group`,
    never individuals.
    `secrets.template/`: `mds-token-keypair` and `ldap-credentials` Secret manifests with
    every credential value as `<PLACEHOLDER>`.
    `validate-rbac.sh`: `set -euo pipefail`; runs `confluent iam rbac role-binding list`
    and asserts a `producer-only` principal attempting consume returns 403. Header comment:
    cluster-dependent, executed by developer against a real OCP-on-LinuxONE cluster.
    `README.md`: FSI rationale (segregation of duties, least privilege — SOX/FFIEC/PCI-DSS;
    failure mode = unbounded cluster admin); cross-link `docs/adr/009`; note cp_rbac role
    provides the optional LIST/DIFF/ADD/REMOVE drift-reconciliation pass (Reuse Map, D-03).
  </action>
  <verify>
    <automated>grep -q "kind: Component" accelerators/confluent-on-linuxone/layers/01-rbac/kustomization.yaml && bash -n accelerators/confluent-on-linuxone/layers/01-rbac/validate-rbac.sh && grep -rq "_confluent-audit-log-events" accelerators/confluent-on-linuxone/layers/01-rbac/rolebindings/ && grep -rL "payments" accelerators/confluent-on-linuxone/layers/01-rbac/rolebindings/*audit* 2>/dev/null; ! grep -rl "payments" accelerators/confluent-on-linuxone/layers/01-rbac/rolebindings/ | xargs -r grep -l "auditor-readonly" ; grep -rq "PLACEHOLDER" accelerators/confluent-on-linuxone/layers/01-rbac/secrets.template/</automated>
  </verify>
  <done>01-rbac is a kind:Component with the Kafka MDS patch + dependency patches, 6 ConfluentRolebinding CRs (auditor-readonly audit-scoped only per D-02), secrets.template/ with <PLACEHOLDER>s, and validate-rbac.sh passing `bash -n`. README states FSI rationale and reuse-map links.</done>
  <commit>feat(accelerator): add 01-rbac MDS RBAC hardening layer</commit>
</task>

<task type="auto">
  <name>Task 5: layers/02-tls/ — mTLS + FIPS Component</name>
  <files>
    accelerators/confluent-on-linuxone/layers/02-tls/README.md
    accelerators/confluent-on-linuxone/layers/02-tls/kustomization.yaml
    accelerators/confluent-on-linuxone/layers/02-tls/patches/ (per-component TLS patches)
    accelerators/confluent-on-linuxone/layers/02-tls/certificate-crs.yaml
    accelerators/confluent-on-linuxone/layers/02-tls/route-tls.yaml
    accelerators/confluent-on-linuxone/layers/02-tls/secrets.template/ (CA + per-component cert PEMs)
    accelerators/confluent-on-linuxone/layers/02-tls/validate-mtls.sh
  </files>
  <action>
    Implement approved plan "Layer detail 2" (lines 116-131). `kustomization.yaml`:
    `kind: Component`.
    Per-component patches replace `autoGeneratedCerts` with `spec.tls.secretRef` (PEM
    cert+key+CA secret); set `listeners.internal.authentication.type: mtls` for
    broker/controller/SR/C3-to-broker mTLS; external listener keeps Route edge-TLS.
    Set `spec.tls.fips.enabled: true` on every component CR. `configOverrides.server`:
    `ssl.enabled.protocols=TLSv1.3,TLSv1.2` and FIPS-compatible `ssl.cipher.suites`.
    `certificate-crs.yaml`: cert-manager `Issuer`/`Certificate` CRs for declarative
    auto-rotation (cert-manager Operator for OpenShift — s390x-supported, FIPS-validated
    >=1.14.0). Document the manual-rotation runbook as the no-cert-manager fallback in
    the README.
    `route-tls.yaml`: OpenShift Route with edge TLS for the external listener.
    `secrets.template/`: CA + per-component cert PEM Secret manifests, all `<PLACEHOLDER>`.
    Source s390x node-affinity / FIPS conventions from
    `scenarios/cfk-openshift-linuxone/values/kafka.yaml` (Reuse Map, D-03).
    `validate-mtls.sh`: `set -euo pipefail`; client with no cert → SSL handshake rejected;
    client with valid cert → `kafka-broker-api-versions` succeeds. Header: cluster-dependent.
    `README.md`: FSI rationale (encryption-in-transit + mutual auth — GLBA/PCI-DSS 4.x/FFIEC;
    failure mode = unauthenticated broker access / cleartext interception); document the
    s390x caveat that FIPS must be enabled at OCP install time (`spec.tls.fips.enabled`
    only takes effect on an already-FIPS cluster) and cross-link KNOWN-GAPS;
    note LinuxOne/ansible-mtls + cp_mtls mint the CA/leaf PEMs that become these secrets.
  </action>
  <verify>
    <automated>grep -q "kind: Component" accelerators/confluent-on-linuxone/layers/02-tls/kustomization.yaml && bash -n accelerators/confluent-on-linuxone/layers/02-tls/validate-mtls.sh && grep -rq "fips" accelerators/confluent-on-linuxone/layers/02-tls/patches/ && grep -q "Certificate" accelerators/confluent-on-linuxone/layers/02-tls/certificate-crs.yaml && grep -rq "PLACEHOLDER" accelerators/confluent-on-linuxone/layers/02-tls/secrets.template/</automated>
  </verify>
  <done>02-tls is a kind:Component with per-component TLS patches (secretRef + mtls + fips), cert-manager Issuer/Certificate CRs, route-tls, secrets.template/ with <PLACEHOLDER>s, validate-mtls.sh passing `bash -n`. README documents FIPS-at-install caveat and reuse-map links.</done>
  <commit>feat(accelerator): add 02-tls mTLS + FIPS hardening layer</commit>
</task>

<task type="auto">
  <name>Task 6: layers/03-schema-governance/ — Schema Registry governance Component</name>
  <files>
    accelerators/confluent-on-linuxone/layers/03-schema-governance/README.md
    accelerators/confluent-on-linuxone/layers/03-schema-governance/kustomization.yaml
    accelerators/confluent-on-linuxone/layers/03-schema-governance/patches/ (SR governance patch)
    accelerators/confluent-on-linuxone/layers/03-schema-governance/job-sr-bootstrap.yaml
    accelerators/confluent-on-linuxone/layers/03-schema-governance/validate-schema-governance.sh
  </files>
  <action>
    Implement approved plan "Layer detail 3" (lines 133-145). `kustomization.yaml`:
    `kind: Component`.
    SR CR patch: `configOverrides.server` sets
    `schema.compatibility.level=full_transitive` (prod) and subject-naming validation;
    make the subject-naming regex an overlay-configurable variable.
    `job-sr-bootstrap.yaml`: idempotent `restartPolicy: Never` Job issuing REST
    `PUT /config` (FULL_TRANSITIVE) + per-subject mode — the one sanctioned explicit
    bootstrap (PUTs are idempotent). Job base image MUST be s390x (UBI9 + curl) — note
    this for KNOWN-GAPS task.
    Hard delete: documented (not a single CR flag) — controlled by withholding
    delete-capable `ResourceOwner` roles broadly + the `?permanent=true` restriction;
    note RBAC subject-prefix scoping from layer 01 is the real enforcement boundary.
    `validate-schema-governance.sh`: `set -euo pipefail`; bad subject name → rejected;
    incompatible change → FULL_TRANSITIVE rejects; hard-delete by non-privileged
    principal → 403. Header: cluster-dependent.
    `README.md`: FSI rationale (auditability of data contracts over time, regulatory
    replay; failure mode = silent breaking schema change / unrecoverable hard delete);
    cross-link `docs/adr/002-compatibility-by-tier.md` and ADR-009 as relevant.
  </action>
  <verify>
    <automated>grep -q "kind: Component" accelerators/confluent-on-linuxone/layers/03-schema-governance/kustomization.yaml && bash -n accelerators/confluent-on-linuxone/layers/03-schema-governance/validate-schema-governance.sh && grep -q "restartPolicy: Never" accelerators/confluent-on-linuxone/layers/03-schema-governance/job-sr-bootstrap.yaml && grep -rqi "full_transitive" accelerators/confluent-on-linuxone/layers/03-schema-governance/</automated>
  </verify>
  <done>03-schema-governance is a kind:Component with the SR governance patch (full_transitive + subject-naming regex), an idempotent restartPolicy:Never bootstrap Job, and validate-schema-governance.sh passing `bash -n`. README documents hard-delete enforcement and FSI rationale.</done>
  <commit>feat(accelerator): add 03-schema-governance hardening layer</commit>
</task>

<task type="auto">
  <name>Task 7: layers/04-audit/ — Audit logging Component</name>
  <files>
    accelerators/confluent-on-linuxone/layers/04-audit/README.md
    accelerators/confluent-on-linuxone/layers/04-audit/kustomization.yaml
    accelerators/confluent-on-linuxone/layers/04-audit/patches/ (Kafka audit patch)
    accelerators/confluent-on-linuxone/layers/04-audit/kafkatopic-audit.yaml
    accelerators/confluent-on-linuxone/layers/04-audit/connect-cr.yaml (Connect CR + Splunk Sink + HTTP Sink Connectors)
    accelerators/confluent-on-linuxone/layers/04-audit/siem/ (references to observability/splunk + observability/dynatrace)
    accelerators/confluent-on-linuxone/layers/04-audit/validate-audit.sh
  </files>
  <action>
    Implement approved plan "Layer detail 4" (lines 147-163). `kustomization.yaml`:
    `kind: Component`.
    Kafka CR patch: `confluent.security.event.router.config` JSON routing auth attempts,
    authz decisions, and management events to `_confluent-audit-log-events`. This depends
    on layer 01's ConfluentServerAuthorizer — rely on component ordering (01 before 04);
    write the patch on a distinct CR field to avoid JSON-merge collisions per the
    composition-mechanism rule.
    `kafkatopic-audit.yaml`: a `KafkaTopic` CR pre-creating `_confluent-audit-log-events`
    with `retentionMs` 7y (`220752000000`) prod / 30d dev — make retention an overlay
    variable so it is declarative and authoritative (not broker auto-create).
    `connect-cr.yaml`: a `Connect` CR (upstream has Connect commented out) + `Connector`
    CRs — a Splunk Sink (HEC) and a generic HTTP Sink → Dynatrace ingest API (no
    first-party Dynatrace connector — flag in KNOWN-GAPS). Note connector JARs are
    arch-neutral Java baked into a custom s390x Connect image.
    `siem/`: reference files / README pointing to `observability/splunk/` and
    `observability/dynatrace/` dashboards — no duplication (Reuse Map, D-03).
    `validate-audit.sh`: `set -euo pipefail`; generate each event type, consume the audit
    topic, confirm each lands. Header: cluster-dependent.
    `README.md`: document captured events (authn attempts, authz decisions, schema changes,
    topic config changes, ACL/rolebinding changes); FSI rationale (immutable audit trail
    for OFAC/AML/SOX, 7y retention; failure mode = no forensic record of access).
  </action>
  <verify>
    <automated>grep -q "kind: Component" accelerators/confluent-on-linuxone/layers/04-audit/kustomization.yaml && bash -n accelerators/confluent-on-linuxone/layers/04-audit/validate-audit.sh && grep -q "220752000000" accelerators/confluent-on-linuxone/layers/04-audit/kafkatopic-audit.yaml && grep -qi "Connect" accelerators/confluent-on-linuxone/layers/04-audit/connect-cr.yaml && grep -rqi "_confluent-audit-log-events" accelerators/confluent-on-linuxone/layers/04-audit/</automated>
  </verify>
  <done>04-audit is a kind:Component with the Kafka audit-router patch, a 7y/30d-retention audit KafkaTopic CR, a Connect CR + Splunk/HTTP-Dynatrace Sink Connectors, siem/ references to observability assets, and validate-audit.sh passing `bash -n`. README lists captured events + FSI rationale.</done>
  <commit>feat(accelerator): add 04-audit audit-logging hardening layer</commit>
</task>

<task type="auto">
  <name>Task 8: MIGRATION.md — Cluster Linking wrap</name>
  <files>accelerators/confluent-on-linuxone/MIGRATION.md</files>
  <action>
    Implement approved plan lines 180-184 (MIGRATION.md). Wrap Mondics's Cluster Linking
    pattern (`base/clusterlink.yaml` from the fetched upstream) for x86→LinuxONE migration
    with four sections:
    1. Pre-migration audit checklist — verify RBAC / TLS / schema-governance / audit
       posture on the source cluster before linking.
    2. In-flight data validation — mirror lag → 0, record-count parity, schema parity.
    3. Rollback procedure — re-point producers, demote the mirror topic.
    4. Post-migration evidence collection for regulatory review.
    Cross-link `docs/adr/005-cluster-linking-over-mrc.md`. Technical and dense.
  </action>
  <verify>
    <automated>test -f accelerators/confluent-on-linuxone/MIGRATION.md && grep -qi "rollback" accelerators/confluent-on-linuxone/MIGRATION.md && grep -qi "mirror lag" accelerators/confluent-on-linuxone/MIGRATION.md</automated>
  </verify>
  <done>MIGRATION.md exists with all four sections (pre-migration audit, in-flight validation, rollback, post-migration evidence) and cross-links the Cluster Linking ADR.</done>
  <commit>docs(accelerator): add MIGRATION.md Cluster Linking runbook</commit>
</task>

<task type="auto">
  <name>Task 9: KNOWN-GAPS.md — s390x gap register</name>
  <files>accelerators/confluent-on-linuxone/KNOWN-GAPS.md</files>
  <action>
    Implement approved plan lines 185-188 (KNOWN-GAPS.md). s390x gap register, each entry
    with the gap + workaround:
    - fetch-by-SHA network-access requirement (D-01 — upstream cloned at activation).
    - FIPS-at-OCP-install constraint (`spec.tls.fips.enabled` only effective on an
      already-FIPS cluster).
    - No native Dynatrace connector — HTTP Sink workaround (from layer 04).
    - Verify Control Center next-gen s390x image availability.
    - SR bootstrap-Job base image must be s390x (UBI9 + curl).
    - Prefer Connect-based SIEM shipping over x86 node-agent sidecars (QEMU penalty).
    - Flox manifest.lock must be regenerated on a Flox-equipped machine if it could not
      be locked during Task 2.
    Use a clear table or per-entry format: Gap | Impact | Workaround | Status.
  </action>
  <verify>
    <automated>test -f accelerators/confluent-on-linuxone/KNOWN-GAPS.md && grep -qi "FIPS" accelerators/confluent-on-linuxone/KNOWN-GAPS.md && grep -qi "Dynatrace" accelerators/confluent-on-linuxone/KNOWN-GAPS.md && grep -qi "s390x" accelerators/confluent-on-linuxone/KNOWN-GAPS.md</automated>
  </verify>
  <done>KNOWN-GAPS.md exists registering all s390x gaps from the approved plan, each with a documented workaround.</done>
  <commit>docs(accelerator): add KNOWN-GAPS.md s390x gap register</commit>
</task>

<task type="auto">
  <name>Task 10: Top-level README + overlays + CI job</name>
  <files>
    accelerators/confluent-on-linuxone/README.md
    accelerators/confluent-on-linuxone/kustomization.yaml
    accelerators/confluent-on-linuxone/overlays/dev/kustomization.yaml
    accelerators/confluent-on-linuxone/overlays/prod/kustomization.yaml
    .github/workflows/accelerator-linuxone.yml
  </files>
  <action>
    Implement approved plan implementation-sequence step 10 (lines 189-204).
    `overlays/prod/kustomization.yaml`: the single buildable `kind: Kustomization` —
    `resources: [../../base]` + `components: [../../layers/01-rbac, ../../layers/02-tls,
    ../../layers/03-schema-governance, ../../layers/04-audit]` (order encodes dependencies);
    set prod overlay variables — FULL_TRANSITIVE compat, 7y audit retention.
    `overlays/dev/kustomization.yaml`: same base + 4 components, relaxed overlay variables
    — 30d audit retention, BACKWARD compat.
    Top-level `kustomization.yaml`: default build target pointing to `overlays/prod` (per
    approved plan line 49).
    `README.md`: top-level — provenance table (IBM/Mondics upstream vs GoodLabs additions),
    Mondics attribution + article cite, the four hardening layers each with FSI rationale,
    and build/validate instructions (`flox activate` → `fetch-upstream.sh` → `kustomize
    build overlays/prod` → `oc apply` with `--dry-run=server` → per-layer validators).
    Technical and dense, no marketing.
    `.github/workflows/accelerator-linuxone.yml`: CI job triggered on changes under
    `accelerators/confluent-on-linuxone/**` that runs `base/fetch-upstream.sh` then
    `kustomize build` on both overlays and a CRD-schema lint (kubeconform) — extends the
    existing `ci/scripts` pattern. Note in the workflow that cluster-dependent validators
    are NOT run in CI (no cluster available).
  </action>
  <verify>
    <automated>grep -q "components:" accelerators/confluent-on-linuxone/overlays/prod/kustomization.yaml && grep -q "components:" accelerators/confluent-on-linuxone/overlays/dev/kustomization.yaml && test -f accelerators/confluent-on-linuxone/README.md && grep -qi "Mondics" accelerators/confluent-on-linuxone/README.md && test -f .github/workflows/accelerator-linuxone.yml && grep -q "kustomize build" .github/workflows/accelerator-linuxone.yml && (command -v kustomize >/dev/null && kustomize build accelerators/confluent-on-linuxone/overlays/dev >/dev/null 2>&1 && echo "kustomize-build-ok" || echo "kustomize-build-deferred: run after fetch-upstream.sh on a kustomize-equipped machine")</automated>
  </verify>
  <done>overlays/{dev,prod}/kustomization.yaml each compose base + 4 components in order with environment-specific variables; top-level kustomization.yaml targets prod; README distinguishes upstream vs GoodLabs and attributes Mondics; CI workflow runs fetch + kustomize build + CRD lint on both overlays.</done>
  <commit>feat(accelerator): add top-level README, dev/prod overlays, CI build job</commit>
</task>

</tasks>

<verification>
- Directory tree matches the approved plan's "Directory structure" (lines 41-64).
- `base/.gitignore` excludes `upstream/` — fetched upstream code is NEVER committed (D-01).
- Each of `layers/01-04` is a `kind: Component` with a README, CR patches, and a validator.
- `auditor-readonly` rolebinding is scoped to `_confluent-audit-log-events` + SR subjects
  only — NOT to `payments.*` business topics (D-02).
- `overlays/{dev,prod}/kustomization.yaml` are the only buildable `kind: Kustomization`
  files; each composes `base` + the 4 components in order 01→04.
- No secrets committed — all credential values are `<PLACEHOLDER>` in secrets.template/.
- All `*.sh` files pass `bash -n`.
- `kustomize build overlays/dev` and `overlays/prod` succeed AFTER `base/fetch-upstream.sh`
  has populated `base/upstream/` (run on a kustomize-equipped machine; deferred in CI to
  the workflow itself).
- CI workflow `.github/workflows/accelerator-linuxone.yml` runs fetch + kustomize build +
  CRD lint and explicitly does NOT run cluster-dependent validators.
- 10 atomic commits, one per task, with the specified commit messages.
</verification>

<success_criteria>
- `accelerators/confluent-on-linuxone/` exists with DESIGN, README, ATTRIBUTION,
  KNOWN-GAPS, MIGRATION, RUNBOOK, top-level kustomization.yaml, Flox env, base/ scaffold,
  4 layer Components, and dev/prod overlays — matching the approved plan exactly.
- All 10 implementation-sequence steps are committed atomically.
- A developer with an OCP-on-LinuxONE cluster can clone, `flox activate`, follow
  RUNBOOK.md, and deploy production-grade CP with all 4 FSI layers; each layer's validator
  is authored and ready to run against the cluster.
- Locked decisions D-01 (fetch-by-SHA, upstream gitignored), D-02 (auditor-readonly
  audit-scoped), D-03 (reuse existing assets) are honored throughout.
- CI build-and-lint job is wired and references the existing ci/scripts pattern.
</success_criteria>

<output>
After completion, create
`.planning/quick/260521-26u-build-accelerators-confluent-on-linuxone/260521-26u-SUMMARY.md`
documenting: files created, the pinned upstream SHA used, any deferred verification
(kustomize build / validators) and why, and the 10 commit SHAs.
</output>
