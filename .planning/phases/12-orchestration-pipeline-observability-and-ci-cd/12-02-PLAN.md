---
phase: 12-orchestration-pipeline-observability-and-ci-cd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/roles/cp_observability/defaults/main.yml
  - ansible/roles/cp_observability/meta/main.yml
  - ansible/roles/cp_observability/tasks/main.yml
  - ansible/roles/cp_observability/tasks/jmx_exporter.yml
  - ansible/roles/cp_observability/tasks/prometheus.yml
  - ansible/roles/cp_observability/tasks/grafana.yml
  - ansible/roles/cp_observability/tasks/check.yml
  - ansible/roles/cp_observability/templates/prometheus_targets.json.j2
  - ansible/roles/cp_observability/templates/jmx_exporter_broker.yml.j2
  - ansible/roles/cp_observability/templates/jmx_exporter_connect.yml.j2
  - ansible/roles/cp_observability/templates/jmx_exporter_schema_registry.yml.j2
  - ansible/roles/cp_observability/molecule/default/molecule.yml
  - ansible/roles/cp_observability/molecule/default/converge.yml
  - ansible/roles/cp_observability/molecule/default/verify.yml
  - ansible/requirements.yml
  - tests/ansible/test_cp_observability.py
autonomous: true
requirements:
  - AOBS-01
  - AOBS-02
  - AOBS-03
  - AOBS-04

must_haves:
  truths:
    - "JMX exporter configs are deployed to broker, SR, and Connect nodes from existing observability/ templates"
    - "Prometheus scrape config is auto-generated from inventory host groups and updates when nodes are added"
    - "Grafana dashboards from observability/grafana/ are imported with overwrite support"
    - "Alert rules with SLA-tier-aware thresholds are deployed matching existing alerts.yaml definitions"
  artifacts:
    - path: "ansible/roles/cp_observability/tasks/jmx_exporter.yml"
      provides: "JMX exporter config deployment to CP nodes"
      contains: "ansible.builtin.template"
    - path: "ansible/roles/cp_observability/tasks/prometheus.yml"
      provides: "Prometheus file_sd_configs generation from inventory"
      contains: "prometheus_targets"
    - path: "ansible/roles/cp_observability/tasks/grafana.yml"
      provides: "Grafana dashboard import and alert rule deployment"
      contains: "grafana"
    - path: "ansible/roles/cp_observability/templates/prometheus_targets.json.j2"
      provides: "Jinja2 template for Prometheus file_sd_configs"
      contains: "file_sd"
  key_links:
    - from: "ansible/roles/cp_observability/tasks/prometheus.yml"
      to: "ansible/roles/cp_observability/templates/prometheus_targets.json.j2"
      via: "ansible.builtin.template"
      pattern: "prometheus_targets"
    - from: "ansible/roles/cp_observability/tasks/jmx_exporter.yml"
      to: "observability/grafana/jmx-exporter-stub.yaml"
      via: "Template derived from existing JMX exporter stub"
      pattern: "jmx_exporter"
    - from: "ansible/roles/cp_observability/tasks/grafana.yml"
      to: "observability/grafana/dashboard-*.json"
      via: "Dashboard import from existing files"
      pattern: "dashboard.*json"
---

<objective>
Create the cp_observability role for deploying JMX exporter configs, generating Prometheus scrape targets from inventory, importing Grafana dashboards, and deploying SLA-tier-aware alert rules.

Purpose: Automate observability stack deployment on CP clusters using existing dashboard templates from Phase 5.
Output: cp_observability role with JMX/Prometheus/Grafana tasks, Jinja2 templates, molecule tests, pytest unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-orchestration-pipeline-observability-and-ci-cd/12-RESEARCH.md

@ansible/roles/cp_topic/tasks/main.yml
@ansible/roles/cp_topic/defaults/main.yml
@ansible/roles/cp_topic/meta/main.yml
@ansible/roles/cp_topic/molecule/default/molecule.yml
@ansible/roles/cp_topic/molecule/default/converge.yml
@ansible/roles/cp_topic/molecule/default/verify.yml
@ansible/vars/sla_tiers.yml
@ansible/requirements.yml
@observability/grafana/jmx-exporter-stub.yaml
@observability/grafana/alerts.yaml
@observability/grafana/dashboard-cluster-health.json
@tests/ansible/conftest.py
@tests/ansible/test_cp_topic.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cp_observability role with JMX, Prometheus, Grafana, and alert deployment</name>
  <files>
    ansible/roles/cp_observability/defaults/main.yml
    ansible/roles/cp_observability/meta/main.yml
    ansible/roles/cp_observability/tasks/main.yml
    ansible/roles/cp_observability/tasks/jmx_exporter.yml
    ansible/roles/cp_observability/tasks/prometheus.yml
    ansible/roles/cp_observability/tasks/grafana.yml
    ansible/roles/cp_observability/tasks/check.yml
    ansible/roles/cp_observability/templates/prometheus_targets.json.j2
    ansible/roles/cp_observability/templates/jmx_exporter_broker.yml.j2
    ansible/roles/cp_observability/templates/jmx_exporter_connect.yml.j2
    ansible/roles/cp_observability/templates/jmx_exporter_schema_registry.yml.j2
    ansible/roles/cp_observability/molecule/default/molecule.yml
    ansible/roles/cp_observability/molecule/default/converge.yml
    ansible/roles/cp_observability/molecule/default/verify.yml
    ansible/requirements.yml
    tests/ansible/test_cp_observability.py
  </files>
  <read_first>
    ansible/roles/cp_topic/tasks/main.yml
    ansible/roles/cp_topic/defaults/main.yml
    ansible/roles/cp_topic/meta/main.yml
    ansible/roles/cp_topic/molecule/default/molecule.yml
    ansible/roles/cp_topic/molecule/default/converge.yml
    ansible/roles/cp_topic/molecule/default/verify.yml
    ansible/vars/sla_tiers.yml
    ansible/requirements.yml
    observability/grafana/jmx-exporter-stub.yaml
    observability/grafana/alerts.yaml
    observability/grafana/dashboard-cluster-health.json
    observability/grafana/README.md
    tests/ansible/conftest.py
    tests/ansible/test_cp_topic.py
  </read_first>
  <behavior>
    - Test: defaults/main.yml has cp_obs_broker_jmx_port (default 8080), cp_obs_sr_jmx_port (default 8078), cp_obs_connect_jmx_port (default 8077)
    - Test: defaults/main.yml has cp_obs_prometheus_targets_dir, cp_obs_jmx_exporter_config_dir, cp_obs_cluster_name, cp_obs_environment
    - Test: defaults/main.yml has cp_obs_grafana_url, cp_obs_grafana_method (default "file_provisioning"), cp_obs_grafana_provisioning_dir
    - Test: defaults/main.yml has cp_obs_deploy_jmx (default true), cp_obs_deploy_prometheus (default true), cp_obs_deploy_grafana (default true), cp_obs_deploy_alerts (default true)
    - Test: meta/main.yml has role_name cp_observability, dependencies []
    - Test: tasks/main.yml dispatches to jmx_exporter.yml, prometheus.yml, grafana.yml based on cp_obs_deploy_* flags
    - Test: tasks/jmx_exporter.yml uses ansible.builtin.template for each component (broker, SR, Connect) targeting appropriate host groups
    - Test: tasks/prometheus.yml uses ansible.builtin.template with prometheus_targets.json.j2 to generate file_sd_configs
    - Test: tasks/grafana.yml supports both methods (file_provisioning and api) based on cp_obs_grafana_method
    - Test: tasks/grafana.yml deploys alert rules from observability/grafana/alerts.yaml with SLA-tier thresholds
    - Test: prometheus_targets.json.j2 iterates over kafka_broker, schema_registry, kafka_connect groups with correct JMX ports
    - Test: JMX exporter templates reference correct default ports per component
    - Test: requirements.yml includes community.grafana collection
    - Test: All task names start with uppercase, all modules use FQCN
  </behavior>
  <action>
    **Write tests first (RED phase):**

    1. Create `tests/ansible/test_cp_observability.py`:
       - TestRoleStructure: Validates defaults/main.yml has all expected keys with correct default values:
         - cp_obs_broker_jmx_port: 8080
         - cp_obs_sr_jmx_port: 8078
         - cp_obs_connect_jmx_port: 8077
         - cp_obs_zk_jmx_port: 8079
         - cp_obs_jmx_exporter_config_dir: "/opt/prometheus/jmx-exporter"
         - cp_obs_prometheus_targets_dir: "/etc/prometheus/file_sd"
         - cp_obs_grafana_url: "http://localhost:3000"
         - cp_obs_grafana_method: "file_provisioning"
         - cp_obs_grafana_provisioning_dir: "/etc/grafana/provisioning/dashboards"
         - cp_obs_grafana_alert_provisioning_dir: "/etc/grafana/provisioning/alerting"
         - cp_obs_cluster_name: "fsi-kafka"
         - cp_obs_environment: "prod"
         - cp_obs_deploy_jmx: true
         - cp_obs_deploy_prometheus: true
         - cp_obs_deploy_grafana: true
         - cp_obs_deploy_alerts: true
       - TestJmxExporter: Validates jmx_exporter.yml uses ansible.builtin.template, references jmx_exporter_broker.yml.j2, targets dest with cp_obs_jmx_exporter_config_dir. Validates template files exist for broker, connect, schema_registry.
       - TestPrometheusScrapeGen: Validates prometheus.yml uses ansible.builtin.template with prometheus_targets.json.j2. Validates prometheus_targets.json.j2 contains "kafka_broker", "schema_registry", "kafka_connect" group references and port variables.
       - TestGrafanaDashboards: Validates grafana.yml has tasks for dashboard import. Validates it references observability/grafana/dashboard-*.json files. Validates cp_obs_grafana_method conditional logic.
       - TestAlertRules: Validates grafana.yml includes alert rule deployment task referencing observability/grafana/alerts.yaml.
       - TestObservabilityMolecule: Validates molecule/default/ directory has molecule.yml, converge.yml, verify.yml.

    **Run tests (confirm RED):** `pytest tests/ansible/test_cp_observability.py -x`

    **Implement (GREEN phase):**

    2. Update `ansible/requirements.yml` -- add community.grafana collection:
       ```yaml
       - name: community.grafana
         version: ">=1.7.0"
       ```

    3. Create `ansible/roles/cp_observability/defaults/main.yml` with all defaults listed above.

    4. Create `ansible/roles/cp_observability/meta/main.yml`:
       - galaxy_info: role_name: cp_observability, author: FSI C4E, description: "Observability deployment for Confluent Platform (JMX, Prometheus, Grafana)", min_ansible_version: "2.15", platforms (EL 8/9), galaxy_tags [kafka, observability, prometheus, grafana, jmx]
       - dependencies: []

    5. Create `ansible/roles/cp_observability/tasks/main.yml`:
       - Include check.yml when ansible_check_mode
       - Include jmx_exporter.yml when cp_obs_deploy_jmx (tags: [jmx, observability])
       - Include prometheus.yml when cp_obs_deploy_prometheus (tags: [prometheus, observability])
       - Include grafana.yml when cp_obs_deploy_grafana or cp_obs_deploy_alerts (tags: [grafana, alerts, observability])
       - End with set_fact for cp_observability_results summary

    6. Create `ansible/roles/cp_observability/tasks/jmx_exporter.yml`:
       - Task 1: "Create JMX exporter config directory" using ansible.builtin.file with state: directory
       - Task 2: "Deploy JMX exporter config for brokers" using ansible.builtin.template with src: jmx_exporter_broker.yml.j2, dest: "{{ cp_obs_jmx_exporter_config_dir }}/kafka-broker.yml", when: inventory_hostname in groups.get('kafka_broker', [])
       - Task 3: "Deploy JMX exporter config for Schema Registry" using ansible.builtin.template with src: jmx_exporter_schema_registry.yml.j2, when: inventory_hostname in groups.get('schema_registry', [])
       - Task 4: "Deploy JMX exporter config for Connect" using ansible.builtin.template with src: jmx_exporter_connect.yml.j2, when: inventory_hostname in groups.get('kafka_connect', [])

    7. Create `ansible/roles/cp_observability/tasks/prometheus.yml`:
       - Task 1: "Create Prometheus file_sd directory" using ansible.builtin.file with state: directory on monitoring host
       - Task 2: "Generate Prometheus scrape targets from inventory" using ansible.builtin.template with src: prometheus_targets.json.j2, dest: "{{ cp_obs_prometheus_targets_dir }}/confluent-platform.json"
       - Task targets the monitoring host (first broker or dedicated monitoring host)

    8. Create `ansible/roles/cp_observability/tasks/grafana.yml`:
       - Block 1 (when cp_obs_grafana_method == "file_provisioning"):
         - "Create Grafana dashboard provisioning directory" via ansible.builtin.file
         - "Deploy Grafana dashboards via file provisioning" via ansible.builtin.copy with src referencing each observability/grafana/dashboard-*.json
         - "Deploy Grafana dashboard provisioning config" via ansible.builtin.template generating YAML config for Grafana auto-provisioning
       - Block 2 (when cp_obs_grafana_method == "api"):
         - "Import Grafana dashboards via API" via community.grafana.grafana_dashboard with src for each dashboard JSON, overwrite: true
       - Task: "Deploy alert rules" -- copy observability/grafana/alerts.yaml to Grafana alert provisioning directory with SLA-tier threshold variables applied via ansible.builtin.template

    9. Create `ansible/roles/cp_observability/tasks/check.yml`:
       - GET-only tasks that report current state of JMX configs, Prometheus targets, and Grafana dashboards without mutations

    10. Create Jinja2 templates:
        - `templates/prometheus_targets.json.j2`: Generates JSON array with targets for kafka_broker (port cp_obs_broker_jmx_port), schema_registry (port cp_obs_sr_jmx_port), kafka_connect (port cp_obs_connect_jmx_port). Each entry has targets array and labels (job, cluster, environment). Uses groups[group_name] iteration.
        - `templates/jmx_exporter_broker.yml.j2`: Based on observability/grafana/jmx-exporter-stub.yaml with port {{ cp_obs_broker_jmx_port }}, lowercaseOutputName: true, rules for kafka.server ReplicaManager, BrokerTopicMetrics, KafkaRequestHandlerPool
        - `templates/jmx_exporter_connect.yml.j2`: Port {{ cp_obs_connect_jmx_port }}, rules for kafka.connect worker-metrics, connector-metrics, task-metrics
        - `templates/jmx_exporter_schema_registry.yml.j2`: Port {{ cp_obs_sr_jmx_port }}, rules for kafka.schema.registry api-call metrics

    11. Create molecule scenario (follow cp_topic pattern):
        - `molecule.yml`: delegated driver, default scenario
        - `converge.yml`: includes cp_observability role with mock variables and cp_obs_deploy_grafana: false (no API needed), cp_obs_deploy_jmx: false (no filesystem needed)
        - `verify.yml`: verifies cp_observability_results fact was set

    **All task names MUST start with uppercase. All modules MUST use FQCN.**
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && pytest tests/ansible/test_cp_observability.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cp_obs_broker_jmx_port" ansible/roles/cp_observability/defaults/main.yml` matches with value 8080
    - `grep "cp_obs_sr_jmx_port" ansible/roles/cp_observability/defaults/main.yml` matches with value 8078
    - `grep "cp_obs_connect_jmx_port" ansible/roles/cp_observability/defaults/main.yml` matches with value 8077
    - `grep "cp_obs_grafana_method" ansible/roles/cp_observability/defaults/main.yml` matches with value "file_provisioning"
    - `grep "cp_obs_prometheus_targets_dir" ansible/roles/cp_observability/defaults/main.yml` matches
    - `grep "ansible.builtin.template" ansible/roles/cp_observability/tasks/jmx_exporter.yml` matches
    - `grep "ansible.builtin.template" ansible/roles/cp_observability/tasks/prometheus.yml` matches
    - `grep "prometheus_targets.json.j2" ansible/roles/cp_observability/tasks/prometheus.yml` matches
    - `grep "kafka_broker" ansible/roles/cp_observability/templates/prometheus_targets.json.j2` matches
    - `grep "schema_registry" ansible/roles/cp_observability/templates/prometheus_targets.json.j2` matches
    - `grep "kafka_connect" ansible/roles/cp_observability/templates/prometheus_targets.json.j2` matches
    - `grep "community.grafana" ansible/requirements.yml` matches
    - `grep "alerts" ansible/roles/cp_observability/tasks/grafana.yml` matches
    - `test -f ansible/roles/cp_observability/molecule/default/molecule.yml`
    - `test -f ansible/roles/cp_observability/templates/jmx_exporter_broker.yml.j2`
    - `test -f ansible/roles/cp_observability/templates/jmx_exporter_connect.yml.j2`
    - `test -f ansible/roles/cp_observability/templates/jmx_exporter_schema_registry.yml.j2`
    - `pytest tests/ansible/test_cp_observability.py -x` passes
  </acceptance_criteria>
  <done>
    cp_observability role deploys JMX exporter configs from templates to broker/SR/Connect nodes, generates Prometheus file_sd_configs from inventory groups with auto-update on node changes, imports Grafana dashboards via file provisioning or API, and deploys SLA-tier-aware alert rules. All tests pass. Molecule scenario exists.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/ansible/test_cp_observability.py -x -v` all pass
- `ansible-lint ansible/roles/cp_observability/ 2>&1 | head -20` shows no errors
- Prometheus template generates targets for all 3 host groups with correct ports
- Grafana tasks support both file_provisioning and api methods
</verification>

<success_criteria>
- JMX exporter configs deployed per component with correct default ports (broker:8080, SR:8078, Connect:8077)
- Prometheus scrape targets auto-generated from inventory host groups via file_sd_configs
- Grafana dashboards imported from observability/grafana/ with dual-method support
- Alert rules deployed with SLA-tier-aware thresholds from alerts.yaml
- All pytest and ansible-lint checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-orchestration-pipeline-observability-and-ci-cd/12-02-SUMMARY.md`
</output>
