---
phase: 05-observability-templates
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - observability/dynatrace/dashboard.json
  - observability/dynatrace/alerts.json
  - observability/dynatrace/cc-metrics-export.json
  - observability/dynatrace/jmx-exporter-stub.json
  - observability/dynatrace/README.md
  - observability/datadog/dashboard.json
  - observability/datadog/monitors.json
  - observability/datadog/cc-metrics-export.json
  - observability/datadog/jmx-exporter-stub.yaml
  - observability/datadog/README.md
  - observability/splunk/dashboard.xml
  - observability/splunk/alerts.json
  - observability/splunk/cc-metrics-export.conf
  - observability/splunk/jmx-exporter-stub.conf
  - observability/splunk/README.md
autonomous: true
requirements:
  - OBS-01
  - OBS-03
  - OBS-04

must_haves:
  truths:
    - "Dynatrace dashboard template covers all 5 panels (Cluster Health, Consumer Lag, Connect Status, DR Readiness, Flink Jobs stub)"
    - "Datadog dashboard template covers all 5 panels with Datadog-native widget types"
    - "Splunk dashboard template covers all 5 panels in Simple XML format"
    - "All three providers use identical SLA-tier alert thresholds matching Grafana Plan 01 values"
    - "All three providers support auto-discovery via domain prefix wildcard"
    - "All three providers have CC Metrics API export config and JMX exporter stub"
  artifacts:
    - path: "observability/dynatrace/dashboard.json"
      provides: "Dynatrace dashboard API payload with 5 panels"
      contains: "FSI Kafka"
    - path: "observability/dynatrace/alerts.json"
      provides: "Dynatrace custom alerting profiles"
      contains: "MirrorLag"
    - path: "observability/dynatrace/README.md"
      provides: "Dynatrace import instructions"
      contains: "Import Instructions"
    - path: "observability/datadog/dashboard.json"
      provides: "Datadog dashboard API payload with 5 panels"
      contains: "FSI Kafka"
    - path: "observability/datadog/monitors.json"
      provides: "Datadog monitor definitions"
      contains: "MirrorLag"
    - path: "observability/datadog/README.md"
      provides: "Datadog import instructions"
      contains: "Import Instructions"
    - path: "observability/splunk/dashboard.xml"
      provides: "Splunk Simple XML dashboard with 5 panels"
      contains: "FSI Kafka"
    - path: "observability/splunk/alerts.json"
      provides: "Splunk saved search alert definitions"
      contains: "MirrorLag"
    - path: "observability/splunk/README.md"
      provides: "Splunk import instructions"
      contains: "Import Instructions"
  key_links:
    - from: "observability/dynatrace/alerts.json"
      to: "observability/grafana/alerts.yaml"
      via: "Identical SLA-tier threshold values"
      pattern: "30|60|300|900"
    - from: "observability/datadog/monitors.json"
      to: "observability/grafana/alerts.yaml"
      via: "Identical SLA-tier threshold values"
      pattern: "30|60|300|900"
    - from: "observability/splunk/alerts.json"
      to: "observability/grafana/alerts.yaml"
      via: "Identical SLA-tier threshold values"
      pattern: "30|60|300|900"
    - from: "observability/metrics-mapping.md"
      to: "observability/dynatrace/dashboard.json"
      via: "Metric name mapping for Dynatrace DQL queries"
      pattern: "io.confluent.kafka"
---

<objective>
Create dashboard templates for Dynatrace, Datadog, and Splunk -- replicating the 5-panel structure, SLA-tier alerting, auto-discovery, and metrics export configs established by the Grafana reference in Plan 01.

Purpose: Deliver 3 of the 6 provider implementations, covering the most common enterprise observability platforms (Dynatrace for large FSI, Datadog for cloud-native FSI, Splunk for security-focused FSI).

Output: `observability/dynatrace/`, `observability/datadog/`, `observability/splunk/` directories each with dashboard template, alert rules, metrics export configs, and README.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-observability-templates/05-CONTEXT.md
@.planning/phases/05-observability-templates/05-UI-SPEC.md
@.planning/phases/05-observability-templates/05-01-SUMMARY.md
@observability/metrics-mapping.md
@observability/grafana/alerts.yaml
@observability/grafana/README.md
@docs/adr/008-dr-tier-classification.md

<interfaces>
<!-- Reference pattern from Plan 01 Grafana implementation -->
5-Panel Structure (all providers identical):
  Panel 1: Cluster Health (broker count, under-replicated partitions, active controller)
  Panel 2: Consumer Lag (consumer group lag by topic, per-partition breakdown)
  Panel 3: Connect Status (RUNNING/PAUSED/FAILED per connector via REST API)
  Panel 4: DR Readiness (mirror lag per topic with SLA-tier thresholds)
  Panel 5: Flink Jobs (stub -- Phase 6)

SLA-Tier Mirror Lag Thresholds (must match exactly):
  critical:    warn=30s,  alert=60s
  standard:    warn=300s, alert=900s
  best-effort: warn=3600s, alert=14400s
  compliance:  warn=10s,  alert=30s

Consumer Lag Thresholds (records):
  critical:    warn=1000,   alert=5000
  standard:    warn=10000,  alert=50000
  best-effort: warn=100000, alert=500000
  compliance:  warn=500,    alert=2000

Connect Thresholds:
  Any task FAILED: alert (all tiers)
  3 restarts in 15 min: warn

UI-SPEC Colors:
  OK: #22C55E | Warning: #EAB308 | Alert: #EF4444 | Unknown: #6B7280
  RUNNING: #22C55E | PAUSED: #EAB308 | FAILED: #EF4444 | UNASSIGNED: #6B7280
  Mirror topic: #3B82F6

Template Variables:
  {{CLUSTER_ID}}, {{DOMAIN_PREFIX}} (default *), {{ENVIRONMENT}} (default prod),
  {{CONNECT_REST_URL}} (default http://connect.internal:8083),
  {{CC_METRICS_API_KEY}}, {{JMX_EXPORTER_PORT}} (default 9101),
  {{MIRROR_LAG_WARN_CRITICAL}}=30, {{MIRROR_LAG_ALERT_CRITICAL}}=60, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dynatrace and Datadog dashboard templates with alerts and metrics export</name>
  <files>
    observability/dynatrace/dashboard.json
    observability/dynatrace/alerts.json
    observability/dynatrace/cc-metrics-export.json
    observability/dynatrace/jmx-exporter-stub.json
    observability/dynatrace/README.md
    observability/datadog/dashboard.json
    observability/datadog/monitors.json
    observability/datadog/cc-metrics-export.json
    observability/datadog/jmx-exporter-stub.yaml
    observability/datadog/README.md
  </files>
  <read_first>
    observability/metrics-mapping.md
    observability/grafana/alerts.yaml
    observability/grafana/dashboard-cluster-health.json
    observability/grafana/README.md
    .planning/phases/05-observability-templates/05-UI-SPEC.md
    .planning/phases/05-observability-templates/05-CONTEXT.md
    docs/adr/008-dr-tier-classification.md
  </read_first>
  <action>
Create `observability/dynatrace/` and `observability/datadog/` directories.

**DYNATRACE (`observability/dynatrace/`)**

**dashboard.json** -- Dynatrace Dashboard API payload (`POST /api/config/v1/dashboards`):
- Dashboard name: `FSI Kafka Platform - Observability`
- Use Dynatrace dashboard JSON structure with `dashboardMetadata` and `tiles` array
- `dashboardMetadata.name`: "FSI Kafka Platform - Observability"
- `dashboardMetadata.shared`: true
- `dashboardMetadata.dashboardFilter.managementZone`: placeholder `{{MANAGEMENT_ZONE}}`
- 5 tile groups matching the 5 panels:
  - **Cluster Health tiles**: Custom chart tile for broker count, markdown tile for active controller status, data explorer tile for under-replicated partitions with threshold red >= 1
  - **Consumer Lag tiles**: Data explorer tile with `builtin:kafka.consumer.lag` metric filtered by `$DOMAIN_PREFIX` topic prefix pattern. Split by consumer group. Threshold lines at tier values.
  - **Connect Status tiles**: Custom chart tile using REST API integration or ActiveGate plugin. Connector state mapped to numeric (RUNNING=1, PAUSED=2, FAILED=3, UNASSIGNED=0). Color: RUNNING=#22C55E, FAILED=#EF4444.
  - **DR Readiness tiles**: Data explorer tile with mirror lag metric. Threshold rules per SLA tier. Mirror topics colored #3B82F6.
  - **Flink Jobs tiles**: Placeholder markdown tile with text "Flink metrics will be available after Phase 6 deployment. This panel shows placeholder structure." Background color #6B7280.
- Template variables as Dynatrace dashboard variables: `{{CLUSTER_ID}}`, `{{DOMAIN_PREFIX}}` (default `*`), `{{ENVIRONMENT}}` (default `prod`)

**alerts.json** -- Dynatrace custom alerting profile:
- JSON array of custom event definitions for Dynatrace Settings API v2
- Alert definitions mirroring Grafana alerts.yaml exactly:
  - MirrorLagCritical (warn=30s, alert=60s), MirrorLagStandard (warn=300s, alert=900s), MirrorLagBestEffort (warn=3600s, alert=14400s), MirrorLagCompliance (warn=10s, alert=30s)
  - ConsumerLagCritical (warn=1000, alert=5000), ConsumerLagStandard (warn=10000, alert=50000), ConsumerLagBestEffort (warn=100000, alert=500000), ConsumerLagCompliance (warn=500, alert=2000)
  - ConnectTaskFailed (any FAILED task, severity=critical)
  - ConnectTaskRestartLoop (>=3 restarts in 15 min, severity=warning)
  - UnderReplicatedPartitions (>0 for 5m, severity=critical)
- Each alert includes `name`, `description`, `enabled: true`, `severity` (CUSTOM_ALERT, ERROR, AVAILABILITY), `eventType`, `metricSelector`, `threshold`

**cc-metrics-export.json** -- Dynatrace ActiveGate config for CC Metrics API ingestion:
```json
{
  "name": "Confluent Cloud Metrics",
  "type": "custom_metrics",
  "endpoint": "https://api.telemetry.confluent.cloud/v2/metrics/cloud/export",
  "authentication": {
    "type": "basic",
    "username": "{{CC_METRICS_API_KEY}}",
    "password": "{{CC_METRICS_API_SECRET}}"
  },
  "parameters": {
    "resource.kafka.id": "{{CLUSTER_ID}}"
  }
}
```

**jmx-exporter-stub.json** -- Dynatrace JMX plugin config stub:
```json
{
  "name": "FSI Kafka JMX",
  "type": "JMX",
  "hostPort": "{{JMX_EXPORTER_HOST}}:9101",
  "note": "Stub -- wired in Phase 8 (CFK) and Phase 9 (CP)",
  "metrics": [
    {"objectName": "kafka.server:type=ReplicaManager,name=UnderReplicatedPartitions", "attribute": "Value"},
    {"objectName": "kafka.server:type=BrokerTopicMetrics,name=MessagesInPerSec", "attribute": "OneMinuteRate"},
    {"objectName": "kafka.controller:type=KafkaController,name=ActiveControllerCount", "attribute": "Value"},
    {"objectName": "kafka.server:type=ReplicaManager,name=PartitionCount", "attribute": "Value"}
  ]
}
```

**README.md** -- Following same structure as Grafana README:
- Title: `# FSI Kafka Platform - Dynatrace Observability`
- Overview, Prerequisites (Dynatrace SaaS or Managed, ActiveGate for CC Metrics API, management zone configured)
- **Import Instructions**: 1. Copy template. 2. Replace variables. 3. Import via Dynatrace API `POST /api/config/v1/dashboards` or Dashboard Settings UI. 4. Verify data in 5 min.
- **Template Variables** table (same variables as Grafana plus Dynatrace-specific `{{MANAGEMENT_ZONE}}`)
- SLA Tier thresholds table, Dashboard files table, Flink stub note

---

**DATADOG (`observability/datadog/`)**

**dashboard.json** -- Datadog Dashboard API payload (`POST /api/v1/dashboard`):
- `title`: "FSI Kafka Platform - Observability"
- `layout_type`: "ordered"
- `widgets` array with 5 groups:
  - **Cluster Health group**: Query Value widget for broker count, Check Status widget for under-replicated partitions, Timeseries widget for request rate
  - **Consumer Lag group**: Timeseries widget with `avg:confluent_cloud.kafka.consumer_lag_offsets{$domain_prefix}` grouped by `consumer_group`. Markers at threshold levels per SLA tier. Use Datadog template variables: `$cluster_id`, `$domain_prefix`, `$environment`, `$sla_tier`
  - **Connect Status group**: Check Status widget for connector health. Query against Datadog integration or custom metric from Connect REST API. Colors: RUNNING=#22C55E, FAILED=#EF4444.
  - **DR Readiness group**: Timeseries widget with mirror lag metric. Markers (horizontal threshold lines) at SLA tier values: critical warn=30/alert=60, standard warn=300/alert=900, etc. Mirror topics colored #3B82F6.
  - **Flink Jobs group**: Note widget with stub text "Flink metrics will be available after Phase 6 deployment. This panel shows placeholder structure."
- `template_variables`: [{name: "cluster_id", prefix: "cluster_id", default: "*"}, {name: "domain_prefix", prefix: "topic_name", default: "*"}, {name: "environment", prefix: "env", default: "prod"}, {name: "sla_tier", prefix: "sla_tier", default: "*"}]

**monitors.json** -- Array of Datadog Monitor definitions (per `POST /api/v1/monitor`):
- Each monitor has `name`, `type` (metric alert or service check), `query`, `message`, `tags`, `options` with `thresholds`
- Mirror lag monitors per tier: MirrorLagCritical (warning=30, critical=60), MirrorLagStandard (warning=300, critical=900), MirrorLagBestEffort (warning=3600, critical=14400), MirrorLagCompliance (warning=10, critical=30)
- Consumer lag monitors per tier with corresponding thresholds
- ConnectTaskFailed monitor: any FAILED connector triggers critical alert
- ConnectTaskRestartLoop: >=3 restarts in 15min triggers warning
- UnderReplicatedPartitions: >0 for 5m triggers critical
- All monitors tagged: `team:fsi-kafka`, `tier:$sla_tier`

**cc-metrics-export.json** -- Datadog CC Metrics integration config:
```json
{
  "name": "Confluent Cloud",
  "type": "confluent_cloud",
  "api_key": "{{CC_METRICS_API_KEY}}",
  "api_secret": "{{CC_METRICS_API_SECRET}}",
  "cluster_id": "{{CLUSTER_ID}}",
  "note": "Configure in Datadog Integrations > Confluent Cloud tile or via API"
}
```

**jmx-exporter-stub.yaml** -- Datadog JMX check config stub (agent conf.d/kafka.d/conf.yaml format):
```yaml
# Stub -- wired in Phase 8 (CFK) and Phase 9 (CP)
init_config:
instances:
  - host: "{{JMX_EXPORTER_HOST}}"
    port: 9101
    conf:
      - include:
          domain: kafka.server
          bean_regex: ".*type=ReplicaManager.*"
          attribute:
            UnderReplicatedPartitions:
              metric_type: gauge
            PartitionCount:
              metric_type: gauge
      - include:
          domain: kafka.controller
          bean_regex: ".*type=KafkaController.*"
          attribute:
            ActiveControllerCount:
              metric_type: gauge
```

**README.md** -- Same structure as Grafana/Dynatrace READMEs:
- Title: `# FSI Kafka Platform - Datadog Observability`
- Prerequisites (Datadog account, API key, Confluent Cloud integration or custom metrics)
- **Import Instructions**: 1. Copy template. 2. Replace variables. 3. Import via Datadog API `POST /api/v1/dashboard` or Dashboard JSON import. 4. Import monitors via `POST /api/v1/monitor`. 5. Verify in 5 min.
- Template Variables, SLA Tier thresholds, files table, Flink stub note
  </action>
  <verify>
    <automated>test -d observability/dynatrace && test -d observability/datadog && python3 -c "import json; json.load(open('observability/dynatrace/dashboard.json')); json.load(open('observability/dynatrace/alerts.json')); json.load(open('observability/dynatrace/cc-metrics-export.json')); json.load(open('observability/dynatrace/jmx-exporter-stub.json')); json.load(open('observability/datadog/dashboard.json')); json.load(open('observability/datadog/monitors.json')); json.load(open('observability/datadog/cc-metrics-export.json'))" && grep -q "Import Instructions" observability/dynatrace/README.md && grep -q "Import Instructions" observability/datadog/README.md && echo "PASS: Dynatrace and Datadog files valid"</automated>
  </verify>
  <acceptance_criteria>
    - observability/dynatrace/dashboard.json is valid JSON and contains "FSI Kafka"
    - observability/dynatrace/dashboard.json contains "Cluster Health" and "Consumer Lag" and "Connect Status" and "DR Readiness" and "Flink"
    - observability/dynatrace/alerts.json is valid JSON and contains "MirrorLagCritical" with threshold "30" and "60"
    - observability/dynatrace/alerts.json contains "ConnectTaskFailed"
    - observability/dynatrace/alerts.json contains "ConsumerLagCritical" with "1000" and "5000"
    - observability/dynatrace/cc-metrics-export.json contains "api.telemetry.confluent.cloud"
    - observability/dynatrace/jmx-exporter-stub.json contains "9101" and "Phase 8"
    - observability/dynatrace/README.md contains "Import Instructions" and "Template Variables"
    - observability/datadog/dashboard.json is valid JSON and contains "FSI Kafka"
    - observability/datadog/dashboard.json contains "template_variables" and "domain_prefix"
    - observability/datadog/monitors.json is valid JSON and contains "MirrorLagCritical"
    - observability/datadog/monitors.json contains "ConnectTaskFailed"
    - observability/datadog/monitors.json contains threshold "30" and "60" (critical mirror lag)
    - observability/datadog/cc-metrics-export.json contains "CC_METRICS_API_KEY"
    - observability/datadog/jmx-exporter-stub.yaml contains "9101" and "Phase 8"
    - observability/datadog/README.md contains "Import Instructions" and "Template Variables"
  </acceptance_criteria>
  <done>
    Dynatrace dashboard template (dashboard.json) created with 5 tile groups, alert profiles, CC metrics export config, and JMX stub. Datadog dashboard template (dashboard.json) created with 5 widget groups, monitor definitions, CC metrics integration config, and JMX check stub. Both providers use identical SLA-tier thresholds matching Grafana Plan 01 and fsi-dr.sh. Both have README with import instructions and variable tables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Splunk dashboard template with alerts and metrics export</name>
  <files>
    observability/splunk/dashboard.xml
    observability/splunk/alerts.json
    observability/splunk/cc-metrics-export.conf
    observability/splunk/jmx-exporter-stub.conf
    observability/splunk/README.md
  </files>
  <read_first>
    observability/metrics-mapping.md
    observability/grafana/alerts.yaml
    observability/grafana/README.md
    .planning/phases/05-observability-templates/05-UI-SPEC.md
    docs/adr/008-dr-tier-classification.md
  </read_first>
  <action>
Create `observability/splunk/` directory.

**dashboard.xml** -- Splunk Simple XML dashboard format:
```xml
<dashboard version="1.1" theme="dark">
  <label>FSI Kafka Platform - Observability</label>
  <description>Cluster Health, Consumer Lag, Connect Status, DR Readiness, Flink Jobs</description>
  <fieldset submitButton="false">
    <input type="text" token="CLUSTER_ID"><label>Cluster ID</label><default>*</default></input>
    <input type="text" token="DOMAIN_PREFIX"><label>Domain Prefix</label><default>*</default></input>
    <input type="dropdown" token="ENVIRONMENT"><label>Environment</label><default>prod</default>
      <choice value="prod">Production</choice><choice value="staging">Staging</choice><choice value="dev">Dev</choice>
    </input>
    <input type="dropdown" token="SLA_TIER"><label>SLA Tier</label><default>*</default>
      <choice value="*">All</choice><choice value="critical">Critical</choice><choice value="standard">Standard</choice><choice value="best-effort">Best Effort</choice><choice value="compliance">Compliance</choice>
    </input>
  </fieldset>
  <!-- Panel rows for all 5 sections -->
</dashboard>
```

- 5 row sections, each containing panels for one observability area:
  - **Row 1 - Cluster Health**: Single value panel (broker count), single value (under-replicated partitions with red threshold >=1), chart (request rate over time), single value (active controller)
  - **Row 2 - Consumer Lag**: Chart panel with SPL `index=kafka sourcetype=kafka:consumer_lag topic="$DOMAIN_PREFIX$*" | stats avg(lag_records) by consumer_group, topic` with threshold overlay lines at SLA-tier values. Table panel showing per-partition breakdown.
  - **Row 3 - Connect Status**: Table panel with connector name, state (RUNNING/PAUSED/FAILED), worker, task count. SPL using Connect REST API data or HEC-ingested metrics. Color map: RUNNING=green, PAUSED=yellow, FAILED=red.
  - **Row 4 - DR Readiness**: Chart panel with mirror lag per topic. Threshold overlay at SLA-tier values. Table with per-topic assessment (OK/WARN/ALERT). Mirror topics identified with "[MIRROR]" label prefix.
  - **Row 5 - Flink Jobs**: HTML panel with stub message: "Flink metrics will be available after Phase 6 deployment. This panel shows placeholder structure."

Auto-discovery via SPL wildcard: `topic="$DOMAIN_PREFIX$*"` matches all topics with the domain prefix.

**alerts.json** -- Array of Splunk saved search / alert definitions:
JSON array (for programmatic import via REST API `POST /servicesNS/{owner}/{app}/saved/searches`):
- Mirror lag alerts per tier: `MirrorLagCritical` (search for lag > 30s, alert action at > 60s), `MirrorLagStandard` (300s/900s), `MirrorLagBestEffort` (3600s/14400s), `MirrorLagCompliance` (10s/30s)
- Consumer lag alerts per tier with corresponding thresholds
- `ConnectTaskFailed`: search for connector_state="FAILED", trigger on result count >= 1
- `ConnectTaskRestartLoop`: search for restart events, trigger on count >= 3 within 15m
- `UnderReplicatedPartitions`: search for under_replicated > 0 over 5m window
- Each alert: `name`, `search` (SPL query), `alert_type` (number of results), `alert_threshold`, `alert.severity` (1-10), `cron_schedule`, `is_scheduled: true`, `actions` (email, webhook placeholder)

**cc-metrics-export.conf** -- Splunk inputs.conf format for CC Metrics API:
```
# Confluent Cloud Metrics API scripted input
# Ingest CC metrics into Splunk via HEC or scripted input
[script://./bin/cc_metrics_collector.py]
disabled = false
interval = 60
index = kafka
sourcetype = confluent_cloud:metrics
# Configuration:
# CC_METRICS_API_KEY = {{CC_METRICS_API_KEY}}
# CC_METRICS_API_SECRET = {{CC_METRICS_API_SECRET}}
# CC_METRICS_ENDPOINT = https://api.telemetry.confluent.cloud/v2/metrics/cloud/export
# CLUSTER_ID = {{CLUSTER_ID}}
```

**jmx-exporter-stub.conf** -- Splunk JMX add-on config stub:
```
# JMX data collection stub for CFK/CP deployments
# Wired in Phase 8 (CFK) and Phase 9 (CP)
[kafka_jmx]
disabled = true
interval = 30
index = kafka
sourcetype = kafka:jmx
jmx_host = {{JMX_EXPORTER_HOST}}
jmx_port = 9101
object_names = kafka.server:type=ReplicaManager,name=UnderReplicatedPartitions;kafka.server:type=BrokerTopicMetrics,name=MessagesInPerSec;kafka.controller:type=KafkaController,name=ActiveControllerCount;kafka.server:type=ReplicaManager,name=PartitionCount
```

**README.md**:
- Title: `# FSI Kafka Platform - Splunk Observability`
- Prerequisites: Splunk Enterprise or Cloud, appropriate sourcetypes configured, Kafka data ingestion via HEC or scripted input
- **Import Instructions**: 1. Copy dashboard.xml. 2. Replace `$CLUSTER_ID$`, `$DOMAIN_PREFIX$` tokens. 3. Import via Splunk Web (Dashboards > Create New Dashboard > Source > Paste XML) or REST API. 4. Import alerts via REST `POST /servicesNS/{owner}/{app}/saved/searches`. 5. Verify data in 5 min.
- Template Variables, SLA Tier thresholds, files table, Flink stub note
  </action>
  <verify>
    <automated>test -d observability/splunk && test -f observability/splunk/dashboard.xml && python3 -c "import json; json.load(open('observability/splunk/alerts.json'))" && grep -q "FSI Kafka" observability/splunk/dashboard.xml && grep -q "DOMAIN_PREFIX" observability/splunk/dashboard.xml && grep -q "Import Instructions" observability/splunk/README.md && echo "PASS: Splunk files valid"</automated>
  </verify>
  <acceptance_criteria>
    - observability/splunk/dashboard.xml contains "FSI Kafka Platform - Observability"
    - observability/splunk/dashboard.xml contains "DOMAIN_PREFIX" (auto-discovery variable)
    - observability/splunk/dashboard.xml contains "Cluster Health" and "Consumer Lag" and "Connect Status" and "DR Readiness" and "Flink"
    - observability/splunk/dashboard.xml contains "SLA_TIER" dropdown
    - observability/splunk/alerts.json is valid JSON and contains "MirrorLagCritical" with "30" and "60"
    - observability/splunk/alerts.json contains "ConnectTaskFailed"
    - observability/splunk/alerts.json contains "ConsumerLagCritical" with "1000" and "5000"
    - observability/splunk/cc-metrics-export.conf contains "api.telemetry.confluent.cloud"
    - observability/splunk/cc-metrics-export.conf contains "CC_METRICS_API_KEY"
    - observability/splunk/jmx-exporter-stub.conf contains "9101" and "Phase 8"
    - observability/splunk/README.md contains "Import Instructions" and "Template Variables"
  </acceptance_criteria>
  <done>
    Splunk Simple XML dashboard created with 5 row sections, SPL queries with domain prefix wildcard auto-discovery, and SLA tier dropdown. Alert saved searches created with identical thresholds to Grafana/Dynatrace/Datadog. CC Metrics API scripted input config and JMX add-on stub config created. README with Splunk-specific import instructions complete. All 3 providers (Dynatrace, Datadog, Splunk) now delivered with threshold parity.
  </done>
</task>

</tasks>

<verification>
1. All 15 files across 3 provider directories exist
2. All JSON files parse without errors
3. Splunk XML is well-formed
4. All 3 providers have identical 5-panel structure
5. Alert thresholds match Grafana Plan 01 values exactly across all 3 providers
6. Auto-discovery uses domain prefix pattern in all 3 providers
7. Connect FAILED alert present in all 3 providers (OBS-10)
8. CC Metrics API export config present in all 3 providers
9. JMX exporter stub present in all 3 providers
10. README import instructions present for all 3 providers
</verification>

<success_criteria>
- Dynatrace, Datadog, and Splunk each have complete dashboard templates covering all 5 panels
- Alert thresholds are identical across all providers: mirror lag (critical 30s/60s, standard 300s/900s, best-effort 3600s/14400s, compliance 10s/30s), consumer lag (critical 1K/5K, standard 10K/50K, best-effort 100K/500K, compliance 500/2K)
- Connect task FAILED alert fires in all 3 providers
- Auto-discovery via domain prefix wildcard works in all 3 providers
- CC Metrics API export config targets api.telemetry.confluent.cloud in all 3 providers
- JMX exporter stub targets port 9101 in all 3 providers
- Each provider README has import instructions, variable table, and threshold reference
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-templates/05-02-SUMMARY.md`
</output>
