---
phase: 02-cc-multi-cloud-scenarios
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/validate-apply.sh
  - .github/workflows/terraform-scenario.yml
  - .github/workflows/terraform-plan-azure.yml
  - .github/workflows/terraform-plan-aws.yml
  - .github/workflows/terraform-plan-gcp.yml
  - .github/workflows/terraform-apply-azure.yml
  - .github/workflows/terraform-apply-aws.yml
  - .github/workflows/terraform-apply-gcp.yml
  - .github/workflows/terraform-plan.yml
  - .github/workflows/terraform-apply.yml
autonomous: true
requirements:
  - IAC-10

must_haves:
  truths:
    - "Validation script checks all 4 resource types: topic exists, schema registered, RBAC applied (implicit), DR mirror exists"
    - "Validation script produces human-readable PASS/FAIL per check with actionable error messages"
    - "Validation script exits non-zero on any failure"
    - "Validation script handles missing DR cluster gracefully (SKIP, not FAIL)"
    - "Reusable CI workflow contains the shared lint/validate/plan logic"
    - "Per-scenario caller workflows trigger on correct path patterns"
    - "Post-apply validation runs automatically in the apply workflow"
  artifacts:
    - path: "scripts/validate-apply.sh"
      provides: "Shared post-apply validation script"
      contains: "PASS"
    - path: ".github/workflows/terraform-scenario.yml"
      provides: "Reusable CI workflow"
      contains: "workflow_call"
    - path: ".github/workflows/terraform-plan-azure.yml"
      provides: "Azure scenario plan caller"
      contains: "scenarios/cc-azure"
    - path: ".github/workflows/terraform-plan-aws.yml"
      provides: "AWS scenario plan caller"
      contains: "scenarios/cc-aws"
    - path: ".github/workflows/terraform-plan-gcp.yml"
      provides: "GCP scenario plan caller"
      contains: "scenarios/cc-gcp"
    - path: ".github/workflows/terraform-apply-azure.yml"
      provides: "Azure scenario apply caller"
      contains: "validate-apply.sh"
    - path: ".github/workflows/terraform-apply-aws.yml"
      provides: "AWS scenario apply caller"
      contains: "validate-apply.sh"
    - path: ".github/workflows/terraform-apply-gcp.yml"
      provides: "GCP scenario apply caller"
      contains: "validate-apply.sh"
  key_links:
    - from: ".github/workflows/terraform-plan-azure.yml"
      to: ".github/workflows/terraform-scenario.yml"
      via: "workflow_call uses reference"
      pattern: "uses:.*terraform-scenario.yml"
    - from: ".github/workflows/terraform-apply-azure.yml"
      to: "scripts/validate-apply.sh"
      via: "post-apply validation step"
      pattern: "validate-apply.sh"
    - from: "scripts/validate-apply.sh"
      to: "Confluent Cloud REST API"
      via: "curl with Basic Auth"
      pattern: "curl.*-u"
---

<objective>
Create the shared post-apply validation script and refactor CI into reusable workflow with per-scenario callers -- completing IAC-10 and decisions D-09 through D-17.

Purpose: Enable automated verification that terraform apply actually created the expected resources (topics, schemas, RBAC, mirrors) and establish DRY CI pipelines for all three CC scenarios.

Output: scripts/validate-apply.sh, .github/workflows/terraform-scenario.yml (reusable), 6 per-scenario caller workflows (plan + apply for azure/aws/gcp), retired old workflows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cc-multi-cloud-scenarios/02-CONTEXT.md
@.planning/phases/02-cc-multi-cloud-scenarios/02-RESEARCH.md

# Prior plan context (scenario dirs must exist)
@.planning/phases/02-cc-multi-cloud-scenarios/02-01-SUMMARY.md

# Existing CI workflows (migration source)
@.github/workflows/terraform-plan.yml
@.github/workflows/terraform-apply.yml

# Validation targets
@modules/topic/outputs.tf

<interfaces>
<!-- Key contracts from existing CI workflows that must be preserved in reusable workflow -->

From .github/workflows/terraform-plan.yml (current lint steps to preserve):
```yaml
# These steps must appear in the reusable workflow's lint job:
- terraform fmt -check -recursive
- Validate Topic Naming Convention (grep domain= in environments/*/ and scenarios/*/)
- python3 ci/scripts/validate-schemas.py --schemas-dir schemas/
- python3 ci/scripts/validate-schemas.py --schemas-dir schemas/ --check-compatibility (continue-on-error)
- bash ci/scripts/check-overrides.sh
- terraform init -backend=false
- terraform validate
```

From .github/workflows/terraform-apply.yml (current apply flow to preserve):
```yaml
# Apply job with environment protection rules:
- terraform init (with backend credentials)
- terraform apply -auto-approve
# NEW: Add validate-apply.sh step after apply
```

From scripts/validate-apply.sh interface (new script):
```bash
# Arguments:
# $1: KAFKA_REST_ENDPOINT (required)
# $2: KAFKA_CLUSTER_ID (required)
# $3: SR_REST_ENDPOINT (required)
# $4: KAFKA_API_KEY (required)
# $5: KAFKA_API_SECRET (required)
# $6: SR_API_KEY (required)
# $7: SR_API_SECRET (required)
# $8: DR_KAFKA_REST_ENDPOINT (optional)
# $9: DR_KAFKA_CLUSTER_ID (optional)
# $10: DR_KAFKA_API_KEY (optional)
# $11: DR_KAFKA_API_SECRET (optional)
# $12+: TOPIC_NAMES (required, space-separated list)
#
# Exit codes:
# 0: All checks passed
# 1: One or more checks failed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared post-apply validation script</name>
  <files>
    scripts/validate-apply.sh
  </files>
  <read_first>
    modules/topic/outputs.tf
    modules/topic/main.tf
    scripts/mirror-failover.sh
    .planning/phases/02-cc-multi-cloud-scenarios/02-RESEARCH.md
  </read_first>
  <action>
**Per D-09, D-10, D-11, D-12:** Create scripts/validate-apply.sh -- a shell smoke script for post-apply validation using curl against Confluent Cloud REST APIs.

The script must validate 4 resource types per topic (per D-10):
1. **Topic exists** -- GET `{kafka_rest}/kafka/v3/clusters/{cluster_id}/topics/{topic_name}` returns HTTP 200
2. **Schema registered** -- GET `{sr_rest}/subjects/{topic_name}-value/versions` returns HTTP 200 with non-empty JSON array
3. **RBAC applied (implicit)** -- GET `{sr_rest}/subjects/{topic_name}-value/versions/latest` with SR credentials returns HTTP 200 (proves read access is granted)
4. **DR mirror exists (conditional)** -- GET `{dr_kafka_rest}/kafka/v3/clusters/{dr_cluster_id}/topics/{topic_name}` returns HTTP 200

**Full script content:**

```bash
#!/usr/bin/env bash
# =============================================================================
# Post-Apply Validation -- Verify Confluent Cloud resources after terraform apply
# =============================================================================
# Usage: validate-apply.sh <kafka-rest> <cluster-id> <sr-rest> \
#          <kafka-key> <kafka-secret> <sr-key> <sr-secret> \
#          [--dr-rest <url> --dr-cluster <id> --dr-key <key> --dr-secret <secret>] \
#          --topics <topic1> [topic2] [topic3] ...
#
# Exit codes:
#   0 = all checks passed
#   1 = one or more checks failed
#   2 = usage error
# =============================================================================
set -euo pipefail

# --- Parse arguments ---
KAFKA_REST_ENDPOINT=""
KAFKA_CLUSTER_ID=""
SR_REST_ENDPOINT=""
KAFKA_API_KEY=""
KAFKA_API_SECRET=""
SR_API_KEY=""
SR_API_SECRET=""
DR_KAFKA_REST_ENDPOINT=""
DR_KAFKA_CLUSTER_ID=""
DR_KAFKA_API_KEY=""
DR_KAFKA_API_SECRET=""
TOPICS=()

# Positional args (first 7 required)
if [ $# -lt 7 ]; then
  echo "ERROR: Missing required arguments."
  echo "Usage: validate-apply.sh <kafka-rest> <cluster-id> <sr-rest> <kafka-key> <kafka-secret> <sr-key> <sr-secret> [--dr-rest ...] --topics <topic1> ..."
  exit 2
fi

KAFKA_REST_ENDPOINT="$1"; shift
KAFKA_CLUSTER_ID="$1"; shift
SR_REST_ENDPOINT="$1"; shift
KAFKA_API_KEY="$1"; shift
KAFKA_API_SECRET="$1"; shift
SR_API_KEY="$1"; shift
SR_API_SECRET="$1"; shift

# Named args
while [ $# -gt 0 ]; do
  case "$1" in
    --dr-rest)     DR_KAFKA_REST_ENDPOINT="$2"; shift 2 ;;
    --dr-cluster)  DR_KAFKA_CLUSTER_ID="$2"; shift 2 ;;
    --dr-key)      DR_KAFKA_API_KEY="$2"; shift 2 ;;
    --dr-secret)   DR_KAFKA_API_SECRET="$2"; shift 2 ;;
    --topics)      shift; while [ $# -gt 0 ] && [[ "$1" != --* ]]; do TOPICS+=("$1"); shift; done ;;
    *)             echo "ERROR: Unknown argument: $1"; exit 2 ;;
  esac
done

if [ ${#TOPICS[@]} -eq 0 ]; then
  echo "ERROR: No topics specified. Use --topics <topic1> [topic2] ..."
  exit 2
fi

# --- Counters ---
PASS=0
FAIL=0
SKIP=0

# --- Check function ---
check() {
  local label="$1" url="$2" key="$3" secret="$4"
  local http_code body
  body=$(curl -s -w "\n%{http_code}" -u "${key}:${secret}" "${url}")
  http_code=$(echo "$body" | tail -1)
  if [ "${http_code}" = "200" ]; then
    echo "  PASS: ${label}"
    ((PASS++))
  else
    echo "  FAIL: ${label} (HTTP ${http_code})"
    echo "        Endpoint: ${url}"
    echo "        Action: Verify the resource was created and credentials have access"
    ((FAIL++))
  fi
}

# --- Run checks per topic ---
echo "============================================"
echo " Post-Apply Validation"
echo "============================================"
echo ""

for topic in "${TOPICS[@]}"; do
  echo "--- ${topic} ---"
  subject="${topic}-value"

  # Check 1: Topic exists on primary cluster
  check "Topic exists" \
    "${KAFKA_REST_ENDPOINT}/kafka/v3/clusters/${KAFKA_CLUSTER_ID}/topics/${topic}" \
    "${KAFKA_API_KEY}" "${KAFKA_API_SECRET}"

  # Check 2: Schema registered in Schema Registry
  check "Schema registered (${subject})" \
    "${SR_REST_ENDPOINT}/subjects/${subject}/versions" \
    "${SR_API_KEY}" "${SR_API_SECRET}"

  # Check 3: RBAC applied (implicit -- read access to schema proves RBAC is working)
  check "Schema readable (RBAC implicit)" \
    "${SR_REST_ENDPOINT}/subjects/${subject}/versions/latest" \
    "${SR_API_KEY}" "${SR_API_SECRET}"

  # Check 4: DR mirror (conditional -- skip if DR cluster not configured)
  if [ -n "${DR_KAFKA_REST_ENDPOINT}" ] && [ -n "${DR_KAFKA_CLUSTER_ID}" ]; then
    check "DR mirror exists" \
      "${DR_KAFKA_REST_ENDPOINT}/kafka/v3/clusters/${DR_KAFKA_CLUSTER_ID}/topics/${topic}" \
      "${DR_KAFKA_API_KEY}" "${DR_KAFKA_API_SECRET}"
  else
    echo "  SKIP: DR mirror (DR cluster not configured)"
    ((SKIP++))
  fi
  echo ""
done

# --- Summary ---
echo "============================================"
echo " Summary: ${PASS} passed, ${FAIL} failed, ${SKIP} skipped"
echo "============================================"

if [ "${FAIL}" -gt 0 ]; then
  echo ""
  echo "RESULT: FAILED -- ${FAIL} check(s) did not pass."
  echo "Review the FAIL lines above for endpoints and remediation actions."
  exit 1
fi

echo ""
echo "RESULT: ALL CHECKS PASSED"
exit 0
```

Make the script executable: `chmod +x scripts/validate-apply.sh`.

Run syntax check: `bash -n scripts/validate-apply.sh` (must exit 0).
  </action>
  <verify>
    <automated>bash -n /Users/jhogan/fsi-kafka-platform/scripts/validate-apply.sh && echo "Syntax OK" && test -x /Users/jhogan/fsi-kafka-platform/scripts/validate-apply.sh && echo "Executable OK"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validate-apply.sh exists and is executable
    - scripts/validate-apply.sh contains `set -euo pipefail`
    - scripts/validate-apply.sh contains `check()` function with curl -u for Basic Auth
    - scripts/validate-apply.sh contains `kafka/v3/clusters` (topic check endpoint pattern)
    - scripts/validate-apply.sh contains `/subjects/` (schema check endpoint pattern)
    - scripts/validate-apply.sh contains `PASS:` and `FAIL:` output labels
    - scripts/validate-apply.sh contains `SKIP: DR mirror` for conditional DR check
    - scripts/validate-apply.sh contains `--topics` argument parsing
    - scripts/validate-apply.sh contains `exit 1` on failure and `exit 0` on success
    - scripts/validate-apply.sh contains actionable error message text ("Verify the resource was created")
    - `bash -n scripts/validate-apply.sh` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>scripts/validate-apply.sh validates all 4 resource types (topic, schema, RBAC implicit, DR mirror conditional) with PASS/FAIL/SKIP output, actionable error messages, and non-zero exit on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable CI workflow and per-scenario callers</name>
  <files>
    .github/workflows/terraform-scenario.yml
    .github/workflows/terraform-plan-azure.yml
    .github/workflows/terraform-plan-aws.yml
    .github/workflows/terraform-plan-gcp.yml
    .github/workflows/terraform-apply-azure.yml
    .github/workflows/terraform-apply-aws.yml
    .github/workflows/terraform-apply-gcp.yml
    .github/workflows/terraform-plan.yml
    .github/workflows/terraform-apply.yml
  </files>
  <read_first>
    .github/workflows/terraform-plan.yml
    .github/workflows/terraform-apply.yml
    ci/scripts/validate-schemas.py
    ci/scripts/check-overrides.sh
    scripts/validate-apply.sh
  </read_first>
  <action>
**Per D-14, D-15, D-16, D-17:** Create reusable workflow and per-scenario callers. Retire old workflows.

**1. .github/workflows/terraform-scenario.yml** -- Reusable called workflow with two jobs: `lint` (plan) and `apply`.

```yaml
name: Terraform Scenario
on:
  workflow_call:
    inputs:
      scenario-dir:
        required: true
        type: string
        description: "Path to scenario directory (e.g., scenarios/cc-aws)"
      environment-name:
        required: false
        type: string
        default: ""
        description: "GitHub environment for protection rules (apply only)"
      tf-version:
        required: false
        type: string
        default: "1.7.0"
      mode:
        required: true
        type: string
        description: "'plan' for PR validation, 'apply' for merge-to-main"
    secrets:
      CONFLUENT_CLOUD_API_KEY:
        required: true
      CONFLUENT_CLOUD_API_SECRET:
        required: true
      KAFKA_API_KEY:
        required: true
      KAFKA_API_SECRET:
        required: true
      SR_API_KEY:
        required: true
      SR_API_SECRET:
        required: true
      DR_KAFKA_API_KEY:
        required: true
      DR_KAFKA_API_SECRET:
        required: true
```

**lint job** (runs always) -- preserves all existing validation steps from terraform-plan.yml:
- actions/checkout@v4
- hashicorp/setup-terraform@v3 with `inputs.tf-version`
- terraform fmt -check -recursive (working-directory: inputs.scenario-dir)
- Validate Topic Naming Convention (same grep logic as current, checking environments/ and scenarios/)
- python3 ci/scripts/validate-schemas.py --schemas-dir schemas/
- python3 ci/scripts/validate-schemas.py --schemas-dir schemas/ --check-compatibility (continue-on-error, only on plan mode, needs SR secrets)
- bash ci/scripts/check-overrides.sh (only on plan mode)
- terraform init -backend=false (working-directory: inputs.scenario-dir)
- terraform validate (working-directory: inputs.scenario-dir)

**plan job** (runs when mode == 'plan', needs lint):
- terraform init with backend creds
- terraform plan -no-color -out=tfplan | tee plan-output.txt
- Comment plan on PR via actions/github-script@v7

**apply job** (runs when mode == 'apply', needs lint):
- environment: inputs.environment-name
- terraform init with backend creds
- terraform apply -auto-approve
- Post-apply validation: `bash scripts/validate-apply.sh` with topic names from terraform output

For the apply job's validation step, the topic names can be extracted from the scenario's example-topics.tf. Use a step that extracts topic names from terraform output:
```yaml
- name: Extract topic names
  id: topics
  run: |
    TOPICS=$(terraform output -json | jq -r 'to_entries[] | select(.key | endswith("_topic_name")) | .value.value // empty' 2>/dev/null || echo "")
    if [ -z "$TOPICS" ]; then
      # Fallback: known reference topics from example-topics.tf
      TOPICS="corebanking.transactions.v1.account-transaction fraud.detection.v1.alert-signal compliance.screening.v1.match-result"
    fi
    echo "topics=$TOPICS" >> "$GITHUB_OUTPUT"
  working-directory: ${{ inputs.scenario-dir }}

- name: Post-Apply Validation
  run: |
    bash scripts/validate-apply.sh \
      "$KAFKA_REST" "$KAFKA_CLUSTER" "$SR_REST" \
      "$KAFKA_KEY" "$KAFKA_SECRET" "$SR_KEY" "$SR_SECRET" \
      --dr-rest "$DR_REST" --dr-cluster "$DR_CLUSTER" \
      --dr-key "$DR_KEY" --dr-secret "$DR_SECRET" \
      --topics ${{ steps.topics.outputs.topics }}
  env:
    KAFKA_REST: ${{ ... from terraform output or secrets }}
    ...
```

Note: The apply job needs Kafka REST endpoint and cluster ID from terraform output or from the scenario's .auto.tfvars. Since these are populated at runtime by the operator, use terraform output to extract them. If terraform doesn't expose these as outputs, the apply workflow should accept them as environment-level variables or secrets. For now, accept them as additional workflow inputs.

Add additional secrets/inputs to the reusable workflow for validation endpoints:
```yaml
    inputs:
      kafka-rest-endpoint:
        required: false
        type: string
        default: ""
        description: "Kafka REST endpoint for post-apply validation"
      kafka-cluster-id:
        required: false
        type: string
        default: ""
        description: "Kafka cluster ID for post-apply validation"
      sr-rest-endpoint:
        required: false
        type: string
        default: ""
        description: "Schema Registry REST endpoint for post-apply validation"
      dr-kafka-rest-endpoint:
        required: false
        type: string
        default: ""
        description: "DR Kafka REST endpoint (optional)"
      dr-kafka-cluster-id:
        required: false
        type: string
        default: ""
        description: "DR Kafka cluster ID (optional)"
      reference-topics:
        required: false
        type: string
        default: "corebanking.transactions.v1.account-transaction fraud.detection.v1.alert-signal compliance.screening.v1.match-result"
        description: "Space-separated topic names to validate"
```

**2. Per-scenario plan caller workflows** -- one for each cloud provider.

**.github/workflows/terraform-plan-azure.yml:**
```yaml
name: "Plan: CC-Azure"
on:
  pull_request:
    paths:
      - 'scenarios/cc-azure/**'
      - 'modules/**'
      - 'schemas/**'
      - 'ci/**'
permissions:
  contents: read
  pull-requests: write
jobs:
  plan:
    uses: ./.github/workflows/terraform-scenario.yml
    with:
      scenario-dir: scenarios/cc-azure
      mode: plan
    secrets: inherit
```

**.github/workflows/terraform-plan-aws.yml:**
```yaml
name: "Plan: CC-AWS"
on:
  pull_request:
    paths:
      - 'scenarios/cc-aws/**'
      - 'modules/**'
      - 'schemas/**'
      - 'ci/**'
permissions:
  contents: read
  pull-requests: write
jobs:
  plan:
    uses: ./.github/workflows/terraform-scenario.yml
    with:
      scenario-dir: scenarios/cc-aws
      mode: plan
    secrets: inherit
```

**.github/workflows/terraform-plan-gcp.yml:**
```yaml
name: "Plan: CC-GCP"
on:
  pull_request:
    paths:
      - 'scenarios/cc-gcp/**'
      - 'modules/**'
      - 'schemas/**'
      - 'ci/**'
permissions:
  contents: read
  pull-requests: write
jobs:
  plan:
    uses: ./.github/workflows/terraform-scenario.yml
    with:
      scenario-dir: scenarios/cc-gcp
      mode: plan
    secrets: inherit
```

**3. Per-scenario apply caller workflows** -- one for each cloud provider.

**.github/workflows/terraform-apply-azure.yml:**
```yaml
name: "Apply: CC-Azure"
on:
  push:
    branches: [main]
    paths:
      - 'scenarios/cc-azure/**'
      - 'modules/**'
      - 'schemas/**'
permissions:
  contents: read
jobs:
  apply:
    uses: ./.github/workflows/terraform-scenario.yml
    with:
      scenario-dir: scenarios/cc-azure
      environment-name: fsi-cc-azure
      mode: apply
    secrets: inherit
```

**.github/workflows/terraform-apply-aws.yml:**
```yaml
name: "Apply: CC-AWS"
on:
  push:
    branches: [main]
    paths:
      - 'scenarios/cc-aws/**'
      - 'modules/**'
      - 'schemas/**'
permissions:
  contents: read
jobs:
  apply:
    uses: ./.github/workflows/terraform-scenario.yml
    with:
      scenario-dir: scenarios/cc-aws
      environment-name: fsi-cc-aws
      mode: apply
    secrets: inherit
```

**.github/workflows/terraform-apply-gcp.yml:**
```yaml
name: "Apply: CC-GCP"
on:
  push:
    branches: [main]
    paths:
      - 'scenarios/cc-gcp/**'
      - 'modules/**'
      - 'schemas/**'
permissions:
  contents: read
jobs:
  apply:
    uses: ./.github/workflows/terraform-scenario.yml
    with:
      scenario-dir: scenarios/cc-gcp
      environment-name: fsi-cc-gcp
      mode: apply
    secrets: inherit
```

**4. Retire old workflows (per D-16):**
- **DO NOT delete** .github/workflows/terraform-plan.yml and terraform-apply.yml. Instead, add a deprecation comment at the top of each file and update them to call the reusable workflow for cc-azure as a transitional step. Add a comment: `# DEPRECATED: This workflow is replaced by terraform-plan-azure.yml / terraform-apply-azure.yml. Kept temporarily for backward compatibility.`
- Alternatively, if cleaner: rename terraform-plan.yml content to redirect to the azure-specific caller, or simply leave a stub that explains the migration. The key is that the old workflow names still exist in git history and any existing branch protections or status checks referencing them will see a clear deprecation message.

Actually, per D-16 the existing workflows should be **migrated** to call the reusable workflow. So update:

**.github/workflows/terraform-plan.yml** -- Replace contents with:
```yaml
# =============================================================================
# MIGRATED: Now calls the reusable terraform-scenario workflow.
# Per-scenario workflows (terraform-plan-azure.yml, etc.) are the primary entry points.
# This file triggers on ALL scenario changes for backward compatibility.
# =============================================================================
name: Terraform Plan (All Scenarios)
on:
  pull_request:
    paths:
      - 'scenarios/**'
      - 'modules/**'
      - 'schemas/**'
      - 'ci/**'
permissions:
  contents: read
  pull-requests: write
jobs:
  plan-azure:
    if: contains(github.event.pull_request.changed_files, 'scenarios/cc-azure') || contains(github.event.pull_request.changed_files, 'modules/') || contains(github.event.pull_request.changed_files, 'schemas/')
    uses: ./.github/workflows/terraform-scenario.yml
    with:
      scenario-dir: scenarios/cc-azure
      mode: plan
    secrets: inherit
```

Actually, the `contains(github.event.pull_request.changed_files, ...)` pattern does not work in GitHub Actions `if` conditions (changed_files is not a simple string). The simpler approach per D-16: just **replace the old workflow content with a comment explaining the migration** and point to the new per-scenario workflows. This avoids breaking status checks that reference the old workflow name.

**.github/workflows/terraform-plan.yml** -- Replace with:
```yaml
# =============================================================================
# DEPRECATED -- Replaced by per-scenario workflows
# =============================================================================
# This workflow has been replaced by:
#   - terraform-plan-azure.yml (scenarios/cc-azure)
#   - terraform-plan-aws.yml (scenarios/cc-aws)
#   - terraform-plan-gcp.yml (scenarios/cc-gcp)
#
# Each calls the reusable terraform-scenario.yml workflow.
# This file is retained for git history. It will not trigger on new PRs.
# =============================================================================
name: "Terraform Plan (Deprecated)"
on:
  pull_request:
    paths:
      - 'environments/**'  # Old path -- no longer exists
```

**.github/workflows/terraform-apply.yml** -- Same deprecation treatment:
```yaml
# =============================================================================
# DEPRECATED -- Replaced by per-scenario workflows
# =============================================================================
# This workflow has been replaced by:
#   - terraform-apply-azure.yml (scenarios/cc-azure)
#   - terraform-apply-aws.yml (scenarios/cc-aws)
#   - terraform-apply-gcp.yml (scenarios/cc-gcp)
#
# Each calls the reusable terraform-scenario.yml workflow.
# This file is retained for git history. It will not trigger on new merges.
# =============================================================================
name: "Terraform Apply (Deprecated)"
on:
  push:
    branches: [main]
    paths:
      - 'environments/**'  # Old path -- no longer exists
```
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && ls .github/workflows/terraform-scenario.yml .github/workflows/terraform-plan-azure.yml .github/workflows/terraform-plan-aws.yml .github/workflows/terraform-plan-gcp.yml .github/workflows/terraform-apply-azure.yml .github/workflows/terraform-apply-aws.yml .github/workflows/terraform-apply-gcp.yml && echo "All workflow files exist"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/terraform-scenario.yml contains `workflow_call:`
    - .github/workflows/terraform-scenario.yml contains `scenario-dir:` input
    - .github/workflows/terraform-scenario.yml contains `mode:` input with 'plan' and 'apply'
    - .github/workflows/terraform-scenario.yml contains `terraform fmt -check`
    - .github/workflows/terraform-scenario.yml contains `validate-schemas.py`
    - .github/workflows/terraform-scenario.yml contains `check-overrides.sh`
    - .github/workflows/terraform-scenario.yml contains `terraform validate`
    - .github/workflows/terraform-scenario.yml contains `terraform plan` step
    - .github/workflows/terraform-scenario.yml contains `terraform apply` step
    - .github/workflows/terraform-scenario.yml contains `validate-apply.sh` reference
    - .github/workflows/terraform-plan-azure.yml contains `scenarios/cc-azure`
    - .github/workflows/terraform-plan-azure.yml contains `uses: ./.github/workflows/terraform-scenario.yml`
    - .github/workflows/terraform-plan-azure.yml contains `secrets: inherit`
    - .github/workflows/terraform-plan-aws.yml contains `scenarios/cc-aws`
    - .github/workflows/terraform-plan-aws.yml contains `uses: ./.github/workflows/terraform-scenario.yml`
    - .github/workflows/terraform-plan-gcp.yml contains `scenarios/cc-gcp`
    - .github/workflows/terraform-plan-gcp.yml contains `uses: ./.github/workflows/terraform-scenario.yml`
    - .github/workflows/terraform-apply-azure.yml contains `scenarios/cc-azure`
    - .github/workflows/terraform-apply-azure.yml contains `environment-name: fsi-cc-azure`
    - .github/workflows/terraform-apply-aws.yml contains `scenarios/cc-aws`
    - .github/workflows/terraform-apply-aws.yml contains `environment-name: fsi-cc-aws`
    - .github/workflows/terraform-apply-gcp.yml contains `scenarios/cc-gcp`
    - .github/workflows/terraform-apply-gcp.yml contains `environment-name: fsi-cc-gcp`
    - .github/workflows/terraform-plan.yml contains `DEPRECATED`
    - .github/workflows/terraform-apply.yml contains `DEPRECATED`
    - All apply caller workflows contain path trigger for `modules/**` and `schemas/**`
  </acceptance_criteria>
  <done>Reusable terraform-scenario.yml contains shared lint/validate/plan/apply/validate logic. Six per-scenario caller workflows trigger on correct paths and pass scenario-specific inputs. Old terraform-plan.yml and terraform-apply.yml are deprecated with environments/** path (no longer triggers). Post-apply validation (validate-apply.sh) is wired into the apply workflow.</done>
</task>

</tasks>

<verification>
```bash
# Validation script is syntactically valid
bash -n scripts/validate-apply.sh

# All workflow files exist
ls .github/workflows/terraform-scenario.yml
ls .github/workflows/terraform-plan-{azure,aws,gcp}.yml
ls .github/workflows/terraform-apply-{azure,aws,gcp}.yml

# Reusable workflow has workflow_call trigger
grep 'workflow_call' .github/workflows/terraform-scenario.yml

# All plan callers reference the reusable workflow
grep 'terraform-scenario.yml' .github/workflows/terraform-plan-azure.yml
grep 'terraform-scenario.yml' .github/workflows/terraform-plan-aws.yml
grep 'terraform-scenario.yml' .github/workflows/terraform-plan-gcp.yml

# All apply callers reference the validation script
grep 'validate-apply.sh' .github/workflows/terraform-scenario.yml

# Old workflows are deprecated
grep 'DEPRECATED' .github/workflows/terraform-plan.yml
grep 'DEPRECATED' .github/workflows/terraform-apply.yml

# Path triggers are correct
grep 'scenarios/cc-azure' .github/workflows/terraform-plan-azure.yml
grep 'scenarios/cc-aws' .github/workflows/terraform-plan-aws.yml
grep 'scenarios/cc-gcp' .github/workflows/terraform-plan-gcp.yml
```
</verification>

<success_criteria>
- scripts/validate-apply.sh validates 4 resource types with human-readable PASS/FAIL/SKIP output
- terraform-scenario.yml is a working reusable workflow with lint + plan + apply jobs
- 6 per-scenario caller workflows exist with correct path triggers
- Old workflows are deprecated and will not trigger on new changes
- Post-apply validation is integrated into the apply workflow
</success_criteria>

<output>
After completion, create `.planning/phases/02-cc-multi-cloud-scenarios/02-02-SUMMARY.md`
</output>
