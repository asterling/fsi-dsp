---
phase: 03-access-control-and-compliance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/terraform-scenario.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - docs/compliance-guide.md
autonomous: true
requirements:
  - COMP-04

must_haves:
  truths:
    - "Terraform apply workflow writes a job summary with scenario, timestamp, actor, commit SHA, run ID, and validation results"
    - "PR template includes compliance checkboxes for data classification, RBAC, schema compatibility, and compliance tier"
    - "Compliance guide maps the PR-to-verify audit chain for regulatory examiners"
  artifacts:
    - path: ".github/workflows/terraform-scenario.yml"
      provides: "Job summary and annotations on apply jobs"
      contains: "GITHUB_STEP_SUMMARY"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "Compliance review checkboxes"
      contains: "data classification"
    - path: "docs/compliance-guide.md"
      provides: "Audit trail documentation for regulatory examiners"
      contains: "change management"
  key_links:
    - from: ".github/workflows/terraform-scenario.yml"
      to: "scripts/validate-apply.sh"
      via: "Apply job captures validation PASS/FAIL output for job summary"
      pattern: "GITHUB_STEP_SUMMARY"
    - from: "docs/compliance-guide.md"
      to: ".github/workflows/terraform-scenario.yml"
      via: "Guide references workflow run pages as audit evidence"
      pattern: "workflow"
---

<objective>
Add audit trail infrastructure to the CI/CD pipeline and create compliance documentation for regulatory examiners (per D-12 through D-15). The terraform apply workflow writes structured job summaries with resource changes and validation results. The PR template gains compliance checkboxes. A compliance guide maps each step in the PR-to-verify chain to FSI control categories.

Purpose: Regulatory examiners can trace any deployed resource from PR through review, merge, apply, and validation -- with clickable links to GitHub artifacts.
Output: Enhanced CI workflow, enhanced PR template, compliance guide document.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-access-control-and-compliance/03-CONTEXT.md

<interfaces>
<!-- Key patterns from existing workflow -->

From .github/workflows/terraform-scenario.yml (apply job -- add summary after):
```yaml
- name: Terraform Apply
  run: terraform apply -auto-approve
  working-directory: ${{ inputs.scenario-dir }}
  env: ...

- name: Extract Topic Names
  id: topics
  run: ...

- name: Post-Apply Validation
  if: inputs.kafka-rest-endpoint != '' && inputs.kafka-cluster-id != ''
  run: bash scripts/validate-apply.sh ...
```

From scripts/validate-apply.sh (output format):
```
--- {topic} ---
  PASS: Topic exists
  PASS: Schema registered ({subject})
  PASS: Schema readable (RBAC implicit)
  SKIP: DR mirror (DR cluster not configured)

============================================
 Summary: 3 passed, 0 failed, 1 skipped
============================================

RESULT: ALL CHECKS PASSED
```

From .github/PULL_REQUEST_TEMPLATE.md (current content):
```markdown
## New Topic / Schema Change

### Checklist (all required for merge)
- [ ] Avro schema (.avsc) added to /schemas/
- [ ] Terraform module call added to /environments/prod-east/
- [ ] Topic name follows convention: {domain}.{application}.{version}.{entity}
- [ ] owner is a valid team email
- [ ] sla_tier is set (critical / standard / best-effort)
- [ ] PII fields identified in pii_fields list
- [ ] Producer and consumer service accounts listed
- [ ] Schema has doc annotations on all fields
- [ ] terraform plan runs clean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit trail job summary and annotations to CI workflow</name>
  <files>.github/workflows/terraform-scenario.yml</files>
  <read_first>
    - .github/workflows/terraform-scenario.yml (full current workflow -- must preserve all existing steps and add new ones after apply and validation)
    - scripts/validate-apply.sh (output format -- capture PASS/FAIL lines for summary)
    - .planning/phases/03-access-control-and-compliance/03-RESEARCH.md (Pattern 5: GitHub Actions Job Summary)
  </read_first>
  <action>
**Add three new steps to the `apply` job in `.github/workflows/terraform-scenario.yml`, after the existing `Post-Apply Validation` step:**

1. **Capture validation results step** (runs after Post-Apply Validation):
   ```yaml
   - name: Capture Validation Results
     id: validation
     if: inputs.kafka-rest-endpoint != '' && inputs.kafka-cluster-id != ''
     run: |
       # Re-run validation capturing output (the previous step already validated)
       RESULT=$(bash scripts/validate-apply.sh \
         "${{ inputs.kafka-rest-endpoint }}" "${{ inputs.kafka-cluster-id }}" \
         "${{ inputs.sr-rest-endpoint }}" \
         "${{ secrets.KAFKA_API_KEY }}" "${{ secrets.KAFKA_API_SECRET }}" \
         "${{ secrets.SR_API_KEY }}" "${{ secrets.SR_API_SECRET }}" \
         ${{ inputs.dr-kafka-rest-endpoint != '' && format('--dr-rest {0} --dr-cluster {1} --dr-key {2} --dr-secret {3}', inputs.dr-kafka-rest-endpoint, inputs.dr-kafka-cluster-id, secrets.DR_KAFKA_API_KEY, secrets.DR_KAFKA_API_SECRET) || '' }} \
         --topics ${{ steps.topics.outputs.topics }} 2>&1 || true)
       echo "output<<EOF" >> "$GITHUB_OUTPUT"
       echo "$RESULT" >> "$GITHUB_OUTPUT"
       echo "EOF" >> "$GITHUB_OUTPUT"
     continue-on-error: true
   ```

   ACTUALLY, a simpler approach: modify the existing `Post-Apply Validation` step to capture output, then add the summary step after. Update the existing step to:
   ```yaml
   - name: Post-Apply Validation
     id: validation
     if: inputs.kafka-rest-endpoint != '' && inputs.kafka-cluster-id != ''
     run: |
       bash scripts/validate-apply.sh \
         "${{ inputs.kafka-rest-endpoint }}" "${{ inputs.kafka-cluster-id }}" \
         "${{ inputs.sr-rest-endpoint }}" \
         "${{ secrets.KAFKA_API_KEY }}" "${{ secrets.KAFKA_API_SECRET }}" \
         "${{ secrets.SR_API_KEY }}" "${{ secrets.SR_API_SECRET }}" \
         ${{ inputs.dr-kafka-rest-endpoint != '' && format('--dr-rest {0} --dr-cluster {1} --dr-key {2} --dr-secret {3}', inputs.dr-kafka-rest-endpoint, inputs.dr-kafka-cluster-id, secrets.DR_KAFKA_API_KEY, secrets.DR_KAFKA_API_SECRET) || '' }} \
         --topics ${{ steps.topics.outputs.topics }} 2>&1 | tee validation-output.txt
   ```
   Add `id: validation` and pipe output to `validation-output.txt`.

2. **Write Compliance Audit Summary step** (per D-12, D-15):
   ```yaml
   - name: Write Compliance Audit Summary
     if: always() && inputs.mode == 'apply'
     run: |
       {
         echo "## Terraform Apply -- Compliance Audit Trail"
         echo ""
         echo "| Property | Value |"
         echo "|----------|-------|"
         echo "| Scenario | \`${{ inputs.scenario-dir }}\` |"
         echo "| Timestamp | $(date -u +%Y-%m-%dT%H:%M:%SZ) |"
         echo "| Triggered by | PR merge to main |"
         echo "| Actor | ${{ github.actor }} |"
         echo "| Commit SHA | \`${{ github.sha }}\` |"
         echo "| Run ID | [${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) |"
         echo ""
         echo "### Validation Results"
         echo ""
         if [ -f validation-output.txt ]; then
           echo '```'
           cat validation-output.txt
           echo '```'
         else
           echo "_Validation skipped (kafka-rest-endpoint not configured)_"
         fi
         echo ""
         echo "---"
         echo "_Audit trail per [compliance-guide.md](../../docs/compliance-guide.md)_"
       } >> "$GITHUB_STEP_SUMMARY"
   ```

3. **Emit compliance annotations step** (per D-12):
   ```yaml
   - name: Emit Compliance Annotations
     if: always() && inputs.mode == 'apply'
     run: |
       echo "::notice title=Audit Trail::Terraform apply completed for ${{ inputs.scenario-dir }} by ${{ github.actor }} at $(date -u +%Y-%m-%dT%H:%M:%SZ). Commit: ${{ github.sha }}"
       if [ -f validation-output.txt ]; then
         if grep -q "RESULT: ALL CHECKS PASSED" validation-output.txt; then
           echo "::notice title=Validation::Post-apply validation PASSED for ${{ inputs.scenario-dir }}"
         else
           echo "::warning title=Validation::Post-apply validation had FAILURES for ${{ inputs.scenario-dir }}. Review job summary."
         fi
       fi
   ```

Keep ALL existing steps exactly as they are. Only add `id: validation` to the Post-Apply Validation step and append `| tee validation-output.txt` to its command, then add the two new steps at the end of the apply job.
  </action>
  <verify>
    <automated>grep -c "GITHUB_STEP_SUMMARY" /Users/jhogan/fsi-kafka-platform/.github/workflows/terraform-scenario.yml && grep -c "::notice" /Users/jhogan/fsi-kafka-platform/.github/workflows/terraform-scenario.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/terraform-scenario.yml contains `GITHUB_STEP_SUMMARY`
    - .github/workflows/terraform-scenario.yml contains `Compliance Audit Trail`
    - .github/workflows/terraform-scenario.yml contains `Timestamp`
    - .github/workflows/terraform-scenario.yml contains `Actor`
    - .github/workflows/terraform-scenario.yml contains `Commit SHA`
    - .github/workflows/terraform-scenario.yml contains `Run ID`
    - .github/workflows/terraform-scenario.yml contains `Validation Results`
    - .github/workflows/terraform-scenario.yml contains `::notice`
    - .github/workflows/terraform-scenario.yml contains `validation-output.txt`
    - .github/workflows/terraform-scenario.yml still contains `Terraform Apply` step
    - .github/workflows/terraform-scenario.yml still contains `Post-Apply Validation` step
    - .github/workflows/terraform-scenario.yml still contains `lint:` job
    - .github/workflows/terraform-scenario.yml still contains `plan:` job
  </acceptance_criteria>
  <done>CI workflow writes structured job summary on apply with compliance metadata and validation results. Annotations surface audit trail at the workflow run level. All existing steps preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance PR template and create compliance guide</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md, docs/compliance-guide.md</files>
  <read_first>
    - .github/PULL_REQUEST_TEMPLATE.md (current template -- must preserve existing checkboxes and add compliance section)
    - .planning/phases/03-access-control-and-compliance/03-CONTEXT.md (D-12, D-13, D-14 decisions)
    - docs/schema-guide.md (if exists -- match doc style)
    - docs/adr/006-oauth-vs-api-keys.md (style reference for docs)
  </read_first>
  <action>
**Update `.github/PULL_REQUEST_TEMPLATE.md` (per D-14):**

Keep the existing `## New Topic / Schema Change` section and `### Checklist` entirely. Add a new section below it:

```markdown
### Compliance Review (required for production changes)
- [ ] Data classification reviewed (confidential / internal / public)
- [ ] RBAC verified: producer and consumer SAs have correct bindings
- [ ] Schema compatibility checked: CI validation passed
- [ ] Compliance tier justified (if sla_tier = compliance, document regulatory basis)
- [ ] PII fields identified and CSFLE configured (if data_classification = confidential)

### Change Description
<!-- Describe what this PR changes and why. Link to JIRA/issue if applicable. -->
```

**Create `docs/compliance-guide.md` (per D-12, D-13):**

Structure the guide with these sections:

1. **Overview** -- Purpose: map the CI/CD workflow to FSI control categories for regulatory examiners. This document is the starting point for audit trail navigation.

2. **Audit Trail Flow** -- Diagram (text-based, not Mermaid since this is for non-technical auditors):
   ```
   PR Created -> CI Validation -> Human Review -> Merge to Main -> Terraform Apply -> Post-Apply Validation
        |              |                |               |                 |                    |
   Change request  Automated checks  Segregation    Approval gate   Infrastructure       Verification
                                     of duties                      deployment
   ```

3. **Control Mapping** (per D-13 -- generic FSI controls, not framework-specific):

   | Control Category | Workflow Step | Evidence Location | What to Look For |
   |-----------------|--------------|-------------------|------------------|
   | Change Management | PR created with template | GitHub PR page | Completed checklist, description, linked issue |
   | Automated Validation | CI lint + validate + plan | GitHub Actions "Lint & Validate" job | terraform fmt, validate, schema validation all green |
   | Schema Governance | Schema compatibility check | GitHub Actions "Lint & Validate" job | SR compatibility check result (continue-on-error noted) |
   | Override Documentation | Compatibility override check | GitHub Actions "Lint & Validate" job | Override detection script output |
   | Access Control Review | PR template compliance checkboxes | GitHub PR page | RBAC verified checkbox checked, data classification reviewed |
   | Segregation of Duties | Human reviewer approval | GitHub PR review tab | At least one approving review from non-author |
   | Approval Gate | Branch protection merge | GitHub PR merge event | Required reviewers met, CI checks passed |
   | Deployment Automation | Terraform apply | GitHub Actions "Terraform Apply" job | Apply step succeeded, resources created |
   | Post-Deploy Verification | Post-apply validation | GitHub Actions job summary "Compliance Audit Trail" | All validation checks PASS |
   | Audit Logging | Job summary and annotations | GitHub Actions workflow run page | Timestamp, actor, commit SHA, run ID, validation results |

4. **How to Navigate an Audit** -- Step-by-step for a regulatory examiner:
   1. Start at the GitHub repository's Pull Requests tab (merged PRs)
   2. Find the PR for the change in question (search by topic name, date, or author)
   3. Review the PR description and checklist completion
   4. Check the "Files changed" tab for what was modified
   5. Check the "Conversation" tab for reviewer approval
   6. Click the CI check link to view the workflow run
   7. In the workflow run, open the "Terraform Apply" job
   8. Review the "Compliance Audit Trail" job summary (at the bottom of the job page)
   9. Verify all validation checks show PASS

5. **Framework Mapping** (per D-13 -- teams map to their specific framework):
   ```
   This guide uses generic FSI control categories. Map to your regulatory framework:
   - OCC/FFIEC: Map "Change Management" to IT Handbook - Development & Acquisition
   - PRA (UK): Map to SS1/21 Operational Resilience
   - MAS (Singapore): Map to TRM Guidelines
   - APRA (Australia): Map to CPS 234 Information Security
   ```

6. **Glossary** -- Define terms: PR, CI/CD, Terraform, RBAC, CSFLE, SLA tier, etc. for non-technical readers.
  </action>
  <verify>
    <automated>grep -c "data classification" /Users/jhogan/fsi-kafka-platform/.github/PULL_REQUEST_TEMPLATE.md && grep -c "Change Management" /Users/jhogan/fsi-kafka-platform/docs/compliance-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - .github/PULL_REQUEST_TEMPLATE.md contains `### Compliance Review`
    - .github/PULL_REQUEST_TEMPLATE.md contains `Data classification reviewed`
    - .github/PULL_REQUEST_TEMPLATE.md contains `RBAC verified`
    - .github/PULL_REQUEST_TEMPLATE.md contains `Schema compatibility checked`
    - .github/PULL_REQUEST_TEMPLATE.md contains `Compliance tier justified`
    - .github/PULL_REQUEST_TEMPLATE.md contains `PII fields identified`
    - .github/PULL_REQUEST_TEMPLATE.md still contains the original checklist items (e.g., `Avro schema (.avsc) added`)
    - docs/compliance-guide.md contains `## Audit Trail Flow`
    - docs/compliance-guide.md contains `Change Management`
    - docs/compliance-guide.md contains `Segregation of Duties`
    - docs/compliance-guide.md contains `Post-Deploy Verification`
    - docs/compliance-guide.md contains `Compliance Audit Trail`
    - docs/compliance-guide.md contains `OCC` or `FFIEC`
    - docs/compliance-guide.md contains `## How to Navigate an Audit`
    - docs/compliance-guide.md contains `## Glossary`
  </acceptance_criteria>
  <done>PR template includes compliance review checkboxes. Compliance guide maps the full audit trail for regulatory examiners with generic FSI control categories and framework mapping guidance.</done>
</task>

</tasks>

<verification>
1. `.github/workflows/terraform-scenario.yml` contains GITHUB_STEP_SUMMARY and ::notice annotations
2. `.github/PULL_REQUEST_TEMPLATE.md` has both original and new compliance checkboxes
3. `docs/compliance-guide.md` exists with control mapping table and examiner walkthrough
4. YAML syntax valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/terraform-scenario.yml'))"` exits 0
</verification>

<success_criteria>
- CI apply job produces structured audit trail visible in GitHub Actions UI
- PR template guides reviewers through compliance checks
- Compliance guide enables non-technical regulatory examiners to trace changes
- All existing CI functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/03-access-control-and-compliance/03-04-SUMMARY.md`
</output>
