---
phase: 13-dr-automation-playbooks-mm2
plan: 02
type: tdd
wave: 2
depends_on:
  - 13-01
files_modified:
  - ansible/roles/cp_dr_mm2/tasks/failback.yml
  - ansible/roles/cp_dr_mm2/tasks/main.yml
  - ansible/roles/cp_dr_mm2/tasks/check.yml
  - ansible/playbooks/dr-failback-mm2.yml
  - tests/ansible/test_cp_dr_mm2.py
autonomous: true
requirements:
  - ADR-02
  - ADR-03
  - ADR-04

must_haves:
  truths:
    - "Operator runs the failback playbook and replication direction is reversed, mirrors re-established, data sync validated, and traffic cut back to primary"
    - "Running the failback playbook with --check generates audit-ready output showing every failback step without executing mutations"
    - "DR state validation checks mirror lag against SLA-tier thresholds, cluster health, and topic writability before and after failback"
  artifacts:
    - path: "ansible/roles/cp_dr_mm2/tasks/failback.yml"
      provides: "7-step failback sequence with reversed replication"
      contains: "mm2_connector_config"
    - path: "ansible/playbooks/dr-failback-mm2.yml"
      provides: "Top-level operator-facing failback playbook"
      contains: "cp_dr_mm2_operation: failback"
    - path: "tests/ansible/test_cp_dr_mm2.py"
      provides: "Extended tests covering failback tasks, reversed connector creation, audit report"
      contains: "TestFailbackTasks"
  key_links:
    - from: "ansible/roles/cp_dr_mm2/tasks/main.yml"
      to: "ansible/roles/cp_dr_mm2/tasks/failback.yml"
      via: "include_tasks with cp_dr_mm2_operation == 'failback' condition"
      pattern: "include_tasks.*failback\\.yml"
    - from: "ansible/roles/cp_dr_mm2/tasks/failback.yml"
      to: "ansible/roles/cp_dr_mm2/tasks/consul_flip.yml"
      via: "include_tasks for Consul region cutback"
      pattern: "include_tasks.*consul_flip"
    - from: "ansible/roles/cp_dr_mm2/tasks/failback.yml"
      to: "ansible/roles/cp_dr_mm2/tasks/connector_pause.yml"
      via: "include_tasks for pausing reversed MM2 connectors"
      pattern: "include_tasks.*connector_pause"
    - from: "ansible/playbooks/dr-failback-mm2.yml"
      to: "ansible/roles/cp_dr_mm2"
      via: "include_role with cp_dr_mm2_operation: failback"
      pattern: "include_role.*cp_dr_mm2"
---

<objective>
Add the failback sequence to the cp_dr_mm2 role -- reversing replication direction by fetching original MM2 connector configs, deleting existing connectors, creating reversed ones with swapped source/target aliases and bootstrap servers, validating data sync, and cutting back to primary via Consul. Extend check mode to cover failback audit steps.

Purpose: Complete the MM2 DR automation by enabling operators to return to the primary region after a failover event with a single playbook run and full audit trail.
Output: failback.yml task file, dr-failback-mm2.yml playbook, extended check.yml with failback audit steps, extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dr-automation-playbooks-mm2/13-RESEARCH.md
@.planning/phases/13-dr-automation-playbooks-mm2/13-01-SUMMARY.md

# Role files from Plan 01 (will exist after Plan 01 completes)
@ansible/roles/cp_dr_mm2/tasks/main.yml
@ansible/roles/cp_dr_mm2/tasks/check.yml
@ansible/roles/cp_dr_mm2/tasks/failover.yml
@ansible/roles/cp_dr_mm2/tasks/connector_pause.yml
@ansible/roles/cp_dr_mm2/tasks/connector_resume.yml
@ansible/roles/cp_dr_mm2/tasks/consul_flip.yml
@ansible/roles/cp_dr_mm2/tasks/validate_state.yml
@ansible/roles/cp_dr_mm2/defaults/main.yml

# Mock fixtures from Plan 01
@tests/ansible/fixtures/mock_responses/connect/mm2_connector_config.json

# Test file to extend
@tests/ansible/test_cp_dr_mm2.py

<interfaces>
<!-- Contracts created by Plan 01 that this plan depends on -->

From ansible/roles/cp_dr_mm2/tasks/main.yml (created in Plan 01):
- Routes to failback.yml when: cp_dr_mm2_operation == 'failback' and not ansible_check_mode
- Already includes pre/post validate_state.yml
- Already builds cp_dr_mm2_results output with connectors_deleted and connectors_created keys

From ansible/roles/cp_dr_mm2/tasks/connector_pause.yml (created in Plan 01):
- Expects: _pause_connector_name, _pause_connect_url variables
- PUT /connectors/{name}/pause, polls for PAUSED state

From ansible/roles/cp_dr_mm2/tasks/connector_resume.yml (created in Plan 01):
- Expects: _resume_connector_name, _resume_connect_url variables
- PUT /connectors/{name}/resume, polls for RUNNING state

From ansible/roles/cp_dr_mm2/tasks/consul_flip.yml (created in Plan 01):
- Reads current region, PUTs new region, verifies
- Uses cp_dr_mm2_consul_url, cp_dr_mm2_consul_kv_key, cp_dr_mm2_target_region

From ansible/roles/cp_dr_mm2/defaults/main.yml (created in Plan 01):
- cp_dr_mm2_source_alias: "east"
- cp_dr_mm2_target_alias: "west"
- cp_dr_mm2_source_connector: "mm2-source-east-west"
- cp_dr_mm2_checkpoint_connector: "mm2-checkpoint-east-west"
- cp_dr_mm2_heartbeat_connector: "mm2-heartbeat-east-west"
- cp_dr_mm2_connect_url: "http://localhost:8083"
- cp_dr_mm2_results includes connectors_deleted and connectors_created keys

From tests/ansible/fixtures/mock_responses/connect/mm2_connector_config.json (created in Plan 01):
- Contains source.cluster.alias, target.cluster.alias, source/target bootstrap.servers
- connector.class: "org.apache.kafka.connect.mirror.MirrorSourceConnector"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failback tests and implement failback sequence with audit report and playbook</name>
  <files>
    tests/ansible/test_cp_dr_mm2.py,
    ansible/roles/cp_dr_mm2/tasks/failback.yml,
    ansible/roles/cp_dr_mm2/tasks/main.yml,
    ansible/roles/cp_dr_mm2/tasks/check.yml,
    ansible/playbooks/dr-failback-mm2.yml
  </files>
  <read_first>
    ansible/roles/cp_dr_mm2/tasks/main.yml,
    ansible/roles/cp_dr_mm2/tasks/check.yml,
    ansible/roles/cp_dr_mm2/tasks/failover.yml,
    ansible/roles/cp_dr_mm2/tasks/connector_pause.yml,
    ansible/roles/cp_dr_mm2/tasks/connector_resume.yml,
    ansible/roles/cp_dr_mm2/tasks/consul_flip.yml,
    ansible/roles/cp_dr_mm2/tasks/validate_state.yml,
    ansible/roles/cp_dr_mm2/defaults/main.yml,
    ansible/roles/cp_dr_mm2/meta/main.yml,
    tests/ansible/test_cp_dr_mm2.py,
    tests/ansible/fixtures/mock_responses/connect/mm2_connector_config.json,
    ansible/playbooks/dr-failover-mm2.yml
  </read_first>
  <behavior>
    ## TestFailbackTasks (NEW test class)
    - failback.yml task file exists
    - failback.yml fetches original connector config via GET BEFORE deleting (pitfall 4 from research)
    - failback.yml deletes existing MM2 connectors (source, checkpoint, heartbeat) via DELETE method
    - failback.yml creates reversed MM2 connectors with swapped source/target aliases via POST or PUT
    - failback.yml has retries/delay/until polling loop for reversed connectors to reach RUNNING state
    - failback.yml includes consul_flip.yml to cut back to primary
    - failback.yml includes connector_resume.yml for application connectors
    - failback.yml increments _dr_connectors_deleted and _dr_connectors_created counters

    ## TestFailbackCheckMode (NEW test class)
    - check.yml contains failback audit steps (7 planned steps) when cp_dr_mm2_operation == 'failback'
    - check.yml failback audit includes: fetch config, delete connectors, create reversed, wait RUNNING, validate sync, flip Consul, final validate
    - No PUT/POST/DELETE methods in check.yml (still only GET)

    ## TestFailbackPlaybook (NEW test class)
    - dr-failback-mm2.yml exists
    - dr-failback-mm2.yml targets kafka_connect[0] hosts
    - dr-failback-mm2.yml sets cp_dr_mm2_operation: failback
    - dr-failback-mm2.yml includes cp_dr_mm2 role via include_role
    - dr-failback-mm2.yml sets gather_facts: false

    ## TestMainRouting (EXTENDED -- verify failback routing exists)
    - main.yml includes failback.yml when cp_dr_mm2_operation == 'failback'

    ## TestReversedConnectorConfig (NEW test class)
    - failback.yml constructs reversed connector names (e.g., mm2-source-west-east from mm2-source-east-west pattern)
    - failback.yml references source_cluster_bootstrap_servers and target_cluster_bootstrap_servers (swapped)
  </behavior>
  <action>
    **RED phase: Add failing tests to test_cp_dr_mm2.py.**

    1. Append new test classes to `tests/ansible/test_cp_dr_mm2.py`:
       - `TestFailbackTasks`: Verify failback.yml exists, contains GET for config fetch, DELETE for connector removal, POST/PUT for reversed connector creation, retries/until for RUNNING state polling, includes consul_flip.yml, includes connector_resume.yml, increments counters
       - `TestFailbackCheckMode`: Verify check.yml has failback audit steps with `cp_dr_mm2_operation.*failback` conditional, step count for failback, no mutations
       - `TestFailbackPlaybook`: Verify dr-failback-mm2.yml exists, targets kafka_connect[0], sets cp_dr_mm2_operation: failback, includes cp_dr_mm2 role
       - `TestMainRouting` (extend existing): Add test that main.yml includes failback.yml with failback condition
       - `TestReversedConnectorConfig`: Verify failback.yml references reversed alias pattern

    2. Run tests -- new tests must FAIL (RED), existing tests must still PASS.

    **GREEN phase: Implement failback.**

    3. Create `ansible/roles/cp_dr_mm2/tasks/failback.yml` with 7-step sequence:

       **Step 1: Fetch original MM2 connector configs (GET before delete -- CRITICAL per pitfall 4)**
       ```yaml
       - name: "Fetch original config for MM2 connector {{ item }}"
         ansible.builtin.uri:
           url: "{{ cp_dr_mm2_connect_url }}/connectors/{{ item }}/config"
           method: GET
           return_content: true
           status_code:
             - 200
         register: _mm2_original_configs
         loop:
           - "{{ cp_dr_mm2_source_connector }}"
           - "{{ cp_dr_mm2_checkpoint_connector }}"
           - "{{ cp_dr_mm2_heartbeat_connector }}"
         loop_control:
           label: "{{ item }}"
         failed_when: false
       ```

       **Step 2: Delete existing MM2 connectors**
       ```yaml
       - name: "Delete MM2 connector {{ item }}"
         ansible.builtin.uri:
           url: "{{ cp_dr_mm2_connect_url }}/connectors/{{ item }}"
           method: DELETE
           status_code:
             - 204
             - 404
         loop:
           - "{{ cp_dr_mm2_source_connector }}"
           - "{{ cp_dr_mm2_checkpoint_connector }}"
           - "{{ cp_dr_mm2_heartbeat_connector }}"
         loop_control:
           label: "{{ item }}"
         failed_when: false
         register: _mm2_delete_results
       ```
       Increment `_dr_connectors_deleted` counter.

       **Step 3: Create reversed MM2 connectors**
       Derive reversed connector names by replacing `{{ cp_dr_mm2_source_alias }}-{{ cp_dr_mm2_target_alias }}` with `{{ cp_dr_mm2_target_alias }}-{{ cp_dr_mm2_source_alias }}` in each connector name. Swap `source.cluster.bootstrap.servers` and `target.cluster.bootstrap.servers` from the fetched original config. Swap `source.cluster.alias` and `target.cluster.alias`. POST to `{{ cp_dr_mm2_connect_url }}/connectors` with the reversed config.

       ```yaml
       - name: Build reversed connector config for MirrorSourceConnector
         ansible.builtin.set_fact:
           _reversed_source_config:
             name: "{{ cp_dr_mm2_source_connector | replace(cp_dr_mm2_source_alias ~ '-' ~ cp_dr_mm2_target_alias, cp_dr_mm2_target_alias ~ '-' ~ cp_dr_mm2_source_alias) }}"
             config: >-
               {{ _mm2_original_configs.results[0].json
                  | combine({
                      'source.cluster.alias': cp_dr_mm2_target_alias,
                      'target.cluster.alias': cp_dr_mm2_source_alias,
                      'source.cluster.bootstrap.servers': _mm2_original_configs.results[0].json['target.cluster.bootstrap.servers'],
                      'target.cluster.bootstrap.servers': _mm2_original_configs.results[0].json['source.cluster.bootstrap.servers']
                    })
               }}
       ```
       Repeat for checkpoint and heartbeat connectors (replacing MirrorSourceConnector class with MirrorCheckpointConnector and MirrorHeartbeatConnector respectively).

       Create each reversed connector via POST:
       ```yaml
       - name: "Create reversed MM2 connector {{ item.name }}"
         ansible.builtin.uri:
           url: "{{ cp_dr_mm2_connect_url }}/connectors"
           method: POST
           body_format: json
           body: "{{ item }}"
           status_code:
             - 201
             - 409
         loop:
           - "{{ _reversed_source_config }}"
           - "{{ _reversed_checkpoint_config }}"
           - "{{ _reversed_heartbeat_config }}"
         loop_control:
           label: "{{ item.name }}"
         failed_when: false
         register: _mm2_create_results
       ```
       Increment `_dr_connectors_created` counter.

       **Step 4: Wait for reversed connectors to reach RUNNING state**
       Poll each reversed connector status with retries/delay/until pattern.

       **Step 5: Validate data sync**
       Check connector health on reversed connectors (all RUNNING, task counts match).

       **Step 6: Flip Consul active-region back to primary**
       Include consul_flip.yml with `cp_dr_mm2_target_region` set to the primary region (cp_dr_mm2_source_alias -- e.g., "east"). Use a `vars:` block to override `cp_dr_mm2_target_region` for the cutback.

       **Step 7: Resume application connectors and final validation**
       Include connector_resume.yml for application connectors. Final validation is handled by post-validation in main.yml.

    4. Update `ansible/roles/cp_dr_mm2/tasks/check.yml`:
       - Add a conditional block for failback operation that records 7 planned failback steps into `_dr_audit_log`
       - Steps: (1) Fetch MM2 connector configs, (2) Delete existing MM2 connectors, (3) Create reversed connectors, (4) Wait for RUNNING, (5) Validate data sync, (6) Flip Consul to primary, (7) Resume application connectors + final validation
       - Keep existing failover audit steps intact
       - Route to failover or failback audit based on `cp_dr_mm2_operation` value
       - Maintain the invariant: NO PUT/POST/DELETE methods in check.yml

    5. Verify `ansible/roles/cp_dr_mm2/tasks/main.yml` already routes to failback.yml (should be done in Plan 01). If the routing condition for failback is missing, add:
       ```yaml
       - name: Execute failback sequence
         ansible.builtin.include_tasks:
           file: failback.yml
         when:
           - not (ansible_check_mode | bool)
           - cp_dr_mm2_operation == 'failback'
       ```

    6. Create `ansible/playbooks/dr-failback-mm2.yml`:
       ```yaml
       ---
       - name: MM2 DR Failback
         hosts: kafka_connect[0]
         gather_facts: false
         vars:
           cp_dr_mm2_operation: failback
         tasks:
           - name: Execute MM2 DR failback
             ansible.builtin.include_role:
               name: cp_dr_mm2
       ```

    7. Run tests -- all tests must PASS (GREEN).

    **Commit pattern:**
    - RED commit: `test(13-02): add failing tests for cp_dr_mm2 failback sequence`
    - GREEN commit: `feat(13-02): implement cp_dr_mm2 failback with reversed replication and audit report`
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && pytest tests/ansible/test_cp_dr_mm2.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f ansible/roles/cp_dr_mm2/tasks/failback.yml
    - grep -q "method: GET" ansible/roles/cp_dr_mm2/tasks/failback.yml (config fetch before delete)
    - grep -q "method: DELETE" ansible/roles/cp_dr_mm2/tasks/failback.yml
    - grep -q "method: POST" ansible/roles/cp_dr_mm2/tasks/failback.yml (reversed connector creation)
    - grep -q "retries" ansible/roles/cp_dr_mm2/tasks/failback.yml (polling for RUNNING)
    - grep -q "consul_flip" ansible/roles/cp_dr_mm2/tasks/failback.yml
    - grep -q "connector_resume" ansible/roles/cp_dr_mm2/tasks/failback.yml
    - grep -q "_dr_connectors_deleted" ansible/roles/cp_dr_mm2/tasks/failback.yml
    - grep -q "_dr_connectors_created" ansible/roles/cp_dr_mm2/tasks/failback.yml
    - grep -q "source_alias.*target_alias\|target_alias.*source_alias" ansible/roles/cp_dr_mm2/tasks/failback.yml (alias swap)
    - grep -q "failback" ansible/roles/cp_dr_mm2/tasks/check.yml (failback audit steps exist)
    - grep -qv "method: PUT\|method: POST\|method: DELETE" ansible/roles/cp_dr_mm2/tasks/check.yml (still GET-only in check mode)
    - grep -q "failback.yml" ansible/roles/cp_dr_mm2/tasks/main.yml (routing exists)
    - grep -q "cp_dr_mm2_operation: failback" ansible/playbooks/dr-failback-mm2.yml
    - grep -q "kafka_connect" ansible/playbooks/dr-failback-mm2.yml
    - grep -q "TestFailbackTasks" tests/ansible/test_cp_dr_mm2.py
    - grep -q "TestFailbackPlaybook" tests/ansible/test_cp_dr_mm2.py
    - pytest tests/ansible/test_cp_dr_mm2.py -x exits 0
    - pytest tests/ansible/ -x exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    cp_dr_mm2 role has complete failback sequence (7 steps: fetch config, delete MM2, create reversed, wait RUNNING, validate sync, flip Consul to primary, resume+validate). Check mode extended with failback-specific audit steps. Failback playbook entry point exists. All tests pass including failback-specific and full suite with zero regressions. ADR-02 (failback), ADR-03 (check mode for failback), and ADR-04 (validation for failback) are fully addressed.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/ansible/test_cp_dr_mm2.py -x -v` -- all tests pass (failover + failback)
2. `pytest tests/ansible/ -x` -- full test suite passes (zero regressions)
3. Both playbook entry points exist: `dr-failover-mm2.yml` and `dr-failback-mm2.yml`
4. check.yml still has NO PUT/POST/DELETE methods (dry-run safety)
5. failback.yml fetches config BEFORE delete (pitfall 4 verified)
6. All task files use FQCN -- `grep -rL "ansible.builtin" ansible/roles/cp_dr_mm2/tasks/*.yml` returns nothing
</verification>

<success_criteria>
- Failback sequence follows 7-step flow with GET-before-DELETE for connector configs
- Reversed connectors have swapped source/target aliases and bootstrap servers
- Check mode covers both failover (6 steps) and failback (7 steps) audit paths
- Failback playbook entry point targets kafka_connect[0]
- All unit tests pass, full test suite has zero regressions
- Both ADR-02 and the failback portions of ADR-03/ADR-04 are complete
</success_criteria>

<output>
After completion, create `.planning/phases/13-dr-automation-playbooks-mm2/13-02-SUMMARY.md`
</output>
