---
phase: 09-cp-on-rhel-and-private-cloud
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - scenarios/cp-rhel/roles/flink_standalone/tasks/main.yml
  - scenarios/cp-rhel/roles/flink_standalone/templates/flink-jobmanager.service.j2
  - scenarios/cp-rhel/roles/flink_standalone/templates/flink-taskmanager.service.j2
  - scenarios/cp-rhel/roles/flink_standalone/templates/flink-conf.yaml.j2
  - scenarios/cp-rhel/roles/flink_standalone/defaults/main.yml
  - scenarios/cp-rhel/roles/flink_standalone/handlers/main.yml
  - scenarios/cp-rhel/playbooks/deploy-flink.yml
  - scenarios/cp-rhel/playbooks/validate-fips.yml
  - scenarios/cp-rhel/inventory/group_vars/flink.yml
  - scripts/validate-fips.sh
  - .env.example
autonomous: true
requirements:
  - FLINK-03
  - COMP-03
must_haves:
  truths:
    - "Standalone Flink is deployable via Ansible role with JobManager and TaskManager systemd services"
    - "Flink integrates with Schema Registry for Avro serde via flink-sql-avro-confluent connector JAR"
    - "FIPS 140-2 compliance is validatable for CP on RHEL (OS mode, BCFKS keystore, BC FIPS provider)"
    - "FIPS validation playbook checks CFK on FIPS-enabled OpenShift via oc node inspection"
    - ".env.example has Section 16 for CP on RHEL and Private Cloud environment variables"
  artifacts:
    - path: "scenarios/cp-rhel/roles/flink_standalone/tasks/main.yml"
      provides: "Ansible tasks for Flink install, config, systemd, SR connector JAR"
      contains: "flink-sql-avro-confluent"
    - path: "scenarios/cp-rhel/roles/flink_standalone/templates/flink-jobmanager.service.j2"
      provides: "systemd unit for Flink JobManager"
      contains: "jobmanager.sh"
    - path: "scenarios/cp-rhel/roles/flink_standalone/templates/flink-taskmanager.service.j2"
      provides: "systemd unit for Flink TaskManager"
      contains: "taskmanager.sh"
    - path: "scenarios/cp-rhel/roles/flink_standalone/templates/flink-conf.yaml.j2"
      provides: "Flink configuration with SR endpoint and JMX exporter"
      contains: "rest.address"
    - path: "scenarios/cp-rhel/playbooks/deploy-flink.yml"
      provides: "Playbook to deploy standalone Flink"
      contains: "flink_standalone"
    - path: "scenarios/cp-rhel/playbooks/validate-fips.yml"
      provides: "FIPS compliance validation playbook"
      contains: "fips_enabled"
    - path: "scripts/validate-fips.sh"
      provides: "Standalone FIPS validation script for CI"
      contains: "BCFKS"
    - path: ".env.example"
      provides: "Section 16 for CP/Private Cloud vars"
      contains: "16. CP on RHEL"
  key_links:
    - from: "scenarios/cp-rhel/playbooks/deploy-flink.yml"
      to: "scenarios/cp-rhel/roles/flink_standalone/tasks/main.yml"
      via: "Ansible role inclusion"
      pattern: "flink_standalone"
    - from: "scenarios/cp-rhel/roles/flink_standalone/templates/flink-conf.yaml.j2"
      to: "Schema Registry"
      via: "flink_sr_url variable in config template"
      pattern: "flink_sr_url"
    - from: "scenarios/cp-rhel/playbooks/validate-fips.yml"
      to: "scenarios/cp-rhel/inventory/group_vars/all.yml"
      via: "fips_enabled variable checked in validation tasks"
      pattern: "fips_enabled"
---

<objective>
Create standalone Flink Ansible role with systemd service management and SR integration, add FIPS 140-2 compliance validation for both CP on RHEL and CFK on OpenShift, and extend .env.example with CP/Private Cloud section.

Purpose: FLINK-03 requires standalone Flink deployed via Ansible with systemd (Confluent Platform Flink requires Kubernetes -- RHEL must use open-source Apache Flink standalone mode). COMP-03 requires automated FIPS validation for both CP on RHEL (OS-level + CP config) and CFK on FIPS-enabled OpenShift (Helm flag + node check). The .env.example needs Section 16 for CP-specific environment variables.

Output: Custom Ansible role for Flink, FIPS validation playbook + standalone script, and .env.example extension.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cp-on-rhel-and-private-cloud/09-CONTEXT.md
@.planning/phases/09-cp-on-rhel-and-private-cloud/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From scenarios/cp-rhel/ (created by Plan 01):
```
scenarios/cp-rhel/
  README.md
  inventory/
    hosts.yml.example
    group_vars/
      all.yml          # Contains fips_enabled: false, fips_mode: "fips-140-2"
      kafka_broker.yml
      schema_registry.yml
      kafka_connect.yml
  playbooks/
    deploy-cp.yml      # Main CP deployment playbook
  topics/
    *.yml              # CPTopic definitions
```

From scenarios/cfk-openshift/ (Phase 8, FIPS reference for CFK):
```
# CFK FIPS is enabled via Helm flag: --set fipsmode=true
# CFK components use BCFKS keystores when FIPS mode is enabled
# OpenShift FIPS must be enabled at cluster install time
```

From .env.example (current sections end at 15):
```
# ── 15. CFK on OpenShift ──
# ... (lines 160-181, ends at line 181)
```

From reference/flink-sql/ (Flink SQL templates -- reference for SR integration):
```
# Flink SQL uses 'avro-confluent' format for SR integration
# flink-sql-avro-confluent-3.2.0-1.20.jar required in $FLINK_HOME/lib/
```

From Phase 8 CFK Flink pattern (08-03-SUMMARY.md):
```
# FlinkDeployment CRDs use Application mode with SQL mounted via ConfigMap
# All FlinkDeployments include JMX exporter volumeMount for observability
# Flink version: 1.20, connector: flink-sql-avro-confluent 3.2.0-1.20
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone Flink Ansible role with systemd and SR integration</name>
  <files>
    scenarios/cp-rhel/roles/flink_standalone/tasks/main.yml
    scenarios/cp-rhel/roles/flink_standalone/templates/flink-jobmanager.service.j2
    scenarios/cp-rhel/roles/flink_standalone/templates/flink-taskmanager.service.j2
    scenarios/cp-rhel/roles/flink_standalone/templates/flink-conf.yaml.j2
    scenarios/cp-rhel/roles/flink_standalone/defaults/main.yml
    scenarios/cp-rhel/roles/flink_standalone/handlers/main.yml
    scenarios/cp-rhel/playbooks/deploy-flink.yml
    scenarios/cp-rhel/inventory/group_vars/flink.yml
  </files>
  <read_first>
    scenarios/cp-rhel/README.md
    scenarios/cp-rhel/inventory/hosts.yml.example
    scenarios/cp-rhel/inventory/group_vars/all.yml
    scenarios/cp-rhel/playbooks/deploy-cp.yml
    scenarios/cfk-openshift/flink/flink-jobmanager.yaml
    scenarios/cfk-openshift/flink/flink-taskmanager.yaml
  </read_first>
  <action>
**Create the flink_standalone Ansible role:**

1. **roles/flink_standalone/defaults/main.yml** -- Default variables for the role:
   ```yaml
   ---
   # Apache Flink standalone deployment defaults
   # Confluent Platform Flink requires Kubernetes; RHEL uses open-source Apache Flink
   flink_version: "1.20.1"
   flink_home: "/opt/flink"
   flink_user: flink
   flink_group: flink
   java_home: "/usr/lib/jvm/java-17"

   # Download URL for Apache Flink binary
   flink_download_url: "https://archive.apache.org/dist/flink/flink-{{ flink_version }}/flink-{{ flink_version }}-bin-scala_2.12.tgz"

   # SR integration: Avro-Confluent format connector
   flink_avro_confluent_version: "3.2.0-1.20"
   flink_avro_version: "1.20.1"
   flink_sr_url: "http://sr-east-1:8081"
   flink_sr_basic_auth_enabled: false
   flink_sr_basic_auth_user: ""
   flink_sr_basic_auth_password: ""

   # Cluster configuration
   flink_jobmanager_host: "{{ ansible_hostname }}"
   flink_jobmanager_rpc_port: 6123
   flink_jobmanager_memory: "1024m"
   flink_taskmanager_memory: "2048m"
   flink_taskmanager_slots: 4
   flink_rest_port: 8085
   flink_parallelism_default: 2

   # JMX exporter for observability (matches CFK Flink pattern from Phase 8)
   flink_jmx_enabled: true
   flink_jmx_port: 9999

   # Systemd
   flink_service_restart_sec: 30
   flink_nofile_limit: 65536
   ```

2. **roles/flink_standalone/tasks/main.yml** -- Role tasks:
   ```yaml
   ---
   # Standalone Flink deployment on RHEL via systemd
   # Uses open-source Apache Flink (not CP Flink, which requires Kubernetes)

   - name: Create flink group
     group:
       name: "{{ flink_group }}"
       state: present

   - name: Create flink user
     user:
       name: "{{ flink_user }}"
       group: "{{ flink_group }}"
       home: "{{ flink_home }}"
       shell: /bin/bash
       system: true

   - name: Create Flink directories
     file:
       path: "{{ item }}"
       state: directory
       owner: "{{ flink_user }}"
       group: "{{ flink_group }}"
       mode: "0755"
     loop:
       - "{{ flink_home }}"
       - "{{ flink_home }}/lib"
       - "{{ flink_home }}/log"
       - "{{ flink_home }}/conf"

   - name: Download Apache Flink binary
     get_url:
       url: "{{ flink_download_url }}"
       dest: "/tmp/flink-{{ flink_version }}.tgz"
       mode: "0644"

   - name: Extract Flink binary
     unarchive:
       src: "/tmp/flink-{{ flink_version }}.tgz"
       dest: "{{ flink_home }}"
       remote_src: true
       extra_opts: ["--strip-components=1"]
       owner: "{{ flink_user }}"
       group: "{{ flink_group }}"

   - name: Download flink-sql-avro-confluent connector JAR
     get_url:
       url: "https://repo1.maven.org/maven2/org/apache/flink/flink-sql-avro-confluent/{{ flink_avro_confluent_version }}/flink-sql-avro-confluent-{{ flink_avro_confluent_version }}.jar"
       dest: "{{ flink_home }}/lib/flink-sql-avro-confluent-{{ flink_avro_confluent_version }}.jar"
       owner: "{{ flink_user }}"
       group: "{{ flink_group }}"
       mode: "0644"

   - name: Download flink-avro JAR
     get_url:
       url: "https://repo1.maven.org/maven2/org/apache/flink/flink-avro/{{ flink_avro_version }}/flink-avro-{{ flink_avro_version }}.jar"
       dest: "{{ flink_home }}/lib/flink-avro-{{ flink_avro_version }}.jar"
       owner: "{{ flink_user }}"
       group: "{{ flink_group }}"
       mode: "0644"

   - name: Template Flink configuration
     template:
       src: flink-conf.yaml.j2
       dest: "{{ flink_home }}/conf/flink-conf.yaml"
       owner: "{{ flink_user }}"
       group: "{{ flink_group }}"
       mode: "0644"
     notify:
       - restart flink-jobmanager
       - restart flink-taskmanager

   - name: Template JobManager systemd unit
     template:
       src: flink-jobmanager.service.j2
       dest: /etc/systemd/system/flink-jobmanager.service
       mode: "0644"
     notify:
       - reload systemd
       - restart flink-jobmanager

   - name: Template TaskManager systemd unit
     template:
       src: flink-taskmanager.service.j2
       dest: /etc/systemd/system/flink-taskmanager.service
       mode: "0644"
     notify:
       - reload systemd
       - restart flink-taskmanager

   - name: Enable and start Flink JobManager
     systemd:
       name: flink-jobmanager
       enabled: true
       state: started
       daemon_reload: true
     when: "'flink_jobmanager' in group_names"

   - name: Enable and start Flink TaskManager
     systemd:
       name: flink-taskmanager
       enabled: true
       state: started
       daemon_reload: true
     when: "'flink_taskmanager' in group_names"
   ```

3. **roles/flink_standalone/templates/flink-jobmanager.service.j2** -- systemd unit:
   ```ini
   # Managed by Ansible -- flink_standalone role
   [Unit]
   Description=Apache Flink JobManager
   After=network.target
   Documentation=https://nightlies.apache.org/flink/flink-docs-release-1.20/

   [Service]
   Type=forking
   User={{ flink_user }}
   Group={{ flink_group }}
   ExecStart={{ flink_home }}/bin/jobmanager.sh start
   ExecStop={{ flink_home }}/bin/jobmanager.sh stop
   Environment="JAVA_HOME={{ java_home }}"
   Environment="FLINK_HOME={{ flink_home }}"
   {% if flink_jmx_enabled %}
   Environment="FLINK_ENV_JAVA_OPTS=-Dcom.sun.management.jmxremote -Dcom.sun.management.jmxremote.port={{ flink_jmx_port }} -Dcom.sun.management.jmxremote.authenticate=false -Dcom.sun.management.jmxremote.ssl=false"
   {% endif %}
   Restart=on-failure
   RestartSec={{ flink_service_restart_sec }}
   LimitNOFILE={{ flink_nofile_limit }}

   [Install]
   WantedBy=multi-user.target
   ```

4. **roles/flink_standalone/templates/flink-taskmanager.service.j2** -- systemd unit:
   ```ini
   # Managed by Ansible -- flink_standalone role
   [Unit]
   Description=Apache Flink TaskManager
   After=network.target flink-jobmanager.service
   Documentation=https://nightlies.apache.org/flink/flink-docs-release-1.20/

   [Service]
   Type=forking
   User={{ flink_user }}
   Group={{ flink_group }}
   ExecStart={{ flink_home }}/bin/taskmanager.sh start
   ExecStop={{ flink_home }}/bin/taskmanager.sh stop
   Environment="JAVA_HOME={{ java_home }}"
   Environment="FLINK_HOME={{ flink_home }}"
   {% if flink_jmx_enabled %}
   Environment="FLINK_ENV_JAVA_OPTS=-Dcom.sun.management.jmxremote -Dcom.sun.management.jmxremote.port={{ flink_jmx_port | int + 1 }} -Dcom.sun.management.jmxremote.authenticate=false -Dcom.sun.management.jmxremote.ssl=false"
   {% endif %}
   Restart=on-failure
   RestartSec={{ flink_service_restart_sec }}
   LimitNOFILE={{ flink_nofile_limit }}

   [Install]
   WantedBy=multi-user.target
   ```

5. **roles/flink_standalone/templates/flink-conf.yaml.j2** -- Flink config:
   ```yaml
   # Managed by Ansible -- flink_standalone role
   # Apache Flink standalone configuration for CP on RHEL

   # JobManager
   jobmanager.rpc.address: {{ flink_jobmanager_host }}
   jobmanager.rpc.port: {{ flink_jobmanager_rpc_port }}
   jobmanager.memory.process.size: {{ flink_jobmanager_memory }}

   # TaskManager
   taskmanager.memory.process.size: {{ flink_taskmanager_memory }}
   taskmanager.numberOfTaskSlots: {{ flink_taskmanager_slots }}

   # Parallelism
   parallelism.default: {{ flink_parallelism_default }}

   # REST endpoint (Flink UI)
   rest.address: {{ flink_jobmanager_host }}
   rest.port: {{ flink_rest_port }}

   # Schema Registry integration (avro-confluent format)
   # Used by Flink SQL with FORMAT = 'avro-confluent'
   # flink-sql-avro-confluent JAR must be in $FLINK_HOME/lib/
   {% if flink_sr_url is defined and flink_sr_url %}
   # SR endpoint for avro-confluent format connector
   # Referenced in Flink SQL CREATE TABLE statements via:
   #   'avro-confluent.url' = '{{ flink_sr_url }}'
   {% endif %}

   {% if flink_jmx_enabled %}
   # JMX metrics for observability (matches CFK Flink JMX exporter pattern)
   metrics.reporter.jmx.factory.class: org.apache.flink.metrics.jmx.JMXReporterFactory
   metrics.reporter.jmx.port: {{ flink_jmx_port }}
   {% endif %}

   # High availability (optional -- configure for production)
   # high-availability.type: zookeeper
   # high-availability.storageDir: hdfs:///flink/ha/
   # high-availability.zookeeper.quorum: zk1:2181,zk2:2181
   ```

6. **roles/flink_standalone/handlers/main.yml** -- Handlers:
   ```yaml
   ---
   - name: reload systemd
     systemd:
       daemon_reload: true

   - name: restart flink-jobmanager
     systemd:
       name: flink-jobmanager
       state: restarted
     when: "'flink_jobmanager' in group_names"

   - name: restart flink-taskmanager
     systemd:
       name: flink-taskmanager
       state: restarted
     when: "'flink_taskmanager' in group_names"
   ```

7. **playbooks/deploy-flink.yml** -- Flink deployment playbook:
   ```yaml
   ---
   # Deploy standalone Apache Flink on RHEL
   # Usage: ansible-playbook -i inventory/hosts.yml playbooks/deploy-flink.yml
   #
   # Note: Confluent Platform Flink requires Kubernetes.
   # This playbook deploys open-source Apache Flink 1.20 in standalone mode
   # with the flink-sql-avro-confluent connector for SR integration.
   - name: Deploy Flink standalone
     hosts: flink_jobmanager:flink_taskmanager
     become: true
     roles:
       - flink_standalone
   ```

8. **inventory/group_vars/flink.yml** -- Flink-specific group vars:
   ```yaml
   ---
   # Flink standalone configuration for CP on RHEL
   # Override defaults from roles/flink_standalone/defaults/main.yml

   # SR integration endpoint (matches Schema Registry from CP deployment)
   flink_sr_url: "https://sr-east-1:8081"

   # Flink REST UI port (8085 to avoid conflict with SR on 8081)
   flink_rest_port: 8085

   # JMX for observability
   flink_jmx_enabled: true
   flink_jmx_port: 9999
   ```

9. **Update inventory/hosts.yml.example** -- Add Flink host groups after kafka_connect:
   ```yaml
       # Standalone Flink (open-source Apache Flink, not CP Flink which requires K8s)
       flink_jobmanager:
         hosts:
           flink-jm-1: {}
       flink_taskmanager:
         hosts:
           flink-tm-1: {}
           flink-tm-2: {}
   ```

10. **Update README.md** -- Add Flink section after the existing Security section:
    - Add "## Standalone Flink" section explaining: uses open-source Apache Flink 1.20 (not CP Flink which needs K8s), deployed via custom Ansible role, integrates with SR via flink-sql-avro-confluent connector, managed by systemd
    - Add deploy-flink.yml to the Quick Start steps
    - Add Flink port (8085) to the Architecture section
  </action>
  <verify>
    <automated>ansible-playbook --syntax-check /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/playbooks/deploy-flink.yml 2>&1 || echo "ansible not installed -- checking file structure instead" && ls /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/roles/flink_standalone/tasks/main.yml /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/roles/flink_standalone/templates/flink-jobmanager.service.j2 /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/roles/flink_standalone/templates/flink-taskmanager.service.j2 /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/roles/flink_standalone/templates/flink-conf.yaml.j2 /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/roles/flink_standalone/defaults/main.yml /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/roles/flink_standalone/handlers/main.yml /Users/jhogan/fsi-kafka-platform/scenarios/cp-rhel/playbooks/deploy-flink.yml</automated>
  </verify>
  <acceptance_criteria>
    - scenarios/cp-rhel/roles/flink_standalone/tasks/main.yml contains "flink-sql-avro-confluent"
    - scenarios/cp-rhel/roles/flink_standalone/tasks/main.yml contains "systemd"
    - scenarios/cp-rhel/roles/flink_standalone/templates/flink-jobmanager.service.j2 contains "jobmanager.sh start"
    - scenarios/cp-rhel/roles/flink_standalone/templates/flink-jobmanager.service.j2 contains "Type=forking"
    - scenarios/cp-rhel/roles/flink_standalone/templates/flink-taskmanager.service.j2 contains "taskmanager.sh start"
    - scenarios/cp-rhel/roles/flink_standalone/templates/flink-conf.yaml.j2 contains "rest.address"
    - scenarios/cp-rhel/roles/flink_standalone/templates/flink-conf.yaml.j2 contains "metrics.reporter.jmx"
    - scenarios/cp-rhel/roles/flink_standalone/defaults/main.yml contains 'flink_version: "1.20.1"'
    - scenarios/cp-rhel/roles/flink_standalone/defaults/main.yml contains 'flink_avro_confluent_version: "3.2.0-1.20"'
    - scenarios/cp-rhel/roles/flink_standalone/defaults/main.yml contains "flink_sr_url"
    - scenarios/cp-rhel/roles/flink_standalone/handlers/main.yml contains "restart flink-jobmanager"
    - scenarios/cp-rhel/playbooks/deploy-flink.yml contains "flink_standalone"
    - scenarios/cp-rhel/inventory/group_vars/flink.yml contains "flink_sr_url"
    - scenarios/cp-rhel/inventory/hosts.yml.example contains "flink_jobmanager"
    - scenarios/cp-rhel/inventory/hosts.yml.example contains "flink_taskmanager"
  </acceptance_criteria>
  <done>Standalone Flink Ansible role exists with systemd unit templates, flink-conf.yaml template, SR connector JAR download, JMX metrics, and deployment playbook. Host inventory includes flink_jobmanager and flink_taskmanager groups.</done>
</task>

<task type="auto">
  <name>Task 2: Add FIPS 140-2 compliance validation and .env.example CP/Private Cloud section</name>
  <files>
    scenarios/cp-rhel/playbooks/validate-fips.yml
    scripts/validate-fips.sh
    .env.example
  </files>
  <read_first>
    scenarios/cp-rhel/inventory/group_vars/all.yml
    scenarios/cp-rhel/README.md
    scenarios/cfk-openshift/README.md
    .env.example
  </read_first>
  <action>
**A. Create scenarios/cp-rhel/playbooks/validate-fips.yml** -- Ansible FIPS validation playbook:

```yaml
# --- (YAML document start -- add to actual file)
# Validate FIPS 140-2 compliance for Confluent Platform on RHEL
# Usage: ansible-playbook -i inventory/hosts.yml playbooks/validate-fips.yml
#
# Checks:
#   1. OS-level FIPS mode enabled (/proc/sys/crypto/fips_enabled == 1)
#   2. Java 17+ present (required for FIPS crypto providers)
#   3. BCFKS keystore configured in CP component properties
#   4. Bouncy Castle FIPS provider configured
#   5. Only FIPS-approved TLS cipher suites active
#
# For CFK on OpenShift: See the CFK FIPS section below (oc-based checks)
- name: Validate FIPS 140-2 compliance -- CP on RHEL
  hosts: kafka_broker:schema_registry:kafka_connect
  become: true
  gather_facts: true
  tasks:

    - name: "[FIPS-01] Verify OS FIPS mode enabled"
      command: cat /proc/sys/crypto/fips_enabled
      register: fips_check
      failed_when: fips_check.stdout.strip() != "1"
      changed_when: false

    - name: "[FIPS-02] Verify Java 17+ installed"
      shell: java -version 2>&1 | head -1
      register: java_check
      changed_when: false

    - name: "[FIPS-02] Assert Java 17+"
      assert:
        that:
          - "'17' in java_check.stdout or '21' in java_check.stdout or '17' in java_check.stderr or '21' in java_check.stderr"
        fail_msg: "Java 17 or 21 required for FIPS. Found: {{ java_check.stdout }}{{ java_check.stderr }}"

    - name: "[FIPS-03] Verify BCFKS keystore in Kafka broker config"
      shell: grep -c "ssl.keystore.type=BCFKS" /etc/kafka/server.properties
      register: broker_keystore
      failed_when: broker_keystore.stdout.strip() == "0"
      changed_when: false
      when: "'kafka_broker' in group_names"

    - name: "[FIPS-03] Verify BCFKS keystore in Schema Registry config"
      shell: grep -c "ssl.keystore.type=BCFKS" /etc/schema-registry/schema-registry.properties
      register: sr_keystore
      failed_when: sr_keystore.stdout.strip() == "0"
      changed_when: false
      when: "'schema_registry' in group_names"

    - name: "[FIPS-03] Verify BCFKS keystore in Connect config"
      shell: grep -c "ssl.keystore.type=BCFKS" /etc/kafka-connect/connect-distributed.properties
      register: connect_keystore
      failed_when: connect_keystore.stdout.strip() == "0"
      changed_when: false
      when: "'kafka_connect' in group_names"

    - name: "[FIPS-04] Verify Bouncy Castle FIPS provider in Kafka broker"
      shell: grep -c "BcFipsProviderCreator" /etc/kafka/server.properties
      register: broker_bcfips
      failed_when: broker_bcfips.stdout.strip() == "0"
      changed_when: false
      when: "'kafka_broker' in group_names"

    - name: "[FIPS-05] Report FIPS validation results"
      debug:
        msg: >-
          FIPS 140-2 validation PASSED for {{ inventory_hostname }}.
          OS FIPS: {{ fips_check.stdout.strip() }},
          Java: {{ java_check.stdout }}{{ java_check.stderr }}

# CFK on OpenShift FIPS validation
# Run separately against an OpenShift cluster (not Ansible-managed hosts)
- name: Validate FIPS 140-2 compliance -- CFK on OpenShift
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    cfk_namespace: "{{ lookup('env', 'CFK_NAMESPACE') | default('confluent', true) }}"
  tasks:

    - name: "[CFK-FIPS-01] Check OpenShift FIPS mode on nodes"
      command: >
        oc get nodes -o jsonpath='{range .items[*]}{.metadata.name}{": FIPS="}{.status.nodeInfo.operatingSystem}{"\n"}{end}'
      register: node_fips
      changed_when: false
      ignore_errors: true

    - name: "[CFK-FIPS-01] Report node FIPS status"
      debug:
        msg: "{{ node_fips.stdout_lines | default(['oc not available or not connected to cluster']) }}"

    - name: "[CFK-FIPS-02] Verify CFK operator deployed with FIPS mode"
      command: >
        oc get deployment confluent-operator -n {{ cfk_namespace }}
          -o jsonpath='{.spec.template.spec.containers[0].env}'
      register: cfk_fips_check
      changed_when: false
      ignore_errors: true

    - name: "[CFK-FIPS-02] Report CFK FIPS configuration"
      debug:
        msg: >-
          CFK FIPS check: Verify Helm install used --set fipsmode=true.
          Operator env: {{ cfk_fips_check.stdout | default('N/A') }}
```

**B. Create scripts/validate-fips.sh** -- Standalone FIPS validation script (CI-friendly):

```bash
#!/usr/bin/env bash
# =============================================================================
# FSI Kafka Platform -- FIPS 140-2 Compliance Validation
# Validates FIPS compliance for Confluent Platform on RHEL.
# Can also check CFK on OpenShift via --cfk flag.
#
# Usage:
#   scripts/validate-fips.sh           # Validate CP on RHEL (local host)
#   scripts/validate-fips.sh --cfk     # Validate CFK on OpenShift (via oc)
#   scripts/validate-fips.sh --check   # Syntax check only (CI-safe, no host)
# =============================================================================
set -euo pipefail

MODE="${1:-rhel}"
PASS=0
FAIL=0
WARN=0

echo "=== FIPS 140-2 Compliance Validation ==="
echo ""

if [ "${MODE}" = "--check" ]; then
  echo "  Syntax check mode -- validating script structure only"
  echo "  PASS: Script is valid"
  exit 0
fi

if [ "${MODE}" = "--cfk" ]; then
  echo "  Mode: CFK on OpenShift"
  echo ""

  # Check 1: oc CLI available
  echo -n "  [1] oc CLI available: "
  if command -v oc >/dev/null 2>&1; then
    echo "PASS"
    ((PASS++)) || true
  else
    echo "FAIL -- install oc CLI"
    ((FAIL++)) || true
  fi

  # Check 2: OpenShift FIPS mode (node-level)
  echo -n "  [2] OpenShift nodes FIPS-enabled: "
  if oc get nodes >/dev/null 2>&1; then
    fips_nodes=$(oc debug node/"$(oc get nodes -o jsonpath='{.items[0].metadata.name}')" -- cat /proc/sys/crypto/fips_enabled 2>/dev/null || echo "unknown")
    if [ "${fips_nodes}" = "1" ]; then
      echo "PASS"
      ((PASS++)) || true
    else
      echo "WARN -- could not verify (FIPS must be enabled at install time)"
      ((WARN++)) || true
    fi
  else
    echo "FAIL -- cannot reach OpenShift cluster"
    ((FAIL++)) || true
  fi

  # Check 3: CFK Helm release has fipsmode=true
  echo -n "  [3] CFK fipsmode Helm flag: "
  cfk_ns="${CFK_NAMESPACE:-confluent}"
  if oc get deployment confluent-operator -n "${cfk_ns}" >/dev/null 2>&1; then
    echo "PASS -- operator found in ${cfk_ns}"
    ((PASS++)) || true
    echo "    Note: Verify Helm install used --set fipsmode=true"
  else
    echo "WARN -- operator not found in namespace ${cfk_ns}"
    ((WARN++)) || true
  fi

else
  echo "  Mode: CP on RHEL"
  echo ""

  # Check 1: OS-level FIPS mode
  echo -n "  [1] OS FIPS mode (/proc/sys/crypto/fips_enabled): "
  if [ -f /proc/sys/crypto/fips_enabled ]; then
    fips_val=$(cat /proc/sys/crypto/fips_enabled)
    if [ "${fips_val}" = "1" ]; then
      echo "PASS"
      ((PASS++)) || true
    else
      echo "FAIL (value=${fips_val})"
      echo "    Action: Run 'fips-mode-setup --enable' and reboot"
      ((FAIL++)) || true
    fi
  else
    echo "FAIL -- /proc/sys/crypto/fips_enabled not found (not a FIPS-capable OS?)"
    ((FAIL++)) || true
  fi

  # Check 2: Java version (17 or 21 required for FIPS crypto)
  echo -n "  [2] Java version: "
  if command -v java >/dev/null 2>&1; then
    java_ver=$(java -version 2>&1 | head -1)
    echo "${java_ver}"
    ((PASS++)) || true
  else
    echo "FAIL -- java not found"
    ((FAIL++)) || true
  fi

  # Check 3: BCFKS keystore in CP component configs
  echo "  [3] BCFKS keystore check:"
  for conf in /etc/kafka/server.properties /etc/schema-registry/schema-registry.properties /etc/kafka-connect/connect-distributed.properties; do
    echo -n "    ${conf}: "
    if [ -f "${conf}" ]; then
      if grep -q "ssl.keystore.type=BCFKS" "${conf}"; then
        echo "PASS"
        ((PASS++)) || true
      else
        echo "FAIL -- BCFKS not configured (using PKCS12 is NOT FIPS-compliant)"
        ((FAIL++)) || true
      fi
    else
      echo "SKIP -- file not found"
      ((WARN++)) || true
    fi
  done

  # Check 4: Bouncy Castle FIPS provider
  echo -n "  [4] BC FIPS provider in broker config: "
  if [ -f /etc/kafka/server.properties ]; then
    if grep -q "BcFipsProviderCreator" /etc/kafka/server.properties; then
      echo "PASS"
      ((PASS++)) || true
    else
      echo "FAIL -- BcFipsProviderCreator not configured"
      ((FAIL++)) || true
    fi
  else
    echo "SKIP -- /etc/kafka/server.properties not found"
    ((WARN++)) || true
  fi
fi

echo ""
echo "=== Results: ${PASS} passed, ${WARN} warnings, ${FAIL} failed ==="

if [ "${FAIL}" -gt 0 ]; then
  exit 1
fi
exit 0
```

Make the script executable: `chmod +x scripts/validate-fips.sh`

**C. Extend .env.example** -- Add Section 16 after the existing Section 15 (CFK on OpenShift). Append to the file:

```
# ── 16. CP on RHEL and Private Cloud ──
# Configuration for Confluent Platform on RHEL (Ansible-deployed)
# and Private Cloud (Terraform-managed self-hosted CP).

# CP Deployment (Ansible)
# CP_VERSION=7.6.0
# CP_ANSIBLE_INVENTORY=scenarios/cp-rhel/inventory/hosts.yml

# Private Cloud Terraform
# CP_KAFKA_REST_ENDPOINT=https://kafka-east-1.example.com:8090
# CP_KAFKA_API_KEY=                                    # SENSITIVE
# CP_KAFKA_API_SECRET=                                 # SENSITIVE
# CP_SR_REST_ENDPOINT=https://sr-east-1.example.com:8081
# CP_SR_API_KEY=                                       # SENSITIVE
# CP_SR_API_SECRET=                                    # SENSITIVE
# CP_KAFKA_CLUSTER_ID=kafka-east-cluster

# MRC Backend (Multi-Region Cluster for RPO=0)
# FSI_DR_BACKEND=mrc
# FSI_MRC_BOOTSTRAP=kafka-east-1:9092,kafka-east-2:9092,kafka-east-3:9092
# FSI_MRC_EAST_RACK=us-east
# FSI_MRC_WEST_RACK=us-west
# FSI_MRC_OBSERVER_RACK=us-central
# FSI_MRC_COMMAND_CONFIG=/etc/kafka/admin.properties

# Standalone Flink (open-source Apache Flink on RHEL)
# FLINK_VERSION=1.20.1
# FLINK_HOME=/opt/flink
# FLINK_SR_URL=https://sr-east-1:8081
# FLINK_REST_PORT=8085
# FLINK_JMX_PORT=9999

# FIPS 140-2 Compliance
# FIPS_ENABLED=false
# FIPS_MODE=fips-140-2
```
  </action>
  <verify>
    <automated>bash /Users/jhogan/fsi-kafka-platform/scripts/validate-fips.sh --check</automated>
  </verify>
  <acceptance_criteria>
    - scenarios/cp-rhel/playbooks/validate-fips.yml contains "FIPS-01"
    - scenarios/cp-rhel/playbooks/validate-fips.yml contains "BCFKS"
    - scenarios/cp-rhel/playbooks/validate-fips.yml contains "BcFipsProviderCreator"
    - scenarios/cp-rhel/playbooks/validate-fips.yml contains "fips_enabled"
    - scenarios/cp-rhel/playbooks/validate-fips.yml contains "CFK-FIPS-01"
    - scenarios/cp-rhel/playbooks/validate-fips.yml contains "fipsmode"
    - scripts/validate-fips.sh contains "FIPS 140-2 Compliance Validation"
    - scripts/validate-fips.sh contains "BCFKS"
    - scripts/validate-fips.sh contains "BcFipsProviderCreator"
    - scripts/validate-fips.sh contains "--cfk"
    - scripts/validate-fips.sh contains "--check"
    - `bash scripts/validate-fips.sh --check` exits 0
    - .env.example contains "16. CP on RHEL"
    - .env.example contains "FSI_MRC_BOOTSTRAP"
    - .env.example contains "CP_KAFKA_REST_ENDPOINT"
    - .env.example contains "FLINK_VERSION"
    - .env.example contains "FIPS_ENABLED"
  </acceptance_criteria>
  <done>FIPS validation playbook validates both CP-RHEL (OS mode, BCFKS, BC FIPS provider) and CFK-OpenShift (node FIPS, operator config). Standalone script has --check mode for CI. .env.example Section 16 covers CP deployment, Private Cloud TF, MRC backend, standalone Flink, and FIPS config.</done>
</task>

</tasks>

<verification>
1. All Flink role files exist under scenarios/cp-rhel/roles/flink_standalone/
2. `ansible-playbook --syntax-check scenarios/cp-rhel/playbooks/deploy-flink.yml` passes (if ansible installed) or all files exist
3. `bash scripts/validate-fips.sh --check` exits 0
4. .env.example contains Section 16 with CP, Private Cloud, MRC, Flink, and FIPS vars
5. scenarios/cp-rhel/playbooks/validate-fips.yml validates both CP-RHEL and CFK-OpenShift FIPS
6. `python3 ci/scripts/c4e-precheck.py --scenario-dir scenarios/cp-rhel/ --verbose` still exits 0 (regression from Plan 01)
</verification>

<success_criteria>
- Standalone Flink Ansible role has tasks, templates (JM/TM systemd, flink-conf.yaml), defaults, handlers
- Flink role downloads flink-sql-avro-confluent connector JAR for SR integration
- FIPS validation covers both CP-RHEL (5 checks) and CFK-OpenShift (3 checks)
- validate-fips.sh has --check mode for CI syntax validation
- .env.example Section 16 covers all Phase 9 environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/09-cp-on-rhel-and-private-cloud/09-03-SUMMARY.md`
</output>
