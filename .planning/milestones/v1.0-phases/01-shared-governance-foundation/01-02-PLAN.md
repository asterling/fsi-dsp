---
phase: 01-shared-governance-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ci/scripts/validate-schemas.py
  - ci/scripts/check-overrides.sh
  - .github/workflows/terraform-plan.yml
  - docs/schema-guide.md
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04

must_haves:
  truths:
    - "Python script validates Avro schema structure, namespace convention, and SR compatibility"
    - "CI pipeline runs schema validation on every PR touching schemas/ modules/ or environments/"
    - "CI blocks compatibility_override in scenario .tf files unless PR body contains ADR/JIRA reference"
    - "Schema guide contains a step-by-step breaking change runbook with 5 phases"
  artifacts:
    - path: "ci/scripts/validate-schemas.py"
      provides: "Schema validation: structure, namespace, compatibility"
      contains: "NAMESPACE_PATTERN"
    - path: "ci/scripts/check-overrides.sh"
      provides: "Override detection and PR body validation"
      contains: "compatibility_override"
    - path: ".github/workflows/terraform-plan.yml"
      provides: "Enhanced CI pipeline with schema validation steps"
      contains: "validate-schemas.py"
    - path: "docs/schema-guide.md"
      provides: "Breaking change runbook section"
      contains: "Breaking Change Runbook"
  key_links:
    - from: ".github/workflows/terraform-plan.yml"
      to: "ci/scripts/validate-schemas.py"
      via: "python3 ci/scripts/validate-schemas.py"
      pattern: "validate-schemas\\.py"
    - from: ".github/workflows/terraform-plan.yml"
      to: "ci/scripts/check-overrides.sh"
      via: "bash ci/scripts/check-overrides.sh"
      pattern: "check-overrides\\.sh"
    - from: "ci/scripts/validate-schemas.py"
      to: "Schema Registry REST API"
      via: "POST /compatibility/subjects/{subject}/versions"
      pattern: "compatibility/subjects"
---

<objective>
Create the schema CI validation pipeline: a Python script for Avro schema structure/namespace/compatibility validation, a bash script for compatibility_override detection, enhanced CI workflow steps, and the breaking change runbook in schema-guide.md.

Purpose: SCHEMA-01 through SCHEMA-04 require CI-level enforcement of schema governance before any PR merges. Currently the CI pipeline only checks JSON syntax -- it does not validate namespaces (SCHEMA-02), check compatibility against SR (SCHEMA-01), or detect undocumented overrides (SCHEMA-03). The breaking change runbook (SCHEMA-04) provides teams with a step-by-step process for incompatible schema changes.

Output: Python validation script, bash override checker, enhanced CI workflow, updated schema guide.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-governance-foundation/01-CONTEXT.md
@.planning/phases/01-shared-governance-foundation/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->

From .github/workflows/terraform-plan.yml (current CI structure to enhance):
```yaml
name: Terraform Plan
on:
  pull_request:
    paths:
      - 'environments/**'
      - 'modules/**'
      - 'schemas/**'
jobs:
  lint:
    name: Lint & Validate
    steps:
      - Terraform Format Check
      - Validate Topic Naming Convention (grep-based)
      - Validate Avro Schemas (inline Python: JSON syntax + required fields only)
      - Terraform Init (-backend=false)
      - Terraform Validate
  plan:
    name: Terraform Plan
    needs: lint
    steps:
      - Terraform Init, Plan, Comment on PR
```

From schemas/examples/account-transaction.avsc (existing namespace pattern):
```json
{
  "type": "record",
  "name": "AccountTransaction",
  "namespace": "org.fsi.cncb.core.v1",
  "fields": [...]
}
```
NOTE: Existing namespaces use pattern `org.fsi.{domain}.{application}.{version}` -- the entity is the Avro record name, NOT part of the namespace. Per Research Pitfall 4, validate namespace starts with `org.fsi.` and remaining segments match `[a-z][a-z0-9-]*` with the last segment being `v[0-9]+`.

From docs/schema-guide.md (current structure -- append breaking change runbook):
```markdown
# Schema Governance Guide
## Topic Naming Convention
## Schema Format
## Compatibility Modes
## Schema Evolution Runbook
## PII Tagging
## Avro Best Practices
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema validation Python script and override checker</name>
  <files>ci/scripts/validate-schemas.py, ci/scripts/check-overrides.sh</files>
  <read_first>
    - .github/workflows/terraform-plan.yml (existing inline Python schema validation to replace, lines 54-69)
    - schemas/examples/account-transaction.avsc (reference namespace pattern: org.fsi.cncb.core.v1)
    - schemas/examples/fraud-alert-signal.avsc (verify namespace pattern consistency)
    - schemas/examples/compliance-match-result.avsc (verify namespace pattern consistency)
    - .planning/phases/01-shared-governance-foundation/01-RESEARCH.md (Pattern 3, Pitfall 4, Pitfall 6, Code Examples)
  </read_first>
  <action>
**Per D-08, D-09, D-10, D-11:** Create two CI scripts for schema governance enforcement.

**Create directory `ci/scripts/` and file `ci/scripts/validate-schemas.py`:**

The script must:
1. Accept `--schemas-dir` argument (default: `schemas/`) for the directory of `.avsc` files to validate
2. Accept `--check-compatibility` flag to enable SR compatibility checks (requires SR_ENDPOINT, SR_API_KEY, SR_API_SECRET env vars)
3. For each `.avsc` file found recursively in the schemas dir:
   a. **Structure validation:** Parse JSON, verify `type == "record"`, verify `namespace` exists, verify `fields` is a non-empty array, verify every field has `name` and `type` keys
   b. **Namespace validation (per D-09, corrected by Research Pitfall 4):** Validate namespace matches regex `^org\.fsi\.[a-z][a-z0-9-]*\.[a-z][a-z0-9-]*\.v[0-9]+$` -- the pattern is `org.fsi.{domain}.{application}.{version}`, NOT `org.fsi.{domain}.{application}.{entity}` (entity is the Avro record name)
   c. **Compatibility check (only when --check-compatibility flag present):** Call `POST {SR_ENDPOINT}/compatibility/subjects/{subject}/versions?verbose=true` with Basic auth (base64 of `{SR_API_KEY}:{SR_API_SECRET}`), Content-Type `application/vnd.schemaregistry.v1+json`, body `{"schema": "<json-escaped-schema>", "schemaType": "AVRO"}`. Derive subject name from namespace+name: extract domain.application.version.entity-name-lowered from the schema namespace (last 3 segments after org.fsi) plus the kebab-cased record name, then append `-value`. Handle: HTTP 404 = new subject (skip, no error), URLError = SR unreachable (print WARNING, do not fail), HTTP 200 with `is_compatible: false` = fail with message.
4. Use ONLY Python stdlib (`json`, `re`, `sys`, `os`, `urllib.request`, `urllib.error`, `base64`, `argparse`, `glob`) -- no pip dependencies needed.
5. Exit 0 if all schemas pass, exit 1 if any fail.
6. Print clear per-file results: `OK: schemas/examples/account-transaction.avsc (12 fields, namespace: org.fsi.cncb.core.v1)` or `FAIL: schemas/examples/bad.avsc: Invalid namespace 'com.wrong.ns'. Must match org.fsi.{domain}.{application}.v{N}`
7. Add `#!/usr/bin/env python3` shebang and make executable.

**Concrete namespace regex:**
```python
NAMESPACE_PATTERN = re.compile(r'^org\.fsi\.[a-z][a-z0-9-]*\.[a-z][a-z0-9-]*\.v[0-9]+$')
```

**Create file `ci/scripts/check-overrides.sh`:**

Per D-10 and Research Pitfall 6 (false positive avoidance):
1. Add `#!/bin/bash` shebang and `set -euo pipefail`
2. Read `PR_BODY` from environment variable (default empty string)
3. Get list of changed `.tf` files in `environments/` and `scenarios/` directories ONLY (exclude `modules/` to avoid false positives on the variable definition itself): `git diff origin/main --name-only -- 'environments/**/*.tf' 'scenarios/**/*.tf'`
4. For each changed file, grep for non-null compatibility_override assignments: regex `compatibility_override\s*=\s*"[^"]*"` (this matches actual usage like `compatibility_override = "FULL_TRANSITIVE"` but NOT `compatibility_override = null` or the variable declaration with `default = null`)
5. If overrides found: check PR_BODY for regex `(ADR-[0-9]+|[A-Z]{2,10}-[0-9]+)` (ADR or JIRA reference)
6. If override found but no reference: print error listing the files and exit 1
7. If override found with reference: print OK and exit 0
8. If no override found: print "No compatibility_override detected" and exit 0
9. Make executable.
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && python3 ci/scripts/validate-schemas.py --schemas-dir schemas/examples/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - ci/scripts/validate-schemas.py exists and starts with `#!/usr/bin/env python3`
    - ci/scripts/validate-schemas.py contains `NAMESPACE_PATTERN = re.compile(r'^org\.fsi\.[a-z][a-z0-9-]*\.[a-z][a-z0-9-]*\.v[0-9]+$')`
    - ci/scripts/validate-schemas.py contains `--schemas-dir`
    - ci/scripts/validate-schemas.py contains `--check-compatibility`
    - ci/scripts/validate-schemas.py contains `compatibility/subjects`
    - ci/scripts/validate-schemas.py contains `application/vnd.schemaregistry.v1+json`
    - ci/scripts/validate-schemas.py does NOT import `fastavro` or `requests` (stdlib only)
    - Running `python3 ci/scripts/validate-schemas.py --schemas-dir schemas/examples/` exits 0 and prints OK for each of the 7 existing schemas
    - ci/scripts/check-overrides.sh exists and starts with `#!/bin/bash`
    - ci/scripts/check-overrides.sh contains `set -euo pipefail`
    - ci/scripts/check-overrides.sh contains `environments/**/*.tf`
    - ci/scripts/check-overrides.sh contains `compatibility_override` grep pattern
    - ci/scripts/check-overrides.sh contains `ADR-[0-9]+` regex
    - ci/scripts/check-overrides.sh does NOT grep in `modules/` directory
  </acceptance_criteria>
  <done>Python schema validation script validates all 7 existing schemas (structure + namespace). Override checker script detects compatibility_override usage in scenario files only and requires ADR/JIRA reference in PR body.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance CI workflow and add breaking change runbook</name>
  <files>.github/workflows/terraform-plan.yml, docs/schema-guide.md</files>
  <read_first>
    - .github/workflows/terraform-plan.yml (full file -- understand existing job structure, steps, env vars)
    - docs/schema-guide.md (full file -- understand existing sections, append runbook at end)
    - ci/scripts/validate-schemas.py (just created -- verify the CLI interface for the workflow step)
    - ci/scripts/check-overrides.sh (just created -- verify the interface for the workflow step)
    - .planning/phases/01-shared-governance-foundation/01-RESEARCH.md (Pattern 3: Schema CI Validation Pipeline, Pitfall 2: SR access graceful degradation)
  </read_first>
  <action>
**Per D-08, D-12, D-13:** Enhance the CI workflow with schema validation steps and add the breaking change runbook to schema-guide.md.

**In .github/workflows/terraform-plan.yml:**
1. Replace the existing inline "Validate Avro Schemas" step (lines 53-69) with a call to the new Python script:
   ```yaml
   - name: Validate Avro Schemas
     run: python3 ci/scripts/validate-schemas.py --schemas-dir schemas/
   ```

2. Add a new step AFTER "Validate Avro Schemas" and BEFORE "Terraform Init" for Schema Registry compatibility checking. This step is separate because it requires secrets and should degrade gracefully if SR is unreachable:
   ```yaml
   - name: Check Schema Compatibility (SR)
     if: github.event_name == 'pull_request'
     env:
       SR_ENDPOINT: ${{ secrets.CC_SR_REST_ENDPOINT }}
       SR_API_KEY: ${{ secrets.SR_API_KEY }}
       SR_API_SECRET: ${{ secrets.SR_API_SECRET }}
     run: python3 ci/scripts/validate-schemas.py --schemas-dir schemas/ --check-compatibility
     continue-on-error: true
   ```
   Note: `continue-on-error: true` per Research Pitfall 2 -- SR outage should not block all PRs. The structure and namespace checks (previous step) always run and always block.

3. Add a new step AFTER "Check Schema Compatibility" for override detection:
   ```yaml
   - name: Check Compatibility Overrides
     if: github.event_name == 'pull_request'
     env:
       PR_BODY: ${{ github.event.pull_request.body }}
     run: bash ci/scripts/check-overrides.sh
   ```

4. Update the existing "Validate Topic Naming Convention" step to also check `scenarios/` paths (not just environments/prod-east). Replace the grep path from `environments/prod-east/*.tf` to:
   ```yaml
   run: |
     echo "Checking topic naming convention in Terraform files..."
     for dir in environments/*/  scenarios/*/; do
       if [ -d "$dir" ]; then
         grep -h 'domain\s*=' "$dir"*.tf 2>/dev/null \
           | sed 's/.*=\s*"\(.*\)"/\1/' \
           | while read -r domain; do
               if ! echo "$domain" | grep -qP '^[a-z][a-z0-9-]{1,30}$'; then
                 echo "ERROR: Invalid domain name: $domain (in $dir)"
                 exit 1
               fi
             done
       fi
     done
     echo "Naming convention check passed"
   ```

5. Add `ci/**` to the `paths` trigger list so changes to CI scripts trigger the pipeline:
   ```yaml
   on:
     pull_request:
       paths:
         - 'environments/**'
         - 'modules/**'
         - 'schemas/**'
         - 'ci/**'
   ```

**In docs/schema-guide.md:**
6. Append a new `## Breaking Change Runbook` section AFTER the existing `## Avro Best Practices` section (end of file). Per D-12 and D-13, this goes in the existing schema-guide.md, not a separate document. Content:

```markdown

## Breaking Change Runbook

When you must make an incompatible schema change (field removal, type change,
renaming) on a topic with FULL_TRANSITIVE or BACKWARD_TRANSITIVE compatibility,
follow this 5-step process. **Do not request a compatibility_override** unless
you have exhausted this process and documented the exception in an ADR.

### Step 1: Create Versioned Topic

Create a new topic with the next version using the same Terraform module:

```hcl
module "corebanking_account_txn_v2" {
  source      = "../../modules/topic"
  domain      = "corebanking"
  application = "transactions"
  version     = "v2"                    # Incremented from v1
  entity      = "account-transaction"
  owner       = "core-banking-team@company.com"
  sla_tier    = "critical"
  schema_file = "../../schemas/corebanking/account-transaction-v2.avsc"
  # ... remaining config identical to v1 ...
}
```

Both v1 and v2 topics coexist. v1 continues to operate normally.

### Step 2: Dual-Write Period

Deploy producers that write to BOTH v1 and v2 topics simultaneously.

- **Minimum duration:** 2 business days (allow consumer teams to plan and migrate)
- **Recommended:** 1 sprint (2 weeks) for critical-tier topics
- **Monitoring:** Verify v2 topic is receiving messages at expected throughput via consumer lag metrics

### Step 3: Consumer Migration

Each consumer team:

1. Updates their consumer to read from the v2 topic
2. Deploys to a staging environment and validates deserialization
3. Deploys to production
4. Confirms migration complete to the C4E team via the intake form or PR comment

**Track progress:** Maintain a migration checklist in the PR that adds the v2 topic:

- [ ] Consumer team A migrated (confirmed YYYY-MM-DD)
- [ ] Consumer team B migrated (confirmed YYYY-MM-DD)

### Step 4: Deprecate Old Topic

After ALL consumers confirm migration:

1. Stop producing to v1 topic (remove v1 from producer config)
2. Add `deprecated = true` tag to the v1 Terraform module metadata
3. Set a decommission date: retention period + 30 days buffer
4. Announce deprecation via team communication channel

### Step 5: Decommission

After the decommission date:

1. Verify v1 consumer lag is 0 (no active consumers): `confluent kafka topic consume --from-beginning` should show no new offsets
2. Remove v1 topic module from Terraform configuration
3. Run `terraform apply` to delete the topic and associated RBAC/schema resources
4. Soft-delete the v1 schema subject (optional -- preserves version history for audit):
   `confluent schema-registry subject delete --subject "{topic}-value" --permanent=false`
```

**Do NOT:**
- Remove the existing "Breaking change (new topic)" subsection under "Schema Evolution Runbook" -- it provides a brief reference. The new section provides the detailed runbook.
- Add the `## Breaking Change Runbook` section in the middle of the doc -- append at the end.
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && grep -c 'validate-schemas.py' .github/workflows/terraform-plan.yml && grep -c 'check-overrides.sh' .github/workflows/terraform-plan.yml && grep -c 'Breaking Change Runbook' docs/schema-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/terraform-plan.yml contains `python3 ci/scripts/validate-schemas.py --schemas-dir schemas/`
    - .github/workflows/terraform-plan.yml contains `python3 ci/scripts/validate-schemas.py --schemas-dir schemas/ --check-compatibility`
    - .github/workflows/terraform-plan.yml contains `continue-on-error: true` on the SR compatibility step
    - .github/workflows/terraform-plan.yml contains `bash ci/scripts/check-overrides.sh`
    - .github/workflows/terraform-plan.yml contains `PR_BODY: ${{ github.event.pull_request.body }}`
    - .github/workflows/terraform-plan.yml paths trigger includes `'ci/**'`
    - .github/workflows/terraform-plan.yml contains `CC_SR_REST_ENDPOINT` secret reference
    - docs/schema-guide.md contains `## Breaking Change Runbook`
    - docs/schema-guide.md contains `Step 1: Create Versioned Topic`
    - docs/schema-guide.md contains `Step 2: Dual-Write Period`
    - docs/schema-guide.md contains `Step 3: Consumer Migration`
    - docs/schema-guide.md contains `Step 4: Deprecate Old Topic`
    - docs/schema-guide.md contains `Step 5: Decommission`
    - docs/schema-guide.md still contains `## Schema Evolution Runbook` (existing section preserved)
    - docs/schema-guide.md still contains `## Avro Best Practices` (existing section preserved)
  </acceptance_criteria>
  <done>CI workflow enhanced with schema structure/namespace validation (always runs, always blocks), SR compatibility check (runs on PRs, degrades gracefully), and override detection (requires ADR/JIRA reference). Breaking change runbook added to schema-guide.md with 5-step process (create versioned topic, dual-write, consumer migration, deprecate, decommission).</done>
</task>

</tasks>

<verification>
1. `python3 ci/scripts/validate-schemas.py --schemas-dir schemas/examples/` exits 0 with OK for all 7 schemas
2. `grep -c 'validate-schemas.py' .github/workflows/terraform-plan.yml` returns >= 2 (structure check + compatibility check)
3. `grep -c 'check-overrides.sh' .github/workflows/terraform-plan.yml` returns >= 1
4. `grep -c 'Breaking Change Runbook' docs/schema-guide.md` returns >= 1
5. `grep -c 'Step 1: Create Versioned Topic' docs/schema-guide.md` returns >= 1
6. `grep -c 'continue-on-error' .github/workflows/terraform-plan.yml` returns >= 1
7. `bash ci/scripts/check-overrides.sh` exits 0 (no changes to detect in clean repo)
</verification>

<success_criteria>
- Schema validation pipeline catches namespace violations and reports clear errors per file
- Schema validation pipeline gracefully degrades when SR is unreachable (warn, not block)
- Override detection only checks scenario/environment files, not module definitions
- Override detection requires ADR/JIRA reference in PR body
- CI workflow triggers on changes to ci/, schemas/, modules/, and environments/ paths
- Schema guide contains complete 5-step breaking change runbook
- All 7 existing schemas pass validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-governance-foundation/01-02-SUMMARY.md`
</output>
