---
phase: 01-shared-governance-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/006-oauth-vs-api-keys.md
  - docs/adr/007-topic-naming.md
  - docs/adr/008-dr-tier-classification.md
autonomous: true
requirements:
  - GOV-01
  - GOV-02
  - GOV-03

must_haves:
  truths:
    - "ADR-006 recommends OAuth/OAUTHBEARER for CC with deployment-model-specific guidance"
    - "ADR-007 documents topic naming convention with regex per segment and dot separator rationale"
    - "ADR-008 maps SLA tiers to RPO/RTO targets with backend selection criteria"
    - "All three ADRs follow the existing template format (Context, Decision, Consequences)"
  artifacts:
    - path: "docs/adr/006-oauth-vs-api-keys.md"
      provides: "ADR for authentication decision across deployment models"
      contains: "OAUTHBEARER"
    - path: "docs/adr/007-topic-naming.md"
      provides: "ADR for topic naming convention rationale"
      contains: "domain}.{application}.{version}.{entity}"
    - path: "docs/adr/008-dr-tier-classification.md"
      provides: "ADR for DR tier classification with RPO/RTO targets"
      contains: "RPO"
  key_links:
    - from: "docs/adr/006-oauth-vs-api-keys.md"
      to: "docs/adr/000-template.md"
      via: "Follows template structure"
      pattern: "## Context"
    - from: "docs/adr/007-topic-naming.md"
      to: "modules/topic/variables.tf"
      via: "Documents the regex validation"
      pattern: "a-z][a-z0-9-]"
    - from: "docs/adr/008-dr-tier-classification.md"
      to: "modules/topic/main.tf"
      via: "Maps SLA tiers to DR behavior"
      pattern: "critical.*standard.*best-effort.*compliance"
---

<objective>
Write three Architecture Decision Records: ADR-006 (OAuth vs API Keys), ADR-007 (Topic Naming Rationale), and ADR-008 (DR Tier Classification).

Purpose: GOV-01, GOV-02, GOV-03 require documented architectural decisions with deployment-model-specific guidance before implementation proceeds. These ADRs govern authentication strategy, naming conventions, and DR behavior across all deployment models (CC, CFK, CP). They must exist before Phase 2+ scenarios are built so new scenarios implement consistent governance.

Output: Three ADR markdown files in docs/adr/ following the existing template.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-governance-foundation/01-CONTEXT.md
@.planning/phases/01-shared-governance-foundation/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing codebase. -->

From docs/adr/000-template.md (ADR structure):
```markdown
# ADR-NNN: [Title]

**Status:** Proposed | Accepted | Deprecated | Superseded
**Date:** YYYY-MM-DD
**Author:** [Name]

## Context
What is the issue? What forces are at play?

## Decision
What is the change we are making?

## Consequences
What becomes easier or harder because of this decision?
```

From docs/adr/ (existing ADR numbering -- continue sequentially):
- 000-template.md
- 001-avro-over-protobuf.md
- 002-compatibility-by-tier.md
- 003-consul-service-discovery.md
- 004-onprem-connect.md
- 005-cluster-linking-over-mrc.md

Next available: 006, 007, 008

From modules/topic/variables.tf (naming regex to document in ADR-007):
```hcl
variable "domain" {
  validation {
    condition = can(regex("^[a-z][a-z0-9-]{1,30}$", var.domain))
  }
}
variable "version" {
  validation {
    condition = can(regex("^v[0-9]+$", var.version))
  }
}
```

From modules/topic/main.tf (SLA tier maps to reference in ADR-008):
```hcl
compatibility_map = { critical = "FULL_TRANSITIVE", standard = "BACKWARD_TRANSITIVE", best-effort = "BACKWARD", compliance = "FULL_TRANSITIVE" }
retention_map = { critical = 604800000, standard = 259200000, best-effort = 86400000, compliance = -1 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-006 OAuth vs API Keys and ADR-007 Topic Naming</name>
  <files>docs/adr/006-oauth-vs-api-keys.md, docs/adr/007-topic-naming.md</files>
  <read_first>
    - docs/adr/000-template.md (template structure to follow)
    - docs/adr/001-avro-over-protobuf.md (example of existing ADR style and depth)
    - docs/adr/002-compatibility-by-tier.md (example referencing SLA tiers)
    - modules/topic/variables.tf (naming regex validation to document in ADR-007)
    - .planning/phases/01-shared-governance-foundation/01-CONTEXT.md (D-14, D-15 locked decisions)
    - .planning/phases/01-shared-governance-foundation/01-RESEARCH.md (Pattern 4: ADR Authoring Convention)
  </read_first>
  <action>
**ADR-006: OAuth vs API Keys (per D-14)**

Create `docs/adr/006-oauth-vs-api-keys.md` following the 000-template.md structure:

```markdown
# ADR-006: OAuth vs API Keys for Kafka Authentication

**Status:** Accepted
**Date:** 2026-03-21
**Author:** FSI C4E

## Context

The FSI Kafka Platform supports multiple deployment models (Confluent Cloud, CFK on OpenShift, CP on RHEL). Each requires authentication for producers, consumers, and administrative operations. Two primary authentication mechanisms are available:

1. **API Keys** (static credentials): Confluent Cloud service account API key/secret pairs. Simple to provision via Terraform. Require manual rotation. If leaked, remain valid until explicitly revoked.

2. **OAuth/OAUTHBEARER** (token-based): Short-lived tokens issued by an identity provider (IdP). Confluent Cloud supports OAUTHBEARER since 2023. Tokens auto-expire (typically 1 hour). Rotation is automatic via IdP token refresh. Requires IdP integration (Azure AD, AWS IAM, Okta).

Forces:
- FSI regulatory requirements mandate credential rotation policies (90-day maximum for static credentials)
- Zero-downtime credential rotation is operationally critical for 24/7 financial systems
- Different deployment models have different IdP ecosystems (Azure AD for Azure, AWS IAM for AWS, LDAP/AD for on-prem)
- API keys are simpler to set up initially but create operational burden for rotation
- OAuth requires IdP infrastructure but provides automatic rotation

## Decision

**Recommend OAuth/OAUTHBEARER as the primary authentication mechanism for Confluent Cloud deployments.** Retain API keys as the fallback for environments without IdP integration and for Terraform provider authentication (which does not support OAuth).

### Per Deployment Model

| Deployment Model | Primary Auth | Fallback | IdP |
|------------------|-------------|----------|-----|
| CC on Azure | OAUTHBEARER | API Keys | Azure AD (Entra ID) |
| CC on AWS | OAUTHBEARER | API Keys | AWS IAM Identity Center |
| CC on GCP | OAUTHBEARER | API Keys | Google Workspace / Cloud Identity |
| CFK on OpenShift | LDAP/AD via MDS | RBAC tokens | Active Directory |
| CP on RHEL | MDS with LDAP | SASL/PLAIN | Active Directory / LDAP |

### Credential Rotation Guidance

- **OAuth tokens:** Automatic. Configure token lifetime in IdP (recommended: 1 hour). Client libraries handle refresh.
- **API keys (when used):** Rotate via Vault integration. Create new key, update consumers (dual-credential window), revoke old key. Target: < 90 days per FSI policy.
- **Terraform provider credentials:** API keys only (Confluent Terraform provider does not support OAuth). Store in CI secrets (GitHub Actions) or Vault. Rotate quarterly.

## Consequences

**Easier:**
- Credential rotation is automatic for application workloads using OAuth
- Audit trail of authentication events via IdP logs
- Centralized identity management across Kafka and other services
- Revocation is instant (revoke IdP access, all tokens expire naturally)

**Harder:**
- Initial IdP setup requires identity team coordination
- Each cloud provider has different OAUTHBEARER configuration
- Terraform provider authentication remains API-key-based (separate rotation process)
- Local development may use API keys for simplicity (Docker Compose environments)
- Teams unfamiliar with OAuth need onboarding documentation
```

**ADR-007: Topic Naming Rationale (per D-15)**

Create `docs/adr/007-topic-naming.md` following the 000-template.md structure:

```markdown
# ADR-007: Topic Naming Convention

**Status:** Accepted
**Date:** 2026-03-21
**Author:** FSI C4E

## Context

Kafka topic names are permanent once created (renaming requires creating a new topic and migrating consumers). A consistent naming convention is essential for:

- **Discovery:** Teams can find topics by domain or application without a catalog
- **RBAC:** Role bindings can use wildcard patterns on topic prefixes (e.g., `corebanking.*`)
- **Observability:** Dashboard auto-discovery groups topics by domain prefix
- **Schema Registry:** Subject naming strategy (TopicNameStrategy) derives subject names from topic names
- **Governance:** Topic ownership, SLA tier, and data classification are encoded or derivable from the name

The convention must work across all deployment models (CC, CFK, CP) and all cloud providers.

## Decision

**Topic names follow the pattern: `{domain}.{application}.{version}.{entity}`**

### Segment Rules

| Segment | Regex | Example | Purpose |
|---------|-------|---------|---------|
| domain | `^[a-z][a-z0-9-]{1,30}$` | `corebanking` | Business domain ownership |
| application | `^[a-z][a-z0-9-]{1,30}$` | `transactions` | Application within domain |
| version | `^v[0-9]+$` | `v1` | Schema version (enables breaking change migration) |
| entity | `^[a-z][a-z0-9-]{1,60}$` | `account-transaction` | The data entity carried |

### Why Dots as Separators

- Dots (`.`) are the standard hierarchical separator in Kafka (used by Confluent for internal topics, metrics, consumer group IDs)
- Dots enable prefix-based RBAC: `DeveloperRead` on `corebanking.*` grants read to all corebanking topics
- Dots are visually distinct from hyphens within segments (e.g., `corebanking.transactions.v1.account-transaction` is unambiguous)
- Underscores (`_`) and dots are interchangeable in Kafka metric names, which can cause collisions. Using dots consistently avoids the `metrics_period_problem` where `a.b` and `a_b` map to the same metric.

### Why Version in Topic Name

- Schema compatibility modes (FULL_TRANSITIVE, BACKWARD_TRANSITIVE) prevent incompatible changes to existing topics
- When a breaking change is necessary, a new versioned topic (`v2`) is created alongside `v1`
- Both versions coexist during migration (see Breaking Change Runbook in schema-guide.md)
- The version segment makes dual-version operation explicit and discoverable

### Avro Namespace Convention

Avro schema namespaces follow: `org.fsi.{domain}.{application}.{version}`

The entity is NOT part of the namespace -- it is the Avro record name (e.g., `AccountTransaction`). This matches Java package conventions and allows multiple entity types within the same domain/application/version namespace.

### Examples

| Topic Name | Domain | Application | Version | Entity |
|------------|--------|-------------|---------|--------|
| `corebanking.transactions.v1.account-transaction` | corebanking | transactions | v1 | account-transaction |
| `fraud.detection.v1.alert-signal` | fraud | detection | v1 | alert-signal |
| `compliance.screening.v1.match-result` | compliance | screening | v1 | match-result |
| `corebanking.transactions.v2.account-transaction` | corebanking | transactions | v2 | account-transaction |

## Consequences

**Easier:**
- Topic discovery by domain prefix (`corebanking.*`)
- RBAC wildcard bindings per domain
- Dashboard auto-discovery by domain prefix pattern
- Breaking change migration via versioned topics
- Naming validation enforceable in Terraform module and CI

**Harder:**
- Long topic names (max ~61 chars in practice, well within Kafka 255 limit)
- Renaming a domain requires creating new topics (but this is rare and intentional)
- Teams must agree on domain boundaries upfront
- Version segment adds complexity for teams that never do breaking changes (but it costs nothing to include)
```
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && test -f docs/adr/006-oauth-vs-api-keys.md && test -f docs/adr/007-topic-naming.md && grep -c '## Context' docs/adr/006-oauth-vs-api-keys.md && grep -c '## Decision' docs/adr/006-oauth-vs-api-keys.md && grep -c '## Consequences' docs/adr/006-oauth-vs-api-keys.md && grep -c '## Context' docs/adr/007-topic-naming.md && grep -c '## Decision' docs/adr/007-topic-naming.md && grep -c '## Consequences' docs/adr/007-topic-naming.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/006-oauth-vs-api-keys.md exists
    - docs/adr/006-oauth-vs-api-keys.md contains `# ADR-006:`
    - docs/adr/006-oauth-vs-api-keys.md contains `**Status:** Accepted`
    - docs/adr/006-oauth-vs-api-keys.md contains `## Context`
    - docs/adr/006-oauth-vs-api-keys.md contains `## Decision`
    - docs/adr/006-oauth-vs-api-keys.md contains `## Consequences`
    - docs/adr/006-oauth-vs-api-keys.md contains `OAUTHBEARER`
    - docs/adr/006-oauth-vs-api-keys.md contains `Azure AD` or `Entra ID`
    - docs/adr/006-oauth-vs-api-keys.md contains `AWS IAM`
    - docs/adr/006-oauth-vs-api-keys.md contains `MDS` (for CFK/CP deployment models)
    - docs/adr/006-oauth-vs-api-keys.md contains `Credential Rotation`
    - docs/adr/007-topic-naming.md exists
    - docs/adr/007-topic-naming.md contains `# ADR-007:`
    - docs/adr/007-topic-naming.md contains `**Status:** Accepted`
    - docs/adr/007-topic-naming.md contains `## Context`
    - docs/adr/007-topic-naming.md contains `## Decision`
    - docs/adr/007-topic-naming.md contains `## Consequences`
    - docs/adr/007-topic-naming.md contains `{domain}.{application}.{version}.{entity}`
    - docs/adr/007-topic-naming.md contains `^[a-z][a-z0-9-]{1,30}$`
    - docs/adr/007-topic-naming.md contains `Why Dots as Separators`
    - docs/adr/007-topic-naming.md contains `Avro Namespace Convention`
    - docs/adr/007-topic-naming.md contains `org.fsi.{domain}.{application}.{version}`
  </acceptance_criteria>
  <done>ADR-006 documents OAuth recommendation for CC with deployment-model-specific table (CC: OAUTHBEARER, CFK: LDAP/MDS, CP: MDS/LDAP) and credential rotation guidance. ADR-007 documents topic naming convention with regex per segment, dot separator rationale, version rationale, and Avro namespace relationship.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-008 DR Tier Classification</name>
  <files>docs/adr/008-dr-tier-classification.md</files>
  <read_first>
    - docs/adr/000-template.md (template structure)
    - docs/adr/005-cluster-linking-over-mrc.md (existing DR decision to reference)
    - docs/adr/002-compatibility-by-tier.md (existing SLA tier decision to reference)
    - modules/topic/main.tf (SLA tier maps -- verify compliance tier values after Plan 01)
    - .planning/phases/01-shared-governance-foundation/01-CONTEXT.md (D-16 locked decision with specific RPO/RTO targets)
    - .planning/phases/01-shared-governance-foundation/01-RESEARCH.md (ADR Authoring Convention)
  </read_first>
  <action>
**Per D-16:** Create ADR-008 documenting the DR tier classification with RPO/RTO targets per SLA tier.

Create `docs/adr/008-dr-tier-classification.md` following the 000-template.md structure:

```markdown
# ADR-008: DR Tier Classification

**Status:** Accepted
**Date:** 2026-03-21
**Author:** FSI C4E

## Context

The FSI Kafka Platform operates across multiple SLA tiers (critical, standard, best-effort, compliance). Each tier represents different business criticality and regulatory requirements. Disaster Recovery (DR) capabilities must be proportional to tier criticality:

- **Critical** topics (core banking transactions, fraud alerts) require near-zero data loss and rapid recovery
- **Standard** topics (internal events, batch feeds) tolerate moderate data loss and recovery time
- **Best-effort** topics (logs, metrics, dev/test data) tolerate significant data loss and recovery time
- **Compliance** topics (OFAC screening, AML alerts, CFT reporting) require zero data loss for regulatory audit trails

Different DR backends provide different RPO/RTO characteristics:
- **Cluster Linking** (CC): Asynchronous replication, RPO bounded by mirror lag (typically seconds), manual promotion
- **MirrorMaker 2** (CFK/CP): Asynchronous replication, similar RPO characteristics, more operational overhead
- **Multi-Region Clusters (MRC)** with observer promotion (CP): Synchronous replication, RPO=0, higher latency cost

The platform must define clear RPO/RTO targets per tier and map them to appropriate DR backends per deployment model.

## Decision

### RPO/RTO Targets by SLA Tier

| SLA Tier | RPO Target | RTO Target | DR Priority | Mirror Lag Alert |
|----------|-----------|-----------|-------------|------------------|
| critical | < 5 minutes | < 15 minutes | P1 — immediate failover | Warn at 30s, alert at 60s |
| standard | < 2 hours | < 1 hour | P2 — business hours failover | Warn at 5m, alert at 15m |
| best-effort | < 24 hours | < 4 hours | P3 — next business day | Warn at 1h, alert at 4h |
| compliance | RPO = 0 (zero data loss) | < 15 minutes | P1 — immediate failover | Warn at 10s, alert at 30s |

### DR Backend Selection by Deployment Model

| Deployment Model | DR Backend | RPO Achievable | Notes |
|------------------|-----------|----------------|-------|
| CC (any cloud) | Cluster Linking | seconds (async) | Bidirectional link, mirror topic promotion. See ADR-005. |
| CFK on OpenShift | MirrorMaker 2 | seconds (async) | MM2 connector-based replication, topic renaming configurable. |
| CP on RHEL | Cluster Linking or MRC | seconds (CL) or 0 (MRC) | MRC with observer promotion for compliance tier RPO=0. |

### Compliance Tier: RPO=0 Strategy

For compliance-tier topics requiring RPO=0:
- **CP on RHEL:** Use Multi-Region Clusters (MRC) with automatic observer promotion (2.5-cluster pattern). The observer replica in the DR region is synchronously replicated and can be promoted to leader with zero data loss.
- **CC (any cloud):** Cluster Linking provides near-zero RPO (bounded by mirror lag, typically <1s). True RPO=0 is not available on CC without MRC. Compliance topics on CC should use critical-tier DR with enhanced mirror lag monitoring (alert at 30s).
- **CFK on OpenShift:** Same as CC — MM2 provides async replication. Compliance topics should use critical-tier DR with enhanced monitoring.

### Mirror Lag Monitoring Integration

Mirror lag thresholds defined above feed into the observability templates (Phase 5). The `fsi-dr` CLI (Phase 4) uses these thresholds for pre-failover health checks:
- If mirror lag exceeds alert threshold, `fsi-dr failover` warns the operator about potential data loss
- `fsi-dr failover --dry-run` reports current mirror lag per topic with tier-specific assessment

## Consequences

**Easier:**
- Clear SLA expectations for each topic tier
- Mirror lag alert thresholds are derived, not manually configured per topic
- DR backend selection is deterministic per deployment model
- Compliance teams have documented RPO=0 strategy for regulatory audits
- Failover priority ordering (P1 before P2 before P3) enables triage during multi-topic outages

**Harder:**
- Compliance tier RPO=0 on CC requires accepting near-zero (not absolute zero) — may need regulatory approval
- MRC for compliance tier on CP adds latency to produce calls (synchronous replication)
- Different DR backends per deployment model means the `fsi-dr` CLI must abstract backend differences (see DR-03)
- Mirror lag alerting requires observability platform integration per provider (Phase 5)
- Teams must correctly classify their topics — under-classification (standard instead of critical) risks inadequate DR

**References:**
- ADR-002: Compatibility by SLA tier (defines the tier enum)
- ADR-005: Cluster Linking over MRC for CC deployments (DR backend for CC)
- Phase 4: DR Automation Framework (implements `fsi-dr` CLI)
- Phase 5: Observability Templates (implements mirror lag dashboards and alerts)
```
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && test -f docs/adr/008-dr-tier-classification.md && grep -c '## Context' docs/adr/008-dr-tier-classification.md && grep -c '## Decision' docs/adr/008-dr-tier-classification.md && grep -c '## Consequences' docs/adr/008-dr-tier-classification.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/008-dr-tier-classification.md exists
    - docs/adr/008-dr-tier-classification.md contains `# ADR-008:`
    - docs/adr/008-dr-tier-classification.md contains `**Status:** Accepted`
    - docs/adr/008-dr-tier-classification.md contains `## Context`
    - docs/adr/008-dr-tier-classification.md contains `## Decision`
    - docs/adr/008-dr-tier-classification.md contains `## Consequences`
    - docs/adr/008-dr-tier-classification.md contains `RPO`
    - docs/adr/008-dr-tier-classification.md contains `RTO`
    - docs/adr/008-dr-tier-classification.md contains `< 5 minutes` (critical RPO)
    - docs/adr/008-dr-tier-classification.md contains `< 15 minutes` (critical RTO)
    - docs/adr/008-dr-tier-classification.md contains `< 2 hours` (standard RPO)
    - docs/adr/008-dr-tier-classification.md contains `< 24 hours` (best-effort RPO)
    - docs/adr/008-dr-tier-classification.md contains `RPO = 0` (compliance RPO)
    - docs/adr/008-dr-tier-classification.md contains `Cluster Linking`
    - docs/adr/008-dr-tier-classification.md contains `MirrorMaker 2`
    - docs/adr/008-dr-tier-classification.md contains `MRC` or `Multi-Region Clusters`
    - docs/adr/008-dr-tier-classification.md contains `compliance` tier reference
    - docs/adr/008-dr-tier-classification.md contains `ADR-005` reference
  </acceptance_criteria>
  <done>ADR-008 documents DR tier classification with RPO/RTO targets per SLA tier (critical: <5min/<15min, standard: <2h/<1h, best-effort: <24h/<4h, compliance: RPO=0/<15min), DR backend selection per deployment model (CC: Cluster Linking, CFK: MM2, CP: CL or MRC), compliance RPO=0 strategy via MRC, and mirror lag monitoring thresholds per tier.</done>
</task>

</tasks>

<verification>
1. `test -f docs/adr/006-oauth-vs-api-keys.md` exits 0
2. `test -f docs/adr/007-topic-naming.md` exits 0
3. `test -f docs/adr/008-dr-tier-classification.md` exits 0
4. All three ADRs contain `## Context`, `## Decision`, `## Consequences`
5. ADR-006 contains `OAUTHBEARER` and deployment model table
6. ADR-007 contains naming regex and dot separator rationale
7. ADR-008 contains RPO/RTO table with all 4 SLA tiers
8. ADR numbering is sequential (006, 007, 008) continuing from existing 005
</verification>

<success_criteria>
- Three ADRs exist at docs/adr/006-*, 007-*, 008-* following the 000-template.md structure
- ADR-006 recommends OAuth for CC with fallback to API keys, includes per-deployment-model guidance table, covers credential rotation
- ADR-007 documents topic naming with regex per segment, dot separator rationale, version rationale, and Avro namespace relationship
- ADR-008 maps all 4 SLA tiers to RPO/RTO targets, selects DR backend per deployment model, addresses compliance RPO=0 via MRC
- All ADRs reference relevant existing ADRs (002, 005) where applicable
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-governance-foundation/01-03-SUMMARY.md`
</output>
