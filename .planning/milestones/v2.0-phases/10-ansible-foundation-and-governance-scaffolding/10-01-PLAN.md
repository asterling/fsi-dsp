---
phase: 10-ansible-foundation-and-governance-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/ansible.cfg
  - ansible/requirements.yml
  - ansible/.ansible-lint
  - ansible/inventories/dev/hosts.yml
  - ansible/inventories/dev/group_vars/all.yml
  - ansible/inventories/staging/hosts.yml
  - ansible/inventories/staging/group_vars/all.yml
  - ansible/inventories/prod/hosts.yml
  - ansible/inventories/prod/group_vars/all.yml
  - ansible/inventories/prod/group_vars/kafka_broker.yml
  - ansible/inventories/prod/group_vars/schema_registry.yml
  - ansible/inventories/prod/group_vars/kafka_connect.yml
  - ansible/inventories/dr/hosts.yml
  - ansible/inventories/dr/group_vars/all.yml
  - ansible/playbooks/.gitkeep
  - ansible/roles/.gitkeep
autonomous: true
requirements:
  - AFOUND-01
  - AFOUND-05
must_haves:
  truths:
    - "Running ansible-galaxy collection install -r ansible/requirements.yml installs cp-ansible 7.7.x and all required collections with pinned versions"
    - "ansible-lint with the project config passes on the scaffolded directory with zero violations"
    - "Inventory skeletons exist for dev, staging, prod, and dr environments with documented host group patterns"
  artifacts:
    - path: "ansible/requirements.yml"
      provides: "Pinned collection dependencies"
      contains: "confluent.platform"
    - path: "ansible/ansible.cfg"
      provides: "Project-scoped Ansible configuration"
      contains: "filter_plugins"
    - path: "ansible/.ansible-lint"
      provides: "Lint configuration with shared profile and FQCN enforcement"
      contains: "profile: shared"
    - path: "ansible/inventories/prod/hosts.yml"
      provides: "Production inventory skeleton"
      contains: "kafka_broker"
    - path: "ansible/inventories/dev/hosts.yml"
      provides: "Dev inventory skeleton"
      contains: "kafka_broker"
    - path: "ansible/inventories/staging/hosts.yml"
      provides: "Staging inventory skeleton"
      contains: "kafka_broker"
    - path: "ansible/inventories/dr/hosts.yml"
      provides: "DR inventory skeleton"
      contains: "kafka_broker"
  key_links:
    - from: "ansible/ansible.cfg"
      to: "ansible/filter_plugins/"
      via: "filter_plugins path config"
      pattern: "filter_plugins.*=.*./filter_plugins"
    - from: "ansible/.ansible-lint"
      to: "ansible/"
      via: "lint target directory"
      pattern: "profile: shared"
---

<objective>
Scaffold the ansible/ directory structure with pinned dependencies, project-scoped ansible.cfg, multi-environment inventory skeletons, and ansible-lint configuration.

Purpose: Establish the structural foundation that every subsequent Ansible role and playbook (Phases 11-15) depends on. Without correct directory layout, filter_plugins path, and inventory patterns, no downstream work can function.

Output: ansible/ directory with requirements.yml, ansible.cfg, .ansible-lint, and 4-environment inventory skeletons (dev/staging/prod/dr).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ansible-foundation-and-governance-scaffolding/10-RESEARCH.md

@scenarios/cp-rhel/inventory/hosts.yml.example
@scenarios/cp-rhel/inventory/group_vars/all.yml
@scenarios/cp-rhel/playbooks/deploy-cp.yml
</context>

<interfaces>
<!-- Existing patterns from codebase that executor must replicate -->

From scenarios/cp-rhel/inventory/hosts.yml.example:
```yaml
all:
  vars:
    ansible_connection: ssh
    ansible_user: confluent
    ansible_become: true
    confluent_server_enabled: true
    confluent_package_version: "7.6.0"
  children:
    kafka_broker:
      hosts: ...
    schema_registry:
      hosts: ...
    kafka_connect:
      hosts: ...
    flink_jobmanager:
      hosts: ...
    flink_taskmanager:
      hosts: ...
```

From scenarios/cp-rhel/inventory/group_vars/all.yml (Vault reference pattern):
```yaml
mds_super_user_password: "{{ vault_mds_password }}"
kafka_broker_ldap_password: "{{ vault_kafka_broker_password }}"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ansible/ directory structure, requirements.yml, and ansible.cfg</name>
  <files>
    ansible/ansible.cfg,
    ansible/requirements.yml,
    ansible/playbooks/.gitkeep,
    ansible/roles/.gitkeep
  </files>
  <read_first>
    scenarios/cp-rhel/inventory/hosts.yml.example,
    scenarios/cp-rhel/playbooks/deploy-cp.yml,
    scenarios/cp-rhel/inventory/group_vars/all.yml
  </read_first>
  <action>
Create the `ansible/` directory at the repo root with the following files:

**ansible/requirements.yml** (include `# yaml-doc` as first line instead of bare `---`):
Collections to pin (each as a `- name: / version:` entry under `collections:`):
1. `confluent.platform` version `"7.7.8"` -- CP 7.7.x deployment roles
2. `community.general` version `">=8.0.0"` -- json_query, ini_file, etc.
3. `ansible.posix` version `">=1.5.0"` -- sysctl, selinux, synchronize
4. `ansible.utils` version `">=2.10.0"` -- ipaddr filter, validate

**ansible/ansible.cfg** (INI format):

Section `[defaults]`:
- `collections_path = ./collections:~/.ansible/collections`
- `filter_plugins = ./filter_plugins`
- `inventory = ./inventories/dev`
- `deprecation_warnings = False`
- `retry_files_enabled = False`
- `host_key_checking = False`
- `timeout = 30`

Include a header comment block explaining this is the project-scoped Ansible config. Note that cp-ansible 7.7.x may emit a version warning with ansible-core 2.15+ which is cosmetic and safe to ignore.

Section `[inventory]`:
- `enable_plugins = yaml, ini`

Section `[privilege_escalation]`:
- `become = true`
- `become_method = sudo`

**Placeholder directories:**
- Create `ansible/playbooks/.gitkeep` (empty file)
- Create `ansible/roles/.gitkeep` (empty file)
  </action>
  <verify>
    <automated>test -f ansible/requirements.yml && test -f ansible/ansible.cfg && test -f ansible/playbooks/.gitkeep && test -f ansible/roles/.gitkeep && grep -q "confluent.platform" ansible/requirements.yml && grep -q "7.7.8" ansible/requirements.yml && grep -q "filter_plugins = ./filter_plugins" ansible/ansible.cfg && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ansible/requirements.yml contains `name: confluent.platform` with `version: "7.7.8"`
    - ansible/requirements.yml contains `name: community.general` with `version: ">=8.0.0"`
    - ansible/requirements.yml contains `name: ansible.posix` with `version: ">=1.5.0"`
    - ansible/requirements.yml contains `name: ansible.utils` with `version: ">=2.10.0"`
    - ansible/ansible.cfg contains `filter_plugins = ./filter_plugins`
    - ansible/ansible.cfg contains `collections_path = ./collections:~/.ansible/collections`
    - ansible/ansible.cfg contains `inventory = ./inventories/dev`
    - ansible/ansible.cfg contains `enable_plugins = yaml, ini`
    - ansible/ansible.cfg contains `become = true`
    - ansible/playbooks/.gitkeep exists (empty file)
    - ansible/roles/.gitkeep exists (empty file)
  </acceptance_criteria>
  <done>requirements.yml pins cp-ansible 7.7.8 and 3 supporting collections; ansible.cfg sets filter_plugins path, default inventory, and SSH defaults; placeholder directories exist for playbooks and roles</done>
</task>

<task type="auto">
  <name>Task 2: Create multi-environment inventory skeletons and ansible-lint config</name>
  <files>
    ansible/inventories/dev/hosts.yml,
    ansible/inventories/dev/group_vars/all.yml,
    ansible/inventories/staging/hosts.yml,
    ansible/inventories/staging/group_vars/all.yml,
    ansible/inventories/prod/hosts.yml,
    ansible/inventories/prod/group_vars/all.yml,
    ansible/inventories/prod/group_vars/kafka_broker.yml,
    ansible/inventories/prod/group_vars/schema_registry.yml,
    ansible/inventories/prod/group_vars/kafka_connect.yml,
    ansible/inventories/dr/hosts.yml,
    ansible/inventories/dr/group_vars/all.yml,
    ansible/.ansible-lint
  </files>
  <read_first>
    scenarios/cp-rhel/inventory/hosts.yml.example,
    scenarios/cp-rhel/inventory/group_vars/all.yml,
    scenarios/cp-rhel/inventory/group_vars/kafka_broker.yml,
    scenarios/cp-rhel/inventory/group_vars/schema_registry.yml,
    scenarios/cp-rhel/inventory/group_vars/kafka_connect.yml,
    ansible/ansible.cfg
  </read_first>
  <action>
Create inventory skeletons for all 4 environments and the ansible-lint config.

**Inventory Structure (all 4 envs):**
Each environment gets `hosts.yml` and `group_vars/all.yml`. Production additionally gets per-component group_vars files. All inventories use identical host group names: `kafka_broker`, `schema_registry`, `kafka_connect`, `flink_jobmanager`, `flink_taskmanager`. Use `confluent_package_version: "7.7.0"` in all inventories (not 7.6.0 from the old cp-rhel example).

**ansible/inventories/dev/hosts.yml:**
Header comment: "Development Environment Inventory" with usage example. Under `all:`, set vars: `ansible_connection: ssh`, `ansible_user: confluent`, `ansible_become: true`, `confluent_server_enabled: true`, `confluent_package_version: "7.7.0"`. Under `children:`, create minimal single-node groups:
- `kafka_broker` with 1 host: `kafka-dev-1` (`ansible_host: <IP_ADDRESS>`, `kafka_broker_custom_properties: {broker.id: 1}`)
- `schema_registry` with 1 host: `sr-dev-1`
- `kafka_connect` with 1 host: `connect-dev-1`
- `flink_jobmanager` with 1 host: `flink-jm-dev-1`
- `flink_taskmanager` with 1 host: `flink-tm-dev-1`

**ansible/inventories/staging/hosts.yml:**
Header: "Staging Environment Inventory -- production-like topology". Same structure but with staging hostnames and 3 brokers (multi-rack): `kafka-staging-{1,2,3}` with `broker.rack: <AVAILABILITY_ZONE>`, `sr-staging-{1,2}`, `connect-staging-{1,2}`, `flink-jm-staging-1`, `flink-tm-staging-{1,2}`.

**ansible/inventories/prod/hosts.yml:**
Header: "Production Environment Inventory". Full topology: 3 brokers (`kafka-prod-{1,2,3}`) with `broker.id` and `broker.rack: <AVAILABILITY_ZONE>`, 2 SR nodes (`sr-prod-{1,2}`), 2 Connect workers (`connect-prod-{1,2}`), 1 Flink JM (`flink-jm-prod-1`), 2 Flink TMs (`flink-tm-prod-{1,2}`). All with `<IP_ADDRESS>` placeholders.

**ansible/inventories/dr/hosts.yml:**
Header: "DR (Passive) Region Inventory -- mirrors production configuration. Used during failover/failback operations." Same topology as prod but with `dr` hostnames: `kafka-dr-{1,2,3}`, `sr-dr-{1,2}`, `connect-dr-{1,2}`, `flink-jm-dr-1`, `flink-tm-dr-{1,2}`.

**ansible/inventories/dev/group_vars/all.yml:**
Header: "Dev Environment -- Common Variables". Settings: `confluent_server_enabled: true`, `confluent_package_version: "7.7.0"`, `ssl_enabled: false`, `ssl_mutual_auth_enabled: false`, `rbac_enabled: false`, `jmxexporter_enabled: true`, `fips_enabled: false`. Include comment about Vault references for sensitive values.

**ansible/inventories/staging/group_vars/all.yml:**
Header: "Staging Environment -- production-like security". Settings: `ssl_enabled: true`, `ssl_mutual_auth_enabled: true`, `rbac_enabled: true`, `mds_super_user: mds`, `mds_super_user_password: "{{ vault_mds_password }}"`, `kafka_broker_ldap_user: kafka_broker`, `kafka_broker_ldap_password: "{{ vault_kafka_broker_password }}"`, `schema_registry_ldap_user: schema_registry`, `schema_registry_ldap_password: "{{ vault_sr_password }}"`, `kafka_connect_ldap_user: connect_worker`, `kafka_connect_ldap_password: "{{ vault_connect_password }}"`, `jmxexporter_enabled: true`, `fips_enabled: false`.

**ansible/inventories/prod/group_vars/all.yml:**
Full production config following `scenarios/cp-rhel/inventory/group_vars/all.yml` pattern exactly: `ssl_enabled: true`, `ssl_mutual_auth_enabled: true`, `rbac_enabled: true`, all MDS/LDAP credentials via `{{ vault_* }}` references. Include FIPS toggle (`fips_enabled: false` with comment "# Set true for FIPS 140-2 compliant clusters"), `jmxexporter_enabled: true`. Include commented-out LDAP config placeholders (`mds_ldap_search_base`, `mds_ldap_server_url`, etc.) matching the cp-rhel pattern.

**ansible/inventories/prod/group_vars/kafka_broker.yml:**
Header: "Prod -- Kafka Broker Overrides". Settings under `kafka_broker_custom_properties:`: `log.retention.hours: 168`, `num.io.threads: 16`, `num.network.threads: 8`, `log.segment.bytes: 1073741824`. Comment for additional tuning.

**ansible/inventories/prod/group_vars/schema_registry.yml:**
Header: "Prod -- Schema Registry Overrides". Settings under `schema_registry_custom_properties:` with commented-out example: `# schema.compatibility.level: BACKWARD  # Set per-subject via governance roles`.

**ansible/inventories/prod/group_vars/kafka_connect.yml:**
Header: "Prod -- Kafka Connect Overrides". Settings under `kafka_connect_custom_properties:`: `config.storage.replication.factor: 3`, `offset.storage.replication.factor: 3`, `status.storage.replication.factor: 3`. Comment for plugin path.

**ansible/inventories/dr/group_vars/all.yml:**
Same content as prod/group_vars/all.yml but with header: "DR (Passive) Region -- mirrors production configuration. Used during failover/failback operations."

**ansible/.ansible-lint:**
YAML file with these settings:
- `profile: shared`
- `enable_list:` containing `- fqcn` (FQCN not in shared profile, required by AFOUND-05)
- `exclude_paths:` containing `- collections/` and `- .cache/`
- `mock_roles:` list containing: `confluent.platform.kafka_broker`, `confluent.platform.schema_registry`, `confluent.platform.kafka_connect`, `confluent.platform.zookeeper`, `confluent.platform.kafka_rest`, `confluent.platform.ksql`, `confluent.platform.control_center`
- `var_naming_pattern: "^[a-z_][a-z0-9_]*$"`
- `offline: true`
Header comment: "FSI Kafka Platform -- ansible-lint Configuration" with note about profile choice and FQCN addition.

**ansible-lint validation (AFOUND-05):**
After creating all files, install ansible-lint if not already installed and run it to confirm zero violations:
```bash
pip install ansible-lint --quiet 2>/dev/null
cd /Users/jhogan/fsi-kafka-platform/ansible && ansible-lint --offline
```
Note: First-time `pip install ansible-lint` may take up to 60 seconds. The `--offline` flag prevents network calls during linting.
  </action>
  <verify>
    <automated>test -f ansible/inventories/dev/hosts.yml && test -f ansible/inventories/staging/hosts.yml && test -f ansible/inventories/prod/hosts.yml && test -f ansible/inventories/dr/hosts.yml && test -f ansible/inventories/prod/group_vars/kafka_broker.yml && test -f ansible/.ansible-lint && grep -q "kafka_broker" ansible/inventories/prod/hosts.yml && grep -q "profile: shared" ansible/.ansible-lint && grep -q "fqcn" ansible/.ansible-lint && grep -q "vault_" ansible/inventories/prod/group_vars/all.yml && pip install ansible-lint --quiet 2>/dev/null && cd /Users/jhogan/fsi-kafka-platform/ansible && ansible-lint --offline 2>&1 | tail -5 && echo "LINT_PASS" || echo "LINT_FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ansible/inventories/dev/hosts.yml contains host groups: kafka_broker, schema_registry, kafka_connect, flink_jobmanager, flink_taskmanager
    - ansible/inventories/staging/hosts.yml contains host groups: kafka_broker, schema_registry, kafka_connect, flink_jobmanager, flink_taskmanager
    - ansible/inventories/prod/hosts.yml contains host groups: kafka_broker, schema_registry, kafka_connect, flink_jobmanager, flink_taskmanager
    - ansible/inventories/dr/hosts.yml contains host groups: kafka_broker, schema_registry, kafka_connect, flink_jobmanager, flink_taskmanager
    - ansible/inventories/prod/hosts.yml contains 3 broker hosts (kafka-prod-1, kafka-prod-2, kafka-prod-3)
    - ansible/inventories/prod/group_vars/all.yml contains `vault_mds_password`
    - ansible/inventories/prod/group_vars/all.yml contains `ssl_enabled: true`
    - ansible/inventories/prod/group_vars/all.yml contains `rbac_enabled: true`
    - ansible/inventories/dev/group_vars/all.yml contains `ssl_enabled: false`
    - ansible/inventories/prod/group_vars/kafka_broker.yml exists with `kafka_broker_custom_properties`
    - ansible/inventories/prod/group_vars/schema_registry.yml exists with `schema_registry_custom_properties`
    - ansible/inventories/prod/group_vars/kafka_connect.yml exists with `config.storage.replication.factor: 3`
    - ansible/.ansible-lint contains `profile: shared`
    - ansible/.ansible-lint contains `fqcn` in enable_list
    - ansible/.ansible-lint contains `offline: true`
    - ansible/.ansible-lint contains `confluent.platform.kafka_broker` in mock_roles
    - ansible-lint --offline exits 0 with zero violations
  </acceptance_criteria>
  <done>4 environment inventory skeletons (dev/staging/prod/dr) with consistent host group names matching cp-rhel pattern; prod has per-component group_vars; all sensitive values use vault references; .ansible-lint configured with shared profile + FQCN enforcement; ansible-lint passes with zero violations on scaffolded ansible/ directory</done>
</task>

</tasks>

<verification>
- `test -f ansible/requirements.yml` -- requirements file exists
- `grep -c "name:" ansible/requirements.yml` returns 4 (4 collections)
- `grep "7.7.8" ansible/requirements.yml` -- cp-ansible pinned to 7.7.8
- `test -f ansible/ansible.cfg` -- config file exists
- `ls ansible/inventories/*/hosts.yml | wc -l` returns 4 (dev/staging/prod/dr)
- `grep -q "profile: shared" ansible/.ansible-lint` -- lint uses shared profile
- `grep -q "fqcn" ansible/.ansible-lint` -- FQCN enforced
- `grep -rq "vault_" ansible/inventories/prod/group_vars/all.yml` -- secrets use Vault refs
- `pip install ansible-lint --quiet 2>/dev/null && cd ansible && ansible-lint --offline` -- zero violations
</verification>

<success_criteria>
- requirements.yml installs cp-ansible 7.7.8 plus community.general, ansible.posix, ansible.utils
- ansible.cfg points filter_plugins to ./filter_plugins and default inventory to ./inventories/dev
- Inventory skeletons for dev, staging, prod, dr all share identical host group names (kafka_broker, schema_registry, kafka_connect, flink_jobmanager, flink_taskmanager)
- Production inventory has per-component group_vars with Vault references for secrets
- .ansible-lint uses shared profile with FQCN enforcement enabled
- ansible-lint --offline passes with zero violations on scaffolded ansible/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/10-ansible-foundation-and-governance-scaffolding/10-01-SUMMARY.md`
</output>
