---
phase: 12-orchestration-pipeline-observability-and-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/site.yml
  - ansible/playbooks/deploy-governance.yml
  - ansible/vars/connectors-example.yml
  - tests/ansible/test_orchestration.py
  - tests/ansible/test_cp_connect.py
  - tests/ansible/fixtures/mock_responses/connect/connectors_list.json
  - tests/ansible/fixtures/mock_responses/connect/connector_status_running.json
  - tests/ansible/fixtures/mock_responses/connect/connector_status_failed.json
  - tests/ansible/fixtures/mock_responses/connect/connector_created.json
  - ansible/roles/cp_connect/defaults/main.yml
  - ansible/roles/cp_connect/meta/main.yml
  - ansible/roles/cp_connect/tasks/main.yml
  - ansible/roles/cp_connect/tasks/deploy.yml
  - ansible/roles/cp_connect/tasks/validate.yml
  - ansible/roles/cp_connect/tasks/check.yml
  - ansible/roles/cp_connect/molecule/default/molecule.yml
  - ansible/roles/cp_connect/molecule/default/converge.yml
  - ansible/roles/cp_connect/molecule/default/verify.yml
autonomous: true
requirements:
  - APIPE-01
  - APIPE-02
  - APIPE-03
  - APIPE-04
  - ACI-02

must_haves:
  truths:
    - "Operator runs ansible-playbook site.yml and it chains cluster deploy -> topics -> schemas -> RBAC -> connectors -> observability"
    - "Operator runs ansible-playbook site.yml --tags topics and only topic tasks execute -- no cluster deploy leakage from import_playbook tag inheritance"
    - "Connectors are created or updated via PUT idempotently -- re-running produces no changes if config unchanged"
    - "After connector deployment, health validation checks all connectors are RUNNING with retry on FAILED"
  artifacts:
    - path: "ansible/site.yml"
      provides: "Top-level orchestration playbook"
      contains: "import_playbook"
    - path: "ansible/playbooks/deploy-governance.yml"
      provides: "Governance-only playbook for Day-2"
      contains: "cp_topic"
    - path: "ansible/roles/cp_connect/tasks/main.yml"
      provides: "Connector lifecycle entry point"
      contains: "ansible.builtin.include_tasks"
    - path: "ansible/roles/cp_connect/tasks/deploy.yml"
      provides: "PUT /connectors/{name}/config idempotent create-or-update"
      contains: "method: PUT"
    - path: "ansible/roles/cp_connect/tasks/validate.yml"
      provides: "Health validation with retry"
      contains: "retries:"
  key_links:
    - from: "ansible/site.yml"
      to: "ansible/roles/cp_topic, cp_schema, cp_rbac, cp_connect"
      via: "ansible.builtin.include_role"
      pattern: "include_role"
    - from: "ansible/roles/cp_connect/tasks/deploy.yml"
      to: "Connect REST API"
      via: "ansible.builtin.uri PUT"
      pattern: "connectors.*config"
---

<objective>
Create the orchestration playbooks (site.yml, deploy-governance.yml) with tag-based selective execution and the cp_connect role for Kafka Connect connector lifecycle management via REST API.

Purpose: Compose Phase 11 governance roles into a runnable pipeline and add connector management with idempotent PUT and health validation.
Output: site.yml, deploy-governance.yml, cp_connect role with molecule tests, pytest unit tests, Connect mock fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-orchestration-pipeline-observability-and-ci-cd/12-RESEARCH.md

@ansible/roles/cp_topic/tasks/main.yml
@ansible/roles/cp_topic/defaults/main.yml
@ansible/roles/cp_topic/meta/main.yml
@ansible/roles/cp_topic/molecule/default/molecule.yml
@ansible/roles/cp_topic/molecule/default/converge.yml
@ansible/roles/cp_topic/molecule/default/verify.yml
@ansible/roles/cp_schema/tasks/main.yml
@ansible/roles/cp_rbac/tasks/main.yml
@ansible/filter_plugins/fsi_governance.py
@ansible/vars/sla_tiers.yml
@tests/ansible/conftest.py
@tests/ansible/test_cp_topic.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests, mock fixtures, and orchestration playbooks</name>
  <files>
    tests/ansible/test_orchestration.py
    tests/ansible/test_cp_connect.py
    tests/ansible/fixtures/mock_responses/connect/connectors_list.json
    tests/ansible/fixtures/mock_responses/connect/connector_status_running.json
    tests/ansible/fixtures/mock_responses/connect/connector_status_failed.json
    tests/ansible/fixtures/mock_responses/connect/connector_created.json
    ansible/site.yml
    ansible/playbooks/deploy-governance.yml
    ansible/vars/connectors-example.yml
  </files>
  <read_first>
    ansible/roles/cp_topic/tasks/main.yml
    ansible/roles/cp_topic/defaults/main.yml
    ansible/roles/cp_topic/meta/main.yml
    ansible/roles/cp_topic/molecule/default/molecule.yml
    ansible/roles/cp_topic/molecule/default/converge.yml
    ansible/roles/cp_topic/molecule/default/verify.yml
    ansible/roles/cp_schema/tasks/main.yml
    ansible/roles/cp_rbac/tasks/main.yml
    ansible/vars/sla_tiers.yml
    tests/ansible/conftest.py
    tests/ansible/test_cp_topic.py
    tests/ansible/fixtures/mock_responses/
    ansible/playbooks/
    observability/grafana/alerts.yaml
  </read_first>
  <behavior>
    - Test: site.yml exists and is valid YAML with 4 plays (cluster, governance, connectors, observability)
    - Test: site.yml play 1 imports cp-ansible deployment with tag "cluster"
    - Test: site.yml play 2 includes cp_topic, cp_schema, cp_rbac roles with tags [governance, topics], [governance, schemas], [governance, rbac]
    - Test: site.yml play 3 includes cp_connect role with tag "connectors" targeting kafka_connect[0]
    - Test: site.yml play 4 includes cp_observability role with tag "observability"
    - Test: site.yml tag isolation -- running --tags connectors does NOT trigger cluster play tasks (import_playbook tag inheritance mitigated)
    - Test: deploy-governance.yml is a governance-only playbook (no cluster deploy) containing cp_topic, cp_schema, cp_rbac, cp_connect roles
    - Test: cp_connect role has defaults/main.yml with cp_connect_url, cp_connectors, cp_connect_validate_health, cp_connect_health_retries, cp_connect_health_delay defaults
    - Test: cp_connect meta/main.yml has role_name: cp_connect and dependencies: []
    - Test: cp_connect tasks/main.yml includes deploy.yml and validate.yml with appropriate conditionals
    - Test: cp_connect tasks/deploy.yml uses ansible.builtin.uri with method PUT to /connectors/{name}/config with status_code [200, 201]
    - Test: cp_connect tasks/validate.yml uses GET /connectors/{name}/status with retries and checks for RUNNING state
    - Test: cp_connect tasks/validate.yml restarts FAILED connectors via POST /connectors/{name}/restart?includeTasks=true&onlyFailed=true
    - Test: cp_connect tasks/check.yml reports planned changes via GET without mutations
    - Test: connectors-example.yml contains cp_connectors list with at least 2 example connector definitions (JDBC source and sink)
    - Test: All task names start with uppercase (ansible-lint name[casing])
    - Test: All module references use FQCN (ansible.builtin.uri, ansible.builtin.include_tasks, etc.)
  </behavior>
  <action>
    **Write tests first (RED phase):**

    1. Create `tests/ansible/test_orchestration.py`:
       - TestSiteYml: Validates site.yml structure -- 4 plays, correct hosts (kafka_broker[0] for governance, kafka_connect[0] for connectors, all for observability), correct tags per play, import_playbook for cluster deploy
       - TestTagArchitecture: Validates tag hierarchy -- each play has unique top-level tag, governance play tasks have both "governance" and specific tags (topics/schemas/rbac), no tag leakage between plays. **CRITICAL (from RESEARCH.md Pitfall 1): Verify that the import_playbook for cluster deploy has its own exclusive "cluster" tag, and that no other play inherits or shares this tag. Test that each play's tags are disjoint sets at the top level to prevent --tags connectors from triggering cluster deployment.**
       - TestTagIsolation: Verify that tags used in each play do NOT overlap with tags on other plays. Specifically: "cluster" only on play 1, "governance/topics/schemas/rbac" only on play 2, "connectors" only on play 3, "observability" only on play 4. This mitigates the import_playbook tag inheritance pitfall.
       - TestDeployGovernance: Validates deploy-governance.yml has cp_topic, cp_schema, cp_rbac, cp_connect roles without cluster deployment

    2. Create `tests/ansible/test_cp_connect.py`:
       - TestRoleStructure: Validates defaults/main.yml has keys cp_connect_url (default "http://localhost:8083"), cp_connectors (default []), cp_connect_validate_health (default true), cp_connect_health_retries (default 5), cp_connect_health_delay (default 10)
       - TestDeployTasks: Validates deploy.yml uses ansible.builtin.uri with method: PUT, url contains "/connectors/{{ connector_item.name }}/config", status_code includes 200 and 201, body_format: json
       - TestValidateTasks: Validates validate.yml uses GET on /connectors/{name}/status, has retries: with until: checking for RUNNING state, and includes restart POST for FAILED connectors
       - TestCheckMode: Validates check.yml uses GET-only (no PUT/POST/DELETE)
       - TestConnectMolecule: Validates molecule/default/ has molecule.yml, converge.yml, verify.yml; molecule.yml uses delegated driver; converge.yml includes cp_connect role

    3. Create Connect mock fixtures in `tests/ansible/fixtures/mock_responses/connect/`:
       - `connectors_list.json`: `["jdbc-source-oracle-east", "jdbc-sink-postgres-reporting"]`
       - `connector_status_running.json`: Full status response with connector.state="RUNNING" and tasks[0].state="RUNNING"
       - `connector_status_failed.json`: Status with tasks[0].state="FAILED" and trace field
       - `connector_created.json`: PUT response with name, config, tasks fields

    **Run tests (confirm RED):** `pytest tests/ansible/test_orchestration.py tests/ansible/test_cp_connect.py -x`

    **Implement (GREEN phase):**

    4. Create `ansible/site.yml` -- 4-play orchestration:
       - Play 1: `name: Deploy Confluent Platform cluster` with `ansible.builtin.import_playbook: ../scenarios/cp-rhel/playbooks/deploy-cp.yml` and `tags: [cluster]`. **IMPORTANT: Use only the "cluster" tag here -- do NOT add any other tags that other plays use. This prevents tag inheritance from import_playbook causing cluster tasks to run when --tags connectors or --tags governance is specified (RESEARCH.md Pitfall 1).**
       - Play 2: `name: Deploy governance resources` targeting `hosts: kafka_broker[0]` with `tags: [governance]`. Contains 3 tasks using `ansible.builtin.include_role` for cp_topic (tags: [topics, governance]), cp_schema (tags: [schemas, governance]), cp_rbac (tags: [rbac, governance])
       - Play 3: `name: Deploy Kafka Connect connectors` targeting `hosts: kafka_connect[0]` with `tags: [connectors]`. Pre-task: `ansible.builtin.wait_for` on port 8083 with timeout 120. Task: include_role cp_connect
       - Play 4: `name: Deploy observability stack` targeting `hosts: all` with `tags: [observability]`. Task: include_role cp_observability

    5. Create `ansible/playbooks/deploy-governance.yml` -- governance-only playbook (no cluster deploy):
       - Play 1: Governance on kafka_broker[0] with cp_topic, cp_schema, cp_rbac (same tags as site.yml play 2)
       - Play 2: Connectors on kafka_connect[0] with cp_connect (tags: [connectors])

    6. Create `ansible/vars/connectors-example.yml`:
       ```yaml
       cp_connectors:
         - name: jdbc-source-oracle-east
           config:
             connector.class: io.confluent.connect.jdbc.JdbcSourceConnector
             connection.url: "jdbc:oracle:thin:@oracle-east.fsi.internal:1521/KAFKA"
             connection.user: "{{ vault_oracle_user }}"
             connection.password: "{{ vault_oracle_password }}"
             topic.prefix: "corebanking.transactions.v1."
             mode: "timestamp+incrementing"
             timestamp.column.name: "last_modified"
             incrementing.column.name: "id"
             poll.interval.ms: "5000"
             tasks.max: "2"
         - name: jdbc-sink-postgres-reporting
           config:
             connector.class: io.confluent.connect.jdbc.JdbcSinkConnector
             connection.url: "jdbc:postgresql://pg-reporting.fsi.internal:5432/reporting"
             topics: "corebanking.transactions.v1.account-transaction"
             auto.create: "true"
             insert.mode: "upsert"
             pk.mode: "record_key"
             pk.fields: "transaction_id"
       ```

    **All task names MUST start with uppercase. All modules MUST use FQCN.**
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && pytest tests/ansible/test_orchestration.py tests/ansible/test_cp_connect.py -x -v</automated>
  </verify>
  <done>
    - site.yml chains 4 plays (cluster, governance, connectors, observability) with proper tag architecture
    - Tag isolation verified: each play has disjoint top-level tags preventing import_playbook tag inheritance leakage
    - deploy-governance.yml provides governance-only execution
    - Connect mock fixtures created for all 4 response types
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: cp_connect role implementation and molecule scenario</name>
  <files>
    ansible/roles/cp_connect/defaults/main.yml
    ansible/roles/cp_connect/meta/main.yml
    ansible/roles/cp_connect/tasks/main.yml
    ansible/roles/cp_connect/tasks/deploy.yml
    ansible/roles/cp_connect/tasks/validate.yml
    ansible/roles/cp_connect/tasks/check.yml
    ansible/roles/cp_connect/molecule/default/molecule.yml
    ansible/roles/cp_connect/molecule/default/converge.yml
    ansible/roles/cp_connect/molecule/default/verify.yml
  </files>
  <action>
    Create the cp_connect role structure:

    1. `defaults/main.yml`: cp_connect_url: "http://localhost:8083", cp_connectors: [], cp_connect_validate_health: true, cp_connect_health_retries: 5, cp_connect_health_delay: 10, cp_connect_results: {created: 0, updated: 0, failed: 0, healthy: 0, unhealthy: 0}

    2. `meta/main.yml`: galaxy_info with role_name: cp_connect, author: FSI C4E, description: "Kafka Connect connector lifecycle via REST API", min_ansible_version: "2.15", platforms (EL 8/9), galaxy_tags [kafka, connect, confluent]; dependencies: []

    3. `tasks/main.yml`: Include check.yml when ansible_check_mode, else include deploy.yml then validate.yml (when cp_connect_validate_health). End with set_fact for cp_connect_results summary. Use failed_when with `# noqa: ignore-errors` for error collection pattern.

    4. `tasks/deploy.yml`: Loop over cp_connectors. For each: ansible.builtin.uri with method: PUT to "{{ cp_connect_url }}/connectors/{{ connector_item.name }}/config", body: "{{ connector_item.config }}", body_format: json, status_code: [200, 201], retries: 3, delay: 5. Register result, increment created (201) or updated (200) counts. Use `loop_var: connector_item`.

    5. `tasks/validate.yml`: Loop over deployed connectors. GET /connectors/{name}/status. If FAILED tasks exist, POST restart with ?includeTasks=true&onlyFailed=true. Re-check with retries: "{{ cp_connect_health_retries }}", delay: "{{ cp_connect_health_delay }}", until connector.state == 'RUNNING' and no FAILED tasks. Track healthy/unhealthy counts. Fail at end if any unhealthy.

    6. `tasks/check.yml`: GET /connectors to list existing. For each cp_connectors entry, GET /connectors/{name}/config. Compare with desired config using `ansible.builtin.debug` to report what would change. changed_when: false on all GET tasks.

    7. Create molecule scenario for cp_connect (follow cp_topic pattern exactly):
       - `molecule.yml`: delegated driver, default scenario, lint disabled
       - `converge.yml`: playbook that includes cp_connect role with mock cp_connect_url pointing to localhost:18083 and sample cp_connectors
       - `verify.yml`: playbook that verifies connector deployment results via cp_connect_results fact

    **All task names MUST start with uppercase. All modules MUST use FQCN. Use `loop_var: connector_item` on all loops.**
  </action>
  <verify>
    <automated>cd /Users/jhogan/fsi-kafka-platform && pytest tests/ansible/test_cp_connect.py -x -v</automated>
  </verify>
  <done>
    - cp_connect role deploys connectors via idempotent PUT with status_code [200, 201]
    - Health validation retries FAILED connectors with restart and backoff
    - Check mode uses GET-only without mutations
    - Molecule scenario exists with delegated driver
    - `grep "method: PUT" ansible/roles/cp_connect/tasks/deploy.yml` matches
    - `grep "retries:" ansible/roles/cp_connect/tasks/validate.yml` matches
    - `grep "method: GET" ansible/roles/cp_connect/tasks/check.yml` matches
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `pytest tests/ansible/test_orchestration.py tests/ansible/test_cp_connect.py -x -v` all pass
- `ansible-lint ansible/site.yml ansible/playbooks/deploy-governance.yml ansible/roles/cp_connect/ 2>&1 | head -20` shows no errors
- site.yml has 4 distinct plays with non-overlapping primary tags
- cp_connect uses PUT for idempotent connector management
- Tag isolation: `--tags connectors` does NOT trigger cluster play (verified by disjoint tag sets)
</verification>

<success_criteria>
- site.yml orchestrates full pipeline: cluster -> topics -> schemas -> RBAC -> connectors -> observability
- Tag-based selective execution works: --tags topics runs only topic tasks, no cluster leakage
- cp_connect creates/updates connectors via PUT /connectors/{name}/config (201 for new, 200 for update)
- Health validation retries FAILED connectors with backoff before reporting failure
- All pytest and ansible-lint checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-orchestration-pipeline-observability-and-ci-cd/12-01-SUMMARY.md`
</output>
