# =============================================================================
# DEPRECATED -- Replaced by per-scenario workflows
# =============================================================================
# This workflow has been replaced by:
#   - terraform-apply-azure.yml (scenarios/cc-azure)
#   - terraform-apply-aws.yml (scenarios/cc-aws)
#   - terraform-apply-gcp.yml (scenarios/cc-gcp)
#
# Each calls the reusable terraform-scenario.yml workflow.
# This file is retained for git history.
#
# NOTE: a workflow file without a `jobs:` section is invalid, and GitHub
# records a failed workflow run for invalid files on EVERY push, ignoring
# branch/path filters (it cannot evaluate filters on a file it cannot
# parse). This stub is therefore kept minimally valid: manual-trigger
# only, with a no-op job. It never runs automatically.
# =============================================================================
name: "Terraform Apply (Deprecated)"

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: echo "Deprecated -- use terraform-apply-{aws,azure,gcp}.yml (per-scenario workflows)."
