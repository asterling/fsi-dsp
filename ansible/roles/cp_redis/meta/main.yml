---
galaxy_info:
  role_name: cp_redis
  author: FSI Kafka Platform C4E
  description: >-
    Deploy and validate Confluent Redis Sink Connectors on CP / CFK /
    LinuxONE Connect clusters. Thin wrapper around cp_connect with class
    validation. Sink only -- Redis is not a source of truth per ADR-012.
  license: Internal
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: ["8", "9"]

dependencies:
  - role: cp_connect
