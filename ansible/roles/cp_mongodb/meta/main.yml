---
galaxy_info:
  role_name: cp_mongodb
  author: FSI Kafka Platform C4E
  description: >-
    Deploy and validate MongoDB Kafka connectors (source = change streams,
    sink = collection writes) on CP / CFK / LinuxONE Connect clusters.
    Thin wrapper around cp_connect with class validation. Implements
    ADR-012 MongoDB integration.
  license: Internal
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions: ["8", "9"]

dependencies:
  - role: cp_connect
